# blueAI — Decisions
Last updated: 2026-06-11 (S2 audit #2 — token/component/doc gate: +5 rows: Sparkle SSOT, tokenisation tiering rule, marketing-palette → --bai-mkt-* tokens, style-guide brand-primitive docs, rgba-wash frontier flagged)

> Design/build decisions with reasoning. Promoted from scratchpad at audit passes.

| Date | Decision | Why |
|---|---|---|
| 2026-06-10 | Built as a **Next + React + Tailwind** design-only replica from the Claude-design HTML/CSS/JS export (not Figma). | Matches WSUP/now.gg handoff pattern; the export's README explicitly said "recreate in whatever tech fits (React…)". |
| 2026-06-10 | DS tokens: the **`--bai-*` CSS vars in globals.css are the source of truth**; `tailwind.config.ts` maps utilities (`bg-iris`, `text-ink-heading`, `bg-bai-gradient`) onto them. | Honors the export's CSS-var DS; gives Tailwind utilities for new code without duplicating values. |
| 2026-06-10 | Marketing homepage sections use a **scoped CSS layer** (`src/styles/homepage.css`, scoped `.bai-home`) rather than re-derived Tailwind. | The export's CSS is already polished + token-based; fastest faithful path. DS components in /style-guide are Tailwind. |
| 2026-06-10 | **All 3 hero directions built live** + the options chooser. | Designer's explicit choice (vs only the Recommended). |
| 2026-06-10 | Hero agent motion **rebuilt React-native (framer-motion)**, NOT ported GSAP. **Phase-driven**: timed `STEPS` advance a `phase` int; `motion` components animate off it. | Designer's choice — lighter, more idiomatic handoff codebase than inheriting GSAP. Phase-driven is reliable + easy to retune. |
| 2026-06-10 | **Stage** hero uses the RICH multi-scene agent animations; **Stage Original + 3 Cards** share the LEGACY single-scene ones (separate components, same markup, CSS-sized per context; `FinanceLegacy big` swaps chart geometry). | The export branches rich-vs-legacy on markup; both non-Recommended variants are legacy. Sharing avoids 2× the scene code; do NOT merge with rich. |
| 2026-06-10 | Per-variant hero stylesheets are **scoped-by-route**; cross-hero/options links use full-page `<a>`. | The 3 hero CSS files reuse generic class names at different sizes — full-nav per route prevents cross-route CSS accumulation/collision. |
| 2026-06-10 | Fonts: **Inter** (SF Pro substitute), **Space Grotesk** (marketing display), **Bricolage Grotesque** (wordmark), via `<link>`. | The export's substitutions (SF Pro/SF Pro Display aren't redistributable); flagged in the DS README. |

| 2026-06-10 | Recommended-hero agent UX (first designer correction cycle): **instant** stage swaps (removed the directional slide — overlapped + inconsistent direction), **hover** to select a thumbnail (was click), and **slower** rich-scene pacing (STEPS + typewriter + dwell bumped). → taste rule 11. | Slide transitions read as glitchy/overlapping; hover is the expected affordance for "preview on point"; the demos played too fast to read each beat. |

| 2026-06-10 | Reconciled a 2nd DS (**blueai-pm**, extracted from the shipping web-app codebase): keep blueai-modern's brand for the marketing site; FILL gaps from PM. Added the semantic **status** colors + interactive **accent `#1990FF`** (modern had neither) to the token layer + `/style-guide`. Lateral contradictions (gradient angle, body-font substitute) kept as modern, PM noted; PM's app components/icons/copy = the reference for future app surfaces. | Designer: fill modern's missing details from PM; on contradiction take the more mature. PM is mature on system coverage, modern on brand polish → split by surface (marketing = modern; app/system = PM). |

| 2026-06-10 | Routing + style-guide restructure (designer): `/` → **redirects to `/style-guide`** (default address = the DS during the design phase); style guide rebuilt with the **WSUP sidebar architecture** (grouped Foundations/Components/Pages nav + scroll-spy); Recommended hero moved `/` → `/hero/stage`; hero-options is the "homepage"/review hub until a variant is finalized (then it becomes `/`). | Designer wants the DS front-and-center while choosing a hero direction; the chosen variant promotes to the homepage later. Mirrors how WSUP's style guide is the working hub. |

| 2026-06-10 | **DS UX audit + completion** (designer: "make the DS complete, think like a UX designer; have we incorporated all of PM?"). Completed the **radius** scale (badge/chat/credits added → 7 + bubbles), the **shadow** scale (tokenized the brand `cta`/`cta-hover` glow that was inlined on every CTA + `brand-sm`), tokenized the **cta gradient**, and a full **badge system** (status chips · pill badges WITH outlines · outline tags · gradient credits pill). Then **incorporated PM's app-component sections** into /style-guide — Cards · Overview cards · Inputs & forms · Navigation · Credits (button/alerts/modal) · Icons (Heroicons) — + PM's interactive **indigo** token. | WSUP maturity bar (multiple radii/shadows) + PM authoritative specs (badges-status / spacing-radius / shadows previews). The webapp UI-kit screens (Dashboard/Chat/Jobs/Skills) are the LIVE product app, left as reference — not rebuilt as DS sections. |

| 2026-06-11 | **Recommended-hero animation model finalized** — spotlight loop (one active scene; keyed remount replay; inactive rest at FINAL state; `initial={false}` so idle cards don't replay their entrance) across both rich (Stage) and legacy (3-Cards/Stage-Original) heroes; scene footers pinned via flex `margin-top:auto` (not absolute). | Designer's spec: only the highlighted scene animates, the rest sit at their completed state; loop + hover-restart. Absolute footers overlapped flowing content; flex pin + measured slack fixed it. → KB Motion + Spotlight. |
| 2026-06-11 | **Text-wrap discipline adopted** — `balance` for headings/short marketing lines, `pretty` for body with full-stop breaks; never cap body width below its container; size to fit one line, no wrap unless truly no room. | Designer corrected `balance`→`pretty` for body (`balance` split a phrase mid-line). → taste rule 14. |
| 2026-06-11 | **Unified content-width token** `--bai-content: 1180px` (40px gutter) across nav + all 3 heroes + the homepage body. | Content edges marched inward down the page (nav → hero → body); one shared token aligns every band. A flex item with `margin:auto` needed `width:100%` to stretch. → KB token/layout hygiene. |
| 2026-06-11 | **HeroNav consolidated to ONE shared component** + showcased as a live `/style-guide` section; per-variant padding via `--nav-pad-y`. | Was 2 markup copies + 3 CSS copies. Designer: "just 1 + reported to style guide." Duplicated-across-variants = DS smell. → KB Components + taste rule 16. |
| 2026-06-11 | **Stage hero prod/dev text-align mismatch fixed** + systemic finding logged: production CSS chunking leaked hero-cards.css's generic `.hero{text-align:center}` onto `/hero/stage`; `.hero-right` inherited it (center on Vercel, left in dev). Guarded with own-declaration `.hero-right{text-align:left}`; proper fix (scope variant stylesheets under unique root classes) deferred + flagged. | Confirmed on the live bundle. An own declaration beats an inherited value regardless of chunk order; `.hero-right` is Stage-only. The cheap guard can't reach the leaked `.hero` padding → scoping refactor recommended. → KB CSS architecture; evolution active gap. |
| 2026-06-11 | **blueAI deployed** — GitHub `arpityadav-bst/blueai-screen-library` (public) + Vercel `blueai-screen-library.vercel.app`, git-connected auto-deploy, same Vercel team as WSUP. Two prod build fixes en route (framer `init` typed `TargetAndTransition`; the CSS leak above). | Designer asked to deploy like WSUP. `next build`'s strict TS caught what `next dev` didn't. → project-insights Deployment. |

| 2026-06-11 | **Scoping refactor DONE** — scoped all 3 hero stylesheets under unique root classes `.v-stage`/`.v-cards`/`.v-original` (added to each page wrapper), via an idempotent `.scripts/scope-css.js` postcss transform; `:root`+`@keyframes` kept global. | Root-cause fix for the recurring prod CSS-leak category: Next co-bundles the client-component CSS so generic `.hero`/`.cv-trend`/etc. leaked across routes (text-align + the Stage-Original "Trending" pill→full-width bug). Scoping makes cross-route leaks structurally impossible. Verified: built bundle has zero unscoped generic hero rules + confirmed live. Mirrors `.bai-home`/`.ho`. → KB CSS architecture. |

| 2026-06-11 | **Markets = 4th agent card** in the 3-Cards hero (🎯, "A prediction market agent that watches the odds") — new `MarketsLegacy` scene (pick market → read live odds → one swings past threshold → alert lands), same spotlight loop; grid 3→4 cols via `minmax(0,1fr)`; pill "3"→"4 Agents Live Now". | Designer: add a 4th agent, single row of 4. Added to 3-Cards ONLY (Stage + Stage-Original still show 3). → KB. |
| 2026-06-11 | **Content width refined → full-bleed nav + contained 1280 body.** Superseded the earlier "every band = 1180" idea: the 3-Cards/SEO bands were widened, then aligning everything to the wide header (1640) read too wide → settled on a full-bleed nav over a `--seo-content: 1280px` content column (hero + sections + footer). | Aligning ALL bands to the wide header hurt UX (line length, sparse grids); a full-bleed nav over contained content is the standard pattern, not misalignment. → taste rule 20. |
| 2026-06-11 | **NEW SEO Homepage (`/seo`)** — standalone search-optimized page from the PM mock: hero (2×2 animated agents) · What-is (featured snippet) · chatbot/assistant/worker frame · 8-card task hub (internal links) · 4 steps · FAQ accordion · CTA · footer. Scoped `.v-seo`; **FAQPage JSON-LD** server-side from the FAQ source; scroll-reveal + ambient backdrop. | PM's SEO mock + the JSON-LD lever (competitors have FAQ copy, no schema). Reused the legacy scenes (CSS duplicated under `.v-seo`). → KB SEO page + project-insights. |
| 2026-06-11 | **Brand canonicalized (SSOT)** — official logo PNG in every nav + both footers; a single `<Wordmark/>` (full iris→cyan gradient, "BlueAI" always one word); canonical Download-CTA sparkle. | Was 3 drifting wordmark treatments + a hand-drawn inline-SVG logo. Designer's brand rules. I first misread the wordmark as two-tone; designer corrected to the full gradient. → taste rule 19. |
| 2026-06-11 | **Root `/` = Screen Library index** (replaced the redirect-to-style-guide); the style guide is now DS-only (dropped the "Pages" group). | Designer: mirror WSUP's root page directory; the index owns page navigation. → project-insights. |
| 2026-06-11 | **SEO mobile pass** — hamburger nav (overlay menu that doesn't push content); backdrop sparkle pinned top-right as a faint spinning gear; full-width hero CTAs; FAQ in a frosted-glass panel. | Mobile UX audit (screenshots at 390px). A section-anchor nav needs a real mobile menu, not hidden links. → taste rule 22 + KB mobile. |
| 2026-06-11 | **Ambient living backdrop** (`SeoBackdrop`) on `/seo` — drifting iris/cyan/blue orbs + a faint rotating logo sparkle, scroll-driven, reduced-motion-safe. | Designer: the sections "felt dead." Calm ambient delight, not literal gears (brand = trustworthy utility). → taste rule 21. |

| 2026-06-11 | **Sparkle extracted to a shared brand primitive** (`components/Sparkle.tsx`) — the canonical lucide-"Sparkles" CTA icon was inlined in 6 files (DownloadCta, HeroCta, HeroStage, SeoHero, SeoCta + a scene). Now ONE source, used by all 5 CTAs; the scene's simpler "generate" glyph (no accent marks, different role) was intentionally left. Sits beside `<Wordmark/>` as the two brand primitives. | Designer's contract: "ALWAYS this wordmark and sparkle icon." 6 copies = 6 drift risks → "always" was only true by luck. Componentising makes it true by construction. → reasonings "brand primitives as SSOT". |
| 2026-06-11 | **Tokenisation tiering rule** (the gate's operating principle): Tier 1 — a literal equal to a DS-primitive value (iris/cyan/ink/accent) is always a leak → tokenise; Tier 2 — a color used on >1 surface (the marketing slate/blue/green) → promote to a global token; Tier 3 — bespoke scene-illustration colors (fake-chart fills, creator-pink, success-ink) → stay LOCAL (promoting one-offs pollutes the DS). | "Fully tokenised" ≠ "every hex is a token." A senior designer tokenises design *decisions*, not decoration. Stating the boundary beats silently skipping Tier 3. |
| 2026-06-11 | **Marketing-surface palette → global `--bai-mkt-*` tokens** (slate #0F172A · blue #2F6DFF · blue-2 #3D7BFF · green #16A34A · green-wash). Was redefined identically as `--bh-*` (homepage), `--seo-*`/`--green` (SEO), `--slate-900`/`--green` (3 heroes + options) across 7 files; now one source, per-file locals alias it. `--bh-orange` folded into `--bai-jobs` (same value). Zero visual change; build-verified. | The actual color the homepage + SEO render in was a duplicated literal, not a token — and the style guide didn't document it. Cross-surface color = token, by definition. |
| 2026-06-11 | **Style guide now documents the brand primitives + marketing palette** — added a "Download CTA" component card (renders the REAL `<DownloadCta/>` under a scoped `.bai-home` wrapper, no replica) + a "Marketing surface" color group in Foundations. | Most-visible page rendered in colors/components the DS reference never showed. Render the real component (not a hand-built swatch) so the doc can't drift. → project-insights. |
| 2026-06-11 | **Tokenisation frontier CLOSED (designer green-lit) — rgb-channel tokens.** Added `--bai-iris-rgb` / `--bai-cyan-rgb` / `--bai-glow-rgb` (#5F46FF, the brand shadow-glow purple) + `--bai-mkt-blue-rgb` / `--bai-mkt-blue-2-rgb` / `--bai-mkt-green-rgb`. The brand RGB now lives in ONE place: solids DERIVE from the channel (`--bai-iris: rgb(var(--bai-iris-rgb))`) AND every `rgba(…)` wash/glow/overlay across all 7 stylesheets + globals references it. Build GREEN, zero visual change. | Completes Tier 2 for alpha washes. LEFT (stated boundary): `.tsx` rgba in SVG `<stop>` stops, Tailwind-arbitrary shadows, and one inline page-bg gradient — `var()` is unreliable/fragile in those contexts. |

| 2026-06-11 | **Replicated the 6 bluestacks.ai inner pages via isolated Edge CDP** (S3). Launched a separate Edge (port 9333, own profile) so the designer's 2 open windows were untouched; inspected each live page (shots + outline + exact copy), then rebuilt faithfully in our DS. Routes use the live slugs. | Designer: "create them exactly using inspect… faithful look, our DS underneath." The live pages were extracted from the same source as our DS, so re-expression in `--bai-*` tokens is 1:1. → project-insights, KB (CDP workflow). |
| 2026-06-11 | **Shared marketing chrome + AgentShell template.** One `SiteNav`/`SiteFooter`/`SiteFaq`/`SiteReveal` (scoped `.v-site`) for all 6 pages; the 4 agent pages share `AgentShell` + per-agent data; hero demos are faithful STATIC reps (design-only). Copy normalized "Blue AI"→"BlueAI"; curly quotes. | DRY across 6 pages; agent pages share a template differing only in hero demo + feature + copy. Static demos match the live (forms/widgets), not our animated hero scenes. |
| 2026-06-11 | **Agent cards + Social Rewards/Developer now navigate to the new pages.** SeoAgentGrid + HeroCards cards became `<a>` (click → agent page; hover still previews the scene). SEO nav Social Rewards/Developer → internal routes, dropping the ↗ external icon (they're real internal pages now, same-tab, matching live). | Designer wants the 4 agents + Social Rewards/Developer to open their pages. The ↗ was for stubs going "elsewhere"; obsolete now. Flagged the same-tab change for the designer. |

| 2026-06-11 | **ONE shared `<MarketingHeader/>` on every page** (S3-cont). The SEO homepage + the 6 inner pages now all render the same header (`header.css`, scoped to the header element `.bai-hdr` so it's route-scope-independent). Section links → `/seo#…`; **Social Rewards + Developer are ↗ external / new-tab** (restored per the designer). Replaced + deleted SeoNav + SiteNav; pruned site.css nav block. | Designer: "header same on all pages like the SEO homepage." Per-page navs drift → a single header component is brand-SSOT for the nav (like `<Wordmark/>`). The ↗ marks the two pages as distinct destinations the designer wants opened in a new tab. |

| 2026-06-11 | **ONE shared `<MarketingFooter/>` on every page** (S3-cont) — mirrors the header. `footer.css` scoped to the footer element `.bai-ftr` (route-independent). Brand lockup + tagline + the 3 brand-colored social icons + columns (Live agents · **Product: Social Rewards / Developer / Try Skills** · Learn · Company, real internal hrefs) + copyright. Replaced + deleted SeoFooter + SiteFooter. `SOCIAL` moved to site-data (shared by header + footer). | Designer: "same footer on all pages, with Social Rewards/Developer + icons." Header + footer are now both shared-SSOT chrome; the footer carries the same brand-colored social as the header. |

*(New rows accumulate at audit passes.)*
