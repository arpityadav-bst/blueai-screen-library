# blueAI — Decisions
Last updated: 2026-08-11 (Session-17 promotion pass — the 12-item design batch, the independent Key-saved review, and the toast redesign across three designer-critique rounds. 17 rows added; see session-logs.md for the full narrative and `designer_caught_count`.)

> Design/build decisions with reasoning. Promoted from scratchpad at audit passes.

> **Marketing-site era decisions (2026-06-10 → 2026-07-03) live in
> [`decisions-archive-marketing.md`](decisions-archive-marketing.md)** — 72 rows, archived 2026-08-11 to bring
> this file under the 100-row threshold. Dormant scope, not retracted: the 2026-07-25 pivot put the marketing
> site to sleep in favour of blueai-desktop, and those rows are preserved verbatim for if it ever wakes up.
> **This file now holds the ACTIVE surface only — `/blueai-desktop`, 2026-07-24 onward.**

| Date | Decision | Why |
|---|---|---|
| 2026-07-24 | **blueai-desktop: AI Credits screen's Prime-upsell branch rebuilt** — replaced the flat centered-text stack (eyebrow + title + number + inert link + 2 dividers + warning + button) with a bordered `.bai-set-card` reusing the header's own ring-gauge technique (bigger, via new `.bai-credit-ring.lg`), the empty-balance warning nested INSIDE that same card, and a single `.bai-set-card-head` for the upsell pitch instead of 3 separate lines competing for the same accent color. | Designer: "doesn't look UX optimised... modernistic and creative." Root cause: the screen had invented its own plain-text visual language for a balance the header pill already shows with a real gauge, so the two never read as the same product; it was also the one sibling branch reverting to centered-text+dividers while its neighbors (BYOK-active, Prime-member) already used bordered-card anatomy. |
| 2026-07-25 | **Extended the ring+card treatment to the other 3 branches** (Prime-member, non-Prime-geo, BYOK-active+Prime-paused) for consistency; added a `.bai-set-btn.hero` modifier (41px min-height, 12.5px/700 — matching `.bai-modalact`'s weight) applied ONLY to each screen's sole primary CTA via a dedicated class, not by widening the shared `.bai-set-btn.primary` (used by ~6 unrelated small confirm buttons elsewhere in Settings that are correctly small in their own context). | Designer: "update other states too similarly," then separately flagged the CTA-weight mismatch against the OOC modal's buttons. Matching ROLE (the one hero action) rather than CLASS NAME kept the fix scoped to buttons that actually needed it. |
| 2026-07-25 | **Added the missing "Add / manage own key" action to the Prime-member branch** — it had no BYOK path at all, wrongly implying BYOK is only for people without Prime. | Designer: "even when you have credits and you are a prime member... we should give an option to add your own key right?" → project-insights "BYOK/Prime coexistence." |
| 2026-07-25 | **Removed 2 redundant dividers**: before "MORE WAYS TO GET AI CREDITS" (the label's own 24px top-margin already signals the new section — confirmed against 6 sibling labeled sections on the Settings home screen, zero dividers between any of them) and between the BYOK-active + Prime-paused cards (converted that branch to the same `gap:10` flex-wrap pattern as the other 3 — two bordered cards already separate themselves). | Designer asked "do you think the divider is needed here?" on two different screens/branches — same answer both times once checked against precedent. → taste rule 38. |
| 2026-07-25 | **Fixed a real proximity-inversion bug** — "CREDIT BREAKDOWN"'s label margin (`0 0 8px`) was stacking with its new flex-wrap's `gap:10` instead of being netted against it, so the label sat 18px from the table it labels but only 10px from the unrelated card above it (backwards). Netted the margin to `-2px` → 10px above / 8px below. | Designer asked to check "something closer to X but more related to Y" — found via `getBoundingClientRect` measurement, not eyeballed. → knowledge-base "flex-gap+margin stacking." |
| 2026-07-25 | **Header credit hover-popover fixed on 3 fronts**: `.bai-credits-wrap`'s `cursor:default` (despite being genuinely clickable — opens the AI Credits screen) corrected to `cursor:pointer`; added a "View details ›" affordance line inside the popover; fixed a real "closes before you can reach it" bug by wiring mouseenter/mouseleave to BOTH the trigger and the popover (plus a 150ms grace timeout) — the popover's `bottom:calc(100% + 6px)` positioning left a real screen-space gap the cursor had to cross that the trigger-only listener didn't survive. | Designer: "nothing tells the user that you click here too," then "it closes before I reach it." Cursor = the ambient pre-hover signal; the popover line = the explicit signal; the gap-crossing fix = why the explicit signal couldn't be reached once added. Verified with real stepped `mouse.move()`, not scripted class toggles. → knowledge-base "hover-menu reachability testing." |
| 2026-07-25 | **Removed the divider I'd just added under "View details ›"** — its own accent color + bold weight + trailing chevron already read as "this is the action," same job a divider would redundantly repeat. | Designer: "do we need a divider here?" — self-caught precedent check (same rule as the two dividers removed earlier the same session), applied to my OWN newest addition. → taste rule 38 (3rd instance this session). |
| 2026-07-25 | **OOC modal redesigned**: mode-specific titles (byok: "Add your own key" / topup: "Out of Prime credits" / prime: "Out of free credits") replacing a flat "Out of credits" in all 3 modes; new `.tgm-ic-acc` header icon (key / ✦) reusing the credits screen's own accent-chip recipe; the one concrete number each mode offers (renewal date / credit count) pulled from flat prose into inline accent-bold emphasis. Deliberately did NOT port the ring-gauge or card structure from the credits screen — this modal only ever appears at `credits===0`, so a gauge would always render empty. | Designer: "make this more aesthetic too plus the intention... we say out of credits in all cases but it should be more action-personalised." Matching visual FAMILY isn't matching ROLE — same reasoning that rejected `.bai-plancard` reuse earlier. → reasonings "match the fix to the surface's role." |
| 2026-07-25 | **Header credit hover-popover, 2 more fixes**: moved "View details ›" into a new `.cr-headrow` (same row as "CREDITS," right-aligned) instead of the bottom of the stack; removed the "★ PRIME" badge entirely, replacing it with a `.cr-sub-prime` accent-color modifier on the existing plan-line text ("Prime"), applied only when `primeGeo && creditsPrime && !byokActive` so the BYOK-active message doesn't wrongly inherit it. | Designer: "view details can come on top right... we don't need PRIME tag in it... we already have that below the bar, we can just maybe have it in primary color." Two distinct lessons, both promoted: placement (front-load actions in transient/hover UI — reasonings.md) and redundancy (a badge restating a neighboring fact is the same failure as a redundant divider, just a different primitive — broadened taste rule 38 to cover any repeated signal, not just dividers). |
| 2026-08-01 | **blueai-desktop got its own design system** (it had none — every DS artifact in this repo documented the dormant marketing site). Three phases: extracted the 918-line inline `<style>` into `blueai-desktop.css`; built `style-guide.html` as a *view* onto it (links the real sheet + icon set, renders real components inside a tokens-only `.bai-scope` alias, computes every number at load); then documented 19 sections to 77% self-measured class coverage. | The DS had to BE the product's stylesheet rather than a second copy, because a copy is what drifts. Everything derivable from the stylesheet is therefore automatic; everything derivable from index.html's JS (markup, copy, states) is not, and that gap is where 100% of the real defects came from. |
| 2026-08-01 | **Built `ds-drift-check.js` as a gate**, and INVERTED vda-core's deferred-Gate-5 cadence for this project: specimen sync is inline/same-edit here, not deferred to an audit. | Deferring the sync when specimens are hand-written doesn't cause staleness, it causes **fabrication** — the later pass writes what it remembers. That is the literal mechanism behind three escalating incidents (invented icon → invented copy → invented UI state). The check makes the one unautomatable part pass/fail; the cadence change removes the window in which memory substitutes for reading. |
| 2026-08-01 | **Size scale: 20 font-sizes → 6 text steps + 4 display + 2 glyphs; 15 radii → 9 tokens.** Tranche 1 = 101 sub-threshold merges (text ≤11px merges UP to protect legibility, above that toward the higher-count value). Tranche 2 = tokenised all 294 declarations, verified as zero computed drift. Then merged the last half-step (10.5→11). Tranche 3 (6 visible changes) sent to the designer as an interactive accept/reject artifact rather than applied. | Colours were tokenised while sizes were raw literals, so every new component picked its size by copying the nearest neighbour — the drift was self-replicating and would regrow after any one-off cleanup. Tranche 3 was withheld because those 6 change how things LOOK, which is a taste call the designer owns; one of them (16px modal corner) has a legitimate opposite answer. |
| 2026-08-01 | **Corrected two of my own overclaims**, both surfaced by an independent multi-agent audit rather than by self-review (this row said "14-agent" while reasonings.md said "six" for the same run — neither was re-derived from a record, so both counts are now removed rather than one being picked): `blueai-icons.js`'s "neither file keeps its own copy" (23 of 29 paths were still inline in the product), and a token list whose values were live but whose NAMES were hand-written (a new token would have been invisible forever). | Self-review checks work against intent; it cannot check intent against reality, because I wrote both claims and believed them. A false claim inside the file whose job is preventing false claims is the worst possible place for one. → reasonings "have something that didn't build it check the claims". |
| 2026-08-01 | **Tranche 3 applied by designer selection, not by my judgement** — 4 of 6 visible size/radius proposals accepted (modal 16→12, login gate 14→12, all full-width CTAs 10→8 retiring a bespoke button radius, two sub-11px interactive labels →11px); 2 declined (wordmark 14→15, credit figure 38→36) and their reasons recorded in a comment beside the token block. Radius scale 9→6 tokens. | Tranches 1-2 were provably invisible (zero computed drift), so they were mine to make. These six *changed how things look*, which is the designer's call — and #1 had a legitimate opposite answer. Recording the DECLINED reasons next to the code is what stops a future pass "helpfully" re-proposing them. |
| 2026-08-01 | **Removed `font-weight` from the skill switcher's selected state; one-word labels + a shared explainer line.** Selection is now encoded in colour only. | Two problems wearing one symptom. The reflow was a *metrics* bug: bold on an equal-width `flex:1` option tips the longest label to a second line, so selecting a tab resized the row. The wrapping the designer noticed was a *pattern* problem underneath it: three 17-19 char labels each trying to be both name and description, and only one of the three explaining itself — in the body, after you'd already committed. Widened taste rule 41 to cover states, not just text roles, because the rule already existed and had never fired. |
| 2026-08-01 | **Named every icon path (44 occurrences, 0 left inline) via three call shapes**, rather than forcing all of them through `iconSvg()`. | Four stroke weights are in play (1.6/1.7/1.8/2) *[correction, same day: measured EIGHT file-wide, five on the static nodes — see blueai-icons.js's header; even this row's list of four omitted 2.2]* and `iconSvg()` hardcodes 1.8, so the uniform-looking refactor would have silently restyled the header and sidebar. Static markup keeps its authored attributes and hydrates from `data-bai-icon`; JS-built HTML splices `BAI_ICONS.x` in at render time, because a one-time hydration pass cannot reach nodes that do not exist yet — v1 got that wrong and rendered six icons empty. Verified by snapshotting 362 rendered SVGs before and after: identical attributes, paths and sizes. |
| 2026-08-01 | **Added `ds-drift-check.js` §9 (icon choice) and a scope footer stating what the suite does NOT verify.** | §3 only asked "is this a real icon" — so the guide showed a `gear` on the Ask-BlueAI tab for weeks while every check stayed green. Existence checks cannot catch a wrong-but-valid value. §9 cross-references the icon against the product's own choice by anchoring on the label, which only became possible once every path had a name. The footer exists because the banner used to read "no drift detected" over a suite that is mostly plausibility checks: **a green light over an unexamined area is worse than no light, because it discourages the human check that would have caught the thing.** |
| 2026-08-01 | **Deleted stale numbers from CLAUDE.md instead of correcting them.** | Fourth instance of the same failure (three in the parity-audit thread). A live-tallied figure copied into prose is a correct statement with an unrecorded expiry date. → reasonings "a measured number belongs only in the thing that measures it". |
| 2026-08-03 | **Scheduled's task form rebuilt to the live product's full field set** — Name + Prompt as separate fields, Execution Mode and Repeat Type as two controls, Start + End time, and three repeat-dependent sub-fields. | The old form conflated a task's name with its prompt in one textarea, and conflated *how long* a task runs with *how often* in one seg where "Once" was an execution mode hiding among three cadences. Splitting both is what made the conditional matrix expressible at all. |
| 2026-08-03 | **Four things deliberately NOT copied from the live product**, each against a codified rule: its centred modal (→ Scheduled's own subpane), its "Scheduled Tasks" header band + "+ New" (taste rule 45 retired that here), the asterisk on six of seven labels, and End Time's "Not applicable for …" helper line. | The subpane is the established create shell and a 565px modal has no analogue in a 290px drawer. The last two are rule 38: required-ness is carried by Create's disabled state, and the End Time field states its own non-applicability in place, because the Execution Mode explainer already says why. Named rather than buried, so the designer can overrule any of the four in one word. |
| 2026-08-03 | **Removed `.bai-newitem`, the form's card wrapper** (→ `.bai-schedform`, no chrome). Designer catch. | Two faults in one element: a redundant container (the subpane already draws that boundary — rule 38) and a double-paid gutter (its 14px margin on top of `.bai-subpane-body`'s 12px put the card edge at 27px and its fields at 40px, against 13–15px everywhere else — taste rule 37). Months-old code that only became visibly wrong when the form grew from 3 fields to 7. → **taste rule 46.** |
| 2026-08-03 | **Repeat Type became a 4-across seg and Select Days one row of seven**, reversing the 2×2 grid and 4+3 wrap shipped hours earlier. | Those two layouts were honestly measured as forced at 179px — and 179px was the width the removed wrapper left behind. At the real 264px neither is forced. → **reasonings: "a measured constraint inherits the wrongness of whatever produced the measurement."** |
| 2026-08-03 | **Extracted the "selected" state into one shared rule** across `.bai-seg-btn.active` and `.bai-daychip.on`, declared as a `ds-drift-check` §12 family. | The style guide asserted in prose that "selected reads the same everywhere on this form" and nothing enforced it — the exact defect §12 exists to catch. Three pick-one controls stack on this form, so a drifted selected-state would read as incoherent. Proven invisible first (24 computed readings, zero drift), both failure branches negative-tested. |
| 2026-08-03 | **`nextOccurrence` rewritten from iteration to arithmetic.** | An independent reaudit found it could display a "Next run" *before* the task's own start time (Monthly re-anchored on the start's month and never compared against the start), and that its loop guards, when exhausted by an old start date, *returned the partial result* — a past date, and for Weekly a disallowed weekday. A guard that stops a hang by emitting a wrong answer has converted a hang into a lie. No loop now exceeds 7 iterations, so no guard is needed. |
| 2026-08-03 | **Escaped both user-authored fields at every `innerHTML` interpolation site** (`escHtml`). | The prompt echo added that day interpolated raw user text into the card's markup — a reaudit landed a working `<img onerror>` in it. The same expression already escaped quotes for the title attribute, which made the omission a defect rather than a convention. Task *name* was raw too (pre-existing) and is fixed in the same pass, including in the delete-confirm. |
| 2026-08-03 | **A tab change now tears down both subpane shells** (`setMode` → `closeAllSubpanes`). | Opening the New Task form and clicking Chat left the form layered over the Chat pane, opaque and clickable. Pre-existing, but growing the form to seven field groups made the stranded overlay cover most of the drawer. The fix went in `setMode` — the single funnel every tab change passes through — because the two existing callers sat outside it, which is precisely why the tab strip was never covered. |
| 2026-08-03 | **Account button always visible; logged-out click opens the sign-in gate directly** (icon swaps kebab↔person). | `openLoginGate()` had exactly one caller — sending a chat message — and Account was hidden entirely when logged out, on the one button whose other job IS signing in. Designer chose "skip the menu, open the gate directly" over a logged-out Profile screen. An icon must describe the ACTION a control performs, not a fixed identity — this button does two different things depending on session state. |
| 2026-08-03 | **Retracted an unverified claim**: cited "ChatGPT/Notion/Linear do exactly this" from memory as precedent for a color decision, with no screenshot in hand. | Designer posted two real references; both showed the opposite of what I'd claimed (near-uniform sidebar/header/content tone, contrast reserved for the selected item). Citing a competitor's pattern from memory is exactly as unverifiable as inventing a copy string — same discipline applies: screenshot it or don't claim it. |
| 2026-08-03 | **Sidebar split off `--bai-panel` into its own token**, landed barely darker than canvas (dark theme only, first pass). | Acted on the corrected reference above. `--bai-panel` kept for floating menus/tour card (a genuinely elevated role, `--bai-menu-shadow` and all) — giving the docked sidebar that same tone was matching visual family, not role. |
| 2026-08-04 | **Light theme given the same barely-darker sidebar treatment**, same delta magnitude as dark. | The first pass reasoned light's existing gap "wasn't the one screenshotted" and left it at pure white. Designer: the reference was a philosophy, not a value scoped to one theme — a philosophy adopted in one theme and left out of the other isn't adopted. |
| 2026-08-04 | **Reversed the pairing: header+sidebar now share ONE tone (renamed `--bai-shell-bg`), barely LIGHTER than the chat canvas, not darker.** | Designer: header and sidebar are both nav chrome (logo, settings/help/account, mode-switcher, recents) — the same functional role — while chat is the workspace, the thing that changes per-conversation. The prior pairing (header+chat) grouped by physical adjacency, not role — the exact failure already named in this notebook, caught in a decision it made one day earlier. Darker reads as more depth/recessed, which is why the lighter shell sits above the content well rather than level with it. |
| 2026-08-04 | **Reverted `.bai-header`'s CSS background to none; the wide-mode shell tone is now painted by the boot canvas itself** (`boot.js` `HDR_BG`/`setWide()`). | The header/sidebar tone change had given `.bai-header` an opaque background, which silently occluded the canvas underneath it — the thing that actually renders there: a continuously-repainted live logo, ambient sparkle packets, and heartbeat pulses, visible only because the header has never had a background of its own. Verification had checked color VALUES matched, never whether an opaque layer would occlude a completely different rendering mechanism behind it. |
| 2026-08-10 | **Generalised confirm dialog fixed on an independent review's findings**: focus trap on `#baiDialog` (Shift+Tab from the first button now wraps to the last instead of escaping to the control behind the scrim); the destructive-commit's `closeDialog()` now runs the action BEFORE closing and falls back to the trigger's own SURFACE (not `<body>`) when the commit destroys the trigger; `.bai-dialog-body` got `overflow-wrap:anywhere` (a 78-char skill name measured 463px in a 202px column); body colour `--bai-dim`→`--bai-muted-2` (4.22:1 failed WCAG AA); focus ring `--bai-line-focus`→`--bai-text-bright` (was 1.93:1, defeating the safe-focus mechanism it exists to protect); `.bai-dialog-cancel` background `--bai-input-bg`→`--bai-pill` (in light theme `--bai-input-bg` is `#ffffff`, identical to the card — the button was invisible). | Reproduced every failing case named by the review rather than trusting the fix by reading the diff — the ring finding turned out to be a measurement artifact (sampled `currentColor` on an unfocused button; the real `:focus-visible` ring measured 13.8:1/17.8:1), while the focus-restore guard's fault was real: it correctly declined to restore a destroyed trigger, then left focus on `<body>` anyway, the exact outcome it was added to prevent. |
| 2026-08-10 | **`flipIfClipped` replaced with `placeFloating`** — picks a side, then CLAMPS `max-height` to whatever room actually exists there, and can PORTAL the overlay out to `.bai-ui` when neither side has room. The date/time picker now portals; the Settings row dropdowns don't (their 170px CSS cap already fits their clipper — the clamp was the only thing they were missing). | The 258px calendar had 195px below its field and 243px above, both inside `.bai-subpane-body` — `flipIfClipped` correctly declined to flip (above wouldn't fit either) and the picker rendered 75px past the app window, its last two week-rows unclickable. Choosing the less-bad of two failing sides is not a fit guarantee; only a clamp or an escape from the clipper is. |
| 2026-08-10 | **`placeFloating` converts every measured delta into LOCAL px** via `clip.offsetWidth / rect.width`, before writing any `style.left/top/width`. | `#scaler` applies a CSS transform to the whole app, so `getBoundingClientRect()` returns SCALED viewport pixels while `style.*` and `offsetWidth/offsetHeight` are unscaled. Mixing the two spaces produced a 279px picker on a 305px field at one window size and a 36px picker on a 110px field at another — not a rounding error, a category error. → **knowledge-base**, now a confirmed 2-instance trap (see `icon-target-audit.js` below). |
| 2026-08-10 | **Selecting Auto in AI Mode now opens the Hybrid setup modal directly**, gated to `hybridStatus === 'idle'` (never fires over a live download/running/paused state). | Choosing Auto WAS the intent to set it up; the card's own second "Download" button made the user say the same thing twice. The gate is the one thing that keeps the fix from becoming a bug: re-selecting Auto mid-download must not throw a dialog over state the user is already watching. |
| 2026-08-10 | **New non-blocking credits-added surface, v1**: a bottom-right card (`.bai-creditnote`), close button only, no auto-dismiss, deliberately NOT `.bai-dialog`/`.bai-tgmodal` (both own a scrim+focus-trap because they ask a question; this states a fact and asks nothing). Superseded two rounds later — see below. | The app had a passive drain-to-*zero* notification (the OOC modal) and nothing for the opposite event; credits arriving was silent. Position and no-timer were both later reversed by the designer once the surface was redesigned as a transient banner rather than a card — see the toast-redesign rows; recorded here anyway because the reasoning that held for THIS shape (card, read at leisure) is real and the sibling `reasonings.md` principle exists specifically to keep it from being misapplied to the next shape. |
| 2026-08-10 | **`startFlow('plan', 120)`, never `startFlow('plan', 0)`.** | `startFlow`'s signature is `initialDelay \|\| 800` — a literal `0` is falsy in JS, so it silently falls through to the 800ms default instead of meaning "no delay," and `proceedWithReply()` has already spent its own thinking beat before calling this, so an extra 800ms reads as the reply hanging. 120ms is enough to not collide with the spinner's removal. → **knowledge-base**: any `x \|\| default` argument convention in this codebase is a footgun for the one legitimate falsy value that convention can't express; grep before assuming "0 means off." |
| 2026-08-10 | **"plan my day" now runs a 5-line FLOW** (ack → context-check → claim → result → streak-warn → queue-remaining → done) instead of `demoPlan()`'s single card, with deliberately uneven line lengths and non-uniform `think` pauses. | One card read as the agent quitting the instant it finished thinking aloud — "multiple responses… should produce more than one reply" (designer). Routed through the existing `FLOWS` engine rather than a hand-rolled timeout chain, because that engine already sequences multi-message replies correctly and duplicating its sequencing by hand is exactly how the two would drift apart. → **reasonings**: "scripted content that reads as a template is a pacing bug, not a content bug." |
| 2026-08-11 | **Preview panel moved bottom-right → bottom-LEFT, and now defaults OPEN** (`localStorage` check flipped from `=== '1'` to `!== '0'`, so "never chose" now resolves to open; an explicit Collapse still persists). | The bottom-right corner now belongs to product UI (the toast, the composer's send control) — a dev panel competing with it for the same corner was the wrong call once that UI existed. Defaulting open matters because several states (hardware-unsupported, zero-credits, the toast variants) are reachable ONLY through this panel; a reviewer arriving at a collapsed FAB has no way to discover half the surface exists. |
| 2026-08-11 | **Sign-out's body is now derived from `SCHEDULED.length`** — count + correct singular/plural, with a plain fallback ("You'll need to sign in again to use BlueAI.") when the list is empty. | "Your scheduled tasks keep running" is reassurance about a *consequence*; with zero tasks scheduled it stops being reassurance and starts being a false claim that sends the user hunting for tasks they never created. → **reasonings**: "reassurance copy is only reassurance if the thing it reassures about currently exists." |
| 2026-08-11 | **Schedule's empty state gets a centred `.bai-list-add` again; the docked bar (`#baiSchedDock`) now HIDES when the list is empty — they swap, never stack.** Reverses the 2026-08-04 decision that removed the empty-state CTA on the grounds that a permanently-docked bar "cannot swap." | That premise was false, not just outdated — nothing stops a docked bar from hiding. Rule 45's original swap reasoning (the empty screen is the one place the create action has zero competition for attention) still holds; it had simply been argued away by a claim nobody re-tested. → **reasonings**: "a rejection reasoned as 'X is impossible' has to be re-tested before the next session inherits it as settled fact," and → taste rule 45 (no change to the rule itself — this restores its scope on Schedule specifically). |
| 2026-08-11 | **Hybrid AI Mode's inline help icon moved to the section's right edge** (`margin-left: auto`), off the sentence it used to trail. | Three of this app's other four help affordances are already right-edge-aligned (header, model menu, Skills toolbar) — this was the one exception. The article behind the icon is about Hybrid mode, the whole section, not the one sentence it sat beside; and a trailing icon lands wherever the sentence happens to wrap at 290px, which is not a stable position. |
| 2026-08-11 | **Key-saved dialog, 4 fixes from an independent UX review**: the status chip moved INLINE with the title via a `.bai-dialog-card:has(.bai-dialog-ic)` grid (was stacked above it, buying a 30×202px row that was 85% empty and making the ack 22% taller than the confirm shape for zero information); the chip itself shrank 30px/glyph-15 → 22px/glyph-12 to match `.tgm-ic-ok`'s own values (it had been a byte-for-byte duplicate of that recipe at a heavier ratio — 2.31:1 against its title, against a 1.87:1 this same file had already rejected on `.tgm-ic`); the title token `--bai-fs-body`(13) → `--bai-fs-lg`(14) (it had opted out of the file's own single declared modal-title definition, TYP-01); `.bai-ui.wide .bai-dialog-card` gained its own 320px `max-width` (244px, correct at the 290px drawer, was unconditional in wide mode and left a 34-character/202px text column at 920px). | Every fix is scoped by `:has()`/media-context so the confirm shape (4 of 5 callers) is provably untouched; the title size was checked against every shipping title's measured width before landing on 14 rather than the full 15 (the longest title measures exactly 202px in a 202px column at 15 — a coin-flip wrap). |
| 2026-08-11 | **`#baiSchedList` gets its own `padding-top: var(--bai-sp-14)`, scoped by id, not a change to `.bai-list-body` itself.** | `.bai-list-body`'s 2px top padding was only ever half a gap — designed to pair with a `.bai-list-head` toolbar that supplies the other 9px itself (Skills' search bar does this; 9+2=11px total). Schedule has no head row; `renderScheduled()` paints straight into the list as the pane's first child, directly under `.bai-strip`, which carries no bottom padding of its own — so Schedule was inheriting the toolbar's half of a two-part gap with no toolbar. Scoped to the id because Skills' 2px is the correct half of ITS OWN math; raising `.bai-list-body` generally would double-space it. → **reasonings**: "a shared rule's correctness can depend on an assumed sibling that isn't present at every call site." |
| 2026-08-11 | **Credits notification REDESIGNED from the bottom-right persistent card to a top slide-down `.bai-toast` banner** — mobile-OS-notification pattern, one shared root carrying two variants (`reward`/`alert`), auto-dismiss window CONTENT-SCALED (`toastDuration()`: `1400 + text.length*45`, clamped 2500–4000ms) rather than fixed. `positionCreditNote()` and its resize listener deleted outright, not left as dead code — a banner anchored to `.bai-ui`'s own top edge needs no per-pane bottom-chrome measurement. | Designer's explicit spec, and a direct reversal of the card's own "no timer, close button only" rule — that rule's reasoning (a timer half-duplicates a close button on a surface read at leisure) is true of a CARD and false of a BANNER whose whole idea is announce-then-leave. The reversal is legitimate rather than drift because the surface's LIFETIME changed, not just its content. → **reasonings**, new principle. |
| 2026-08-11 | **Toast gets a state-coloured 3px LEFT EDGE at full ok/danger strength, plus a 2× entrance pulse on the icon chip only** (reusing `.bai-highlight-pulse`'s exact box-shadow-ring technique via a `--toast-pulse` custom property, so one keyframe serves both colours). | Asked directly whether reward/alert "look delightful," the honest answer was no: the ONLY thing differentiating the two states was a 22px icon's 16% fill wash — the same recipe an independent review had already measured at ~1.25:1 on a white card. The border is not a new idiom; this app already signals state through border colour at full strength on danger buttons, just on all four sides — the edge is that idiom scoped to one side so it doesn't fight the neutral `--bai-line-2` every other card uses. → **reasonings**, new principle on transient-surface legibility. |
| 2026-08-11 | **Toast row `align-items: flex-start` → `center`; both hacks that were compensating for `flex-start` deleted** (`.bai-toast-body`'s `padding-top:1px`, `.tgm-close`'s negative margins). Padding rebalanced to a uniform 11px inset on all four sides (border+pad, 3px+8px on the accent edge vs 1px+10px elsewhere). | `flex-start` anchored the 22px icon to the row's TOP while the text block grew downward from the same edge — against a 2-line reward the icon's true centre sat ~6px above the text's. Both hacks were tuned to ONE line and never held once a subtitle appeared. Centering the row aligns every child to the same true centre for 1 or 2 lines with zero magic numbers. → **taste rule 49**. |
| 2026-08-11 | **Preview panel's Membership row switched `previewSegRow` → `previewSegRowStacked`.** | "Membership" (10 chars, the longest label in that panel) plus "Non-member"/"Member" (the longest options) together exceeded the ~212px width Geo/Own key/Credits fit inside — measured, it ran the row outside the card. `previewSegRowStacked` already existed for exactly this failure mode (Session's own 3-option row); the fix was routing to an existing escape hatch, not building one. Also added to the style guide's Preview spec, which had never shown the stacked variant despite it predating this session. |


### Session 17 POST-CLOSE promotion pass (2026-08-11) — 9 rows

| Date | Decision | Why |
|---|---|---|
| 2026-08-11 | **`.bai-ui.wide .bai-subpane { top: 46px }`**, overriding the base rule's 84px in wide mode only. | 84px is a SUM — 46px header + 38px tab strip — and `.bai-ui.wide .bai-strip` is `display: none` because the sidebar owns mode switching in landscape. So every subpane opened 38px too low and left exactly a strip-height band of the parent pane uncovered; the designer's screenshot showed Skills' search toolbar sitting above an open "My Skills". `.bai-globalroute` (which deliberately covers the strip in BOTH modes) had always used 46px, so the correct wide-mode value was already in the file — the subpane was the lone outlier. Its own rule rather than joining the adjacent `margin-left` override, because that selector is shared with `.bai-panes`/`.bai-main-title`, which are flex items in the column and must not receive a `top`. → **reasonings**, new principle on composed constants. |
| 2026-08-11 | **`.bai-ui.wide .bai-tour { display: none }`** — the tour is not shown in landscape at all. This REPLACED a `top: 46px` fix made an hour earlier, which is now deleted rather than kept. | The tour carried the identical header+strip sum, so the arithmetic fix was correct — and worthless. `tourHi()` toggles `.tour-hi` on `.bai-tab` elements and the card's arrow is positioned from `tab.offsetLeft`, both dead when the strip is `display: none`: it would have dimmed the screen and pointed an arrow at an unrendered element. Designer confirmed onboarding only ever runs in portrait and offered leave-or-remove; removing is the better half, because **a correctly-positioned scrim over a highlight pointing nowhere is a half-fix that READS as handled**, which is worse than absent. Portrait untouched — the selector requires `.wide`, added only by the ≥600px ResizeObserver. A sidebar-aware tour remains a design question, deliberately not invented in CSS. |
| 2026-08-11 | **`.bai-subpane-body` side+bottom padding `sp-12` → `sp-14`**, unifying the gutter at 15px and closing a question open since 2026-08-03. | Designer: *"we should fix this match by 15px only."* `.bai-list-body` already paid `sp-14`, so a list view's content edge sat at 15px from the drawer edge and a subpane's at 13px — consistent across Skills and Scheduled (house behaviour, not drift), but it meant tapping from a list into its own detail screen shifted every element 2px left. Tops still differ deliberately: a subpane has a head row above it, a list body sits under a toolbar or nothing. **The consequence was the risky part and was measured, not assumed:** every subpane form narrowed 264 → 260px, and the tightest known element in any subpane (Scheduled's 7 day chips, previously recorded at ~1.6px of slack) was re-measured at a 290px drawer — 7 chips at 31px = 217px inside a 238px grid, single row, zero overflow, no clipping; the 4-across Repeat seg also single-row. |
| 2026-08-11 | **`.bai-dialog-cancel` joins `.bai-set-btn` as a declared `ds-drift-check.js` §12 SECONDARY-ACTION family** (outlined, neutral fill); fill `--bai-pill` → `--bai-input-bg`; hover unified; the filled-pill treatment retired. | Designer's ruling settled a three-way split into two ROLES: *"use the hybrid AI popup one for popups and the plain text no border no fill grey one in forms."* Popups + Settings share the outlined treatment; inline forms keep `.bai-newitem-cancel`, which is deliberately NOT a family member — it is the other role, not a variant. **Only the FILL had ever differed** — border, colour, weight and radius already matched, which is precisely the state that drifts silently, so the sharing was moved into the selector rather than left as two rules that happen to agree. Gate negative-tested: the drift was injected, the gate confirmed FAIL, then reverted. → **reasonings**, fresh instance on the existing selector-not-comment principle, plus a new one on fixing unverified defects. |
| 2026-08-11 | **The `--bai-pill` fill it replaced was a fix for a defect that did not exist.** Recorded as its own row because the lesson outlives the code. | On 2026-08-04 I changed `.bai-dialog-cancel`'s background from `--bai-input-bg` to `--bai-pill` reasoning that in light theme `--bai-input-bg` is `#ffffff`, identical to `--bai-card`, so the button was invisible. It was not: the button carries a 1px `--bai-line-2` border, making it an OUTLINED button whose border defines it — exactly as the Hybrid modal's equivalent always has been. **The "fix" solved nothing and created the divergence the designer later had to point out.** → **reasonings**, new principle. |
| 2026-08-11 | **`.bai-dialog-body` colour `--bai-muted-2` → `--bai-text`**, matching `.tgm-body`. Font size deliberately still differs. | Designer: *"generic popup has different body color while the hybrid has different, that shouldn't happen… body color should match the hybrid AI popup."* Two blocking overlays in the same z-tier were reading at two different text weights for no reason. It had been `--bai-muted-2` because a WCAG fix moved it up from `--bai-dim` the previous day; `--bai-text` is a further improvement in the same direction, so this satisfied the consistency ask and the contrast finding at once rather than trading one against the other. Size stays `--bai-fs-sm` vs `.tgm-body`'s `--bai-fs-base`: the dialog is the denser surface, and the designer named the colour — not silently widened into a size change. |
| 2026-08-11 | **Toolbar `.bai-help-inline` radius `--bai-r-sm` → `--bai-r-md`**, scoped to the toolbar context. | Designer approved on sight. In that toolbar it is a 32px box beside a 32px `.bai-search` on `--bai-r-md`, and the rule's own premise (recorded in the stylesheet) is that the two are ONE toolbar — a shared height with an unshared corner reads as two controls that merely happen to match in size. Scoped for the same reason its height/width are: beside the Hybrid subtitle it is a 24px button and keeps `--bai-r-sm`. No concentric constraint applies (rule 44) — the two are siblings in an unrounded row, not nested. |
| 2026-08-11 | **`.bai-preview-seg button` radius `--bai-r-sm`(6) → `--bai-r-xs`(4)** — a rule-44 concentric fix. | The track is `--bai-r-sm` with 2px padding, so `r_inner ≈ r_outer − inset` wants 4px. What makes this worth a row is WHEN it appeared: the pair had coexisted harmlessly for weeks, and the violation only became real when routing the Membership row to the `.stretch` variant made those buttons `flex: 1` and reach the track's corners for the first time. **That is rule 46's mechanism exactly** — a wrapper/nesting relationship whose correctness changed with nobody editing either rule — and it is the first time rule 44 and rule 46 have interacted. |
| 2026-08-11 | **`radius-nesting-audit.js` now walks past `.sg-spec`/`.sg-canvas`** when looking for a rounded ancestor. | Those are the style guide's own documentation wrappers, not product containers, and they were generating two permanent false "mismatches" — one of which demanded a 1px radius on a sidebar row. The gate had never read zero, so its output had become something to skim rather than read, and the real rule-44 violation above was sitting underneath that noise. It now reads a true 0. → **reasonings**, new principle: a gate with standing false positives is worse than no gate. |
| 2026-08-11 | **Single-button acknowledgement keeps its full width — the review's proposal is DECLINED.** No code change; recorded so it cannot be re-flagged as drift. | The independent review found the ack's lone "Got it" at 202×32 = 2.08× the area of the confirm shape's own commit button (97×32) and at a different x-position, and proposed narrowing + right-aligning it. Designer: *"since there is no other button needed here the full width is the right approach — but yes the major action can sit on the right side."* So full width is correct for a lone action, and the committing-action-sits-right convention only has meaning when there ARE two to order (the confirm shape already satisfies it). The designer also supplied a better general rule than the review had, now taste rule 50: **side-by-side vs stacked is decided by whether the labels fit side by side at equal size, not by button count.** |

### Session 18 (2026-08-11) — the BlueStacks/BlueAI install flows

| Date | Decision | Why |
|---|---|---|
| 2026-08-11 | **Two install flags, three valid states, and the flows land on OPPOSITE sides of the VDA boundary — which is the whole structure of the work.** Flow A (BlueStacks present, BlueAI not) is entirely BlueStacks' UI and excluded from this design system; Flow B (BlueAI present, BlueStacks not) is entirely ours and designed here. | Designer's ruling on Flow A: *"use it exactly as it is, from blueAI product, and it shouldn't be a part of any Audit or style guide or DS or VDA for blueAI desktop."* The reason it is not a judgement call: in Flow A **there is no drawer, because the drawer IS BlueAI** — BlueAI is not installed on that machine, so none of this design system exists at that moment and the software raising the prompt is BlueStacks. Flow B is the mirror: BlueAI is speaking, inside its own window, so it must be designed rather than ported. The guard (turning the last flag off flips the other on) exists because "neither installed" is a bare desktop with no way back — not a designed state. |
| 2026-08-11 | **Flow A ported to VANILLA (`bs-install-dialog.js`), not copied as `.jsx`.** | The two prototypes share no runtime: blueai-product is React 18 + Babel-standalone + Tailwind from CDN, and blueai-desktop's defining contract is no build step and no JS dependencies. Copying the source verbatim would have meant importing a transpiler and three CDN scripts into the one prototype built to have none. The designer chose the port from three options offered. The port is near-mechanical because the source is: it inline-styles everything, defines its own `ProgressShell`, and uses only useState/useEffect/useRef. Two things deliberately NOT carried over — `PixelWave` (dead in the source; `<PixelWave` appears zero times in its own render after a layout revert, and porting dead code is how dead code survives) and the unused `body2` prop. |
| 2026-08-11 | **The exclusion is STRUCTURAL, not an exemption list.** The dialog creates its own scrim/host and keeps every style inline — including the host's — so it contributes zero rules to `blueai-desktop.css` and zero to `index.html`. `index.html` gets one `<script>` tag. | `ds-drift-check.js` §8 scans exactly those two files for raw `font-size:Npx` / `border-radius:Npx`, and this dialog legitimately has **8** of them, because BlueStacks' values are not our tokens. Keeping them in a file §8 does not scan means **there is nothing for the gate to see, rather than something it has been told to ignore** — and the gate's own §8 comment already says why that matters: *"an exemption list is a to-do list wearing a scope statement."* The two RUNTIME audits do read the DOM, so they skip `.bs-ui, #bsInstallHost, .bs-window` by name — judging BlueStacks' 26×26 buttons against rule 48's floor, or its radius-0 card against rule 44, would report a conflict between two design systems as a defect in one. `.bs-*` is the boundary prefix: "not ours." |
| 2026-08-11 | **Flow B's player boot: frames + narration driven off ONE shared constant** (`BS_BOOT = { loading: 2400, home: 1400 }`), with a Play listing only for an app we hold a screenshot of (`BS_PLAY_FRAME = { YouTube: … }`). | The scene swaps the player's screenshot off these values and the chat times its narration off the same ones, so each line lands just AFTER the frame it describes. Hard-coding either side is what lets the chat announce a screen the window has not reached. The listing gate is an honesty constraint, not an optimisation: showing a YouTube listing while the chat says "Found TikTok" is a straightforward lie about what the product did, so other apps rest on the home screen and the copy adapts ("Opening TikTok…" instead of the Play-Store line). |
| 2026-08-11 | **`.bai-warnrow.info` + `.bai-donecard.working` — colour-only MODIFIERS, not new components.** | Both surfaces already had exactly the right anatomy; only the colour needed to change, which is rule 41's own prescription (a distinction already carried by colour must not be re-encoded in a channel with layout side-effects). `.info` is blue rather than amber because nothing has gone WRONG — the task simply cannot start until a dependency exists, and amber asserts a problem with the user's request. `.working` exists because a green-headed card reading "INSTALLING" would be claiming completion. |
| 2026-08-11 | **`.bai-warnrow.info`'s values were INVENTED and had to be corrected the same day** — `color-mix(--bai-accent 20%)` / `48%` → `--bai-accent-wash` / `--bai-accent-line`. | Designer: *"where is its DS coming from? is this bg color font color all good?"* It was not. I had copied `.bai-warnrow`'s amber recipe and swapped the hue — mechanical, not considered. The amber row uses `color-mix` **because no amber token pair exists**; the accent has had `--bai-accent-wash` + `--bai-accent-line` all along, the same pair `.bai-sk-try` already uses for an accent surface. Measured consequence: the fill rendered at 20% against the DS's own 10%, i.e. **double the saturation of every other accent surface in the app**, with a border built as an alpha of the accent where the DS uses a solid desaturated blue. A token existed and a new value was invented anyway — Gate 0/Gate 1. |
| 2026-08-11 | **`.bai-progbar` / `-fill` EXTRACTED (Gate 3, threshold 2)** and the Hybrid-AI download's call site migrated onto it. | The recipe was inline in a JS template string; Flow B's install needed the same bar, and two usages is the extraction threshold, so both call sites now share the class rather than a second copy drifting from the first. Its values are the ones the inline version already used — nothing re-picked. Only `width` stays inline, because that is data, not style. |
| 2026-08-11 | **`.bai-chat-cta` created and declared a §12 FILLED-ACTION family with `.bai-newitem-create`, replacing `.bai-opt-row`.** | Designer: *"this doesn't even look like a button unless I hover over it."* Correct, and the cause is specific: I matched the **shape** (a single actionable thing inside a message, exactly like `renderUpload`'s "Attach file") and ignored the **role**. `.bai-opt-row` is a pick-one *list row* — its resting state is a white card with a grey border *because that is what a list row should look like*, and only its hover turns accent. An install is a commit, not an option in a list. The treatment that reads as a button at rest already existed on `.bai-newitem-create`, so it moved into one shared rule rather than being copied. **Filled rather than `.bai-sk-try`'s accent outline** because this button sits directly beneath `.bai-warnrow.info`, itself an accent wash with an accent-line border — an outlined accent button under an accent-washed row is two near-identical surfaces stacked, and it dissolves into the note above it. → **taste rule 51**'s sibling reasoning; widened the existing role-vs-family principle. |
| 2026-08-11 | **Preview install toggles: two `previewSegRow`s → one `previewCheckRow`.** | Designer asked for *"simple checkbox with their name in a single row"*, and the reason generalises past this panel: a segmented control is the shape for a choice **BETWEEN** options, while installed-ness is a fact that is either true or not — and there were two **independent** facts sharing one label. Checkboxes say "this is on" in one glyph, so both fit one row and the row needs no label at all: the names are the labels. Gate 8.2 drove one detail — the UNCHECKED box keeps a visible 1px edge against the panel's own dark surface, since that state is the recurring blind spot on this primitive. → **taste rule 51.** |
| 2026-08-11 | **`applyInstallState()` opens BlueAI's window whenever the player is absent** — and the bug it fixes is the most instructive of the session. | Hiding the player exposed a blank, unbooted BlueAI window. `.drawer`'s closed state was **never CSS-hidden**: it sits at `left: 100%` translated back −290px, i.e. 710px into a 1000px composition — *directly behind the opaque player image*. Its invisibility was **occlusion by a sibling**, not a property of its own, and grepping `.drawer`'s rules would never reveal that dependency. Second problem found while fixing it: even hidden correctly, "player gone + drawer closed" is a **dead end**, because the only affordance that opens BlueAI is clicking the player. Opening the window is also the honest state — BlueAI is installed software and, with no player on the desktop, its own window is the surface. Put in `applyInstallState` rather than the checkbox handler so it covers both routes (BlueAI on, BlueStacks off). → **reasonings**, added as the MIRROR case of an existing principle. |
| 2026-08-11 | **Three defects that only appeared when DRIVEN, recorded because two of them produced plausible wrong states rather than errors.** (a) the three frame PNGs were copied and never referenced; (b) both checkboxes rendered unchecked on load; (c) `bsInstallCard(app)` was missing its parameter. | (a) An unused asset is indistinguishable from a used one until someone looks at the screen — the file being present is not evidence that anything renders it. → **reasonings**, new principle. (b) `var bsInstalled = true` sat ~117 lines BELOW `renderPreviewRows()`; `var` hoists the name but not the value, so both flags read `undefined` — falsy, and **indistinguishable from a deliberate `false`** at the point of use, which is why it produced a plausible wrong state instead of an error. → widened the existing `d \|\| DEFAULT` KB trap, same mechanism. (c) threw `ReferenceError` **inside the requestAnimationFrame loop**, so the card reached "Getting ready 100%" and silently stopped — nothing surfaced anywhere in the UI, only the console. Found by reading console errors rather than re-reading my own code. → **knowledge-base**, new trap. |

*(New rows accumulate at audit passes.)*
