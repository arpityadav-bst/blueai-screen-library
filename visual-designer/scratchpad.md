# blueAI — Scratchpad
Inline correction-resolution log. One line per correction the moment it's
resolved. Promoted to decisions.md / taste.md at audit passes, then wiped.

Format: `YYYY-MM-DD HH:mm — <file> — <what changed> — Why: <one phrase>`

--- Pending audit entries ---
2026-06-11 — seo-data.ts (STEPS.h2 + FAQ "AI worker") — straight ASCII `"…"` → directional curly `“…”` (U+201C/U+201D) — Why: **Space Grotesk draws the straight double-quote glyph as two slanted "99" strokes**, so the OPENING quote rendered as an inverted closing-shape ("From ”do this”…"). Designer caught it. Rule candidate (taste): display/marketing copy always uses directional curly quotes `“ ” ‘ ’` + apostrophe `’`, never straight `" '` — verify in the actual font (Space Grotesk hides it). Fix verified by screenshot.
2026-06-11 — SeoNav (NAV_LINKS + seo-home.css) — added the global "Social Rewards" + "Developer" links from the hero nav to the SEO nav (external `#` stubs). 6 links wrap below ~1100 → raised the nav hamburger breakpoint 720→1080 (links collapse to the burger on tablet; the Download CTA stays in the bar and only moves into the menu ≤720). **Specificity gotcha:** a media-query override must MATCH the base selector's specificity — `.v-seo .seo-nav-menu-cta` (2 classes) lost to base `.v-seo .seo-nav-menu .seo-nav-menu-cta` (3) → the menu CTA double-showed on tablet until I matched the 3-class selector. Verified by screenshots at 1280/1100/960/768.
2026-06-11 — SeoNav + seo-data + seo-home.css — Social Rewards/Developer open in a new tab → added a shared `<ExtIcon/>` (↗ arrow-up-right) after the label + `target=_blank rel=noopener`; marked them `external: true` in a typed NAV_LINKS. CSS `.seo-ext` (vertical-align -0.12em, opacity .5 → .8 on link hover). Verified in the bar + dropdown. **Workflow lesson (→ KB):** running `next build` while `next dev` was up clobbered dev's `.next` → chunk 404s → no hydration → the menu wouldn't open in Playwright (rendered fine in screenshots, but dead). Fixed via kill + rm .next + restart. Don't build over a running dev.
2026-06-11 — MarketingHeader — desktop nav links re-CENTERED (designer caught). Unifying the header, I grouped the links into the right cluster (logo | [links + CTA]) so they sat on the right. The original SEO nav is a 3-part `justify-content: space-between` row (logo | links | CTA) → links centered. Restored the 3-child structure. **Lesson:** when consolidating a nav into a shared component, preserve its flex STRUCTURE (a 3-part space-between row centers the middle group), not just its links + styling.
2026-06-11 — MarketingHeader + header.css — added the divider + 3 social icons (Discord/X/Reddit) to the shared header, like the HeroNav variant. **KEY:** icons in BRAND colors (Discord #5865F2 · X #000 · Reddit #FF4500), NOT monochrome — designer: "not colorless but brand colors" (HeroNav had them gray). Divider matches HeroNav exactly (1px · 22px · --bai-divider). Bar icons hide ≤1080 → appear in the hamburger menu instead (bigger, same brand colors). Also added the trailing → arrow to every primary CTA (Post on r/BlueStacks · Download BlueAI · Get BlueAI free · Claim credits) via a shared `<Arrow/>` + `.site-arrow` hover-slide, matching the canonical Download CTA.
2026-06-11 — MarketingFooter + footer.css — unified the footer across all pages (header + footer now both shared-SSOT chrome, `.bai-hdr`/`.bai-ftr` scoped to the element so they're route-independent). Footer has the brand-colored social icons + a Product column (Social Rewards · Developer). `SOCIAL` consolidated into site-data (one source for header + footer). **Dead-CSS flag (prune later):** after unifying header+footer, `seo-home.css` still has inert `.seo-nav*` + `.seo-footer*` rules, and `site.css` has inert `.site-footer*` — no elements render them (components deleted). Left in place to avoid risky surgery on the live SEO stylesheet mid-task; safe to remove in a focused pass.
2026-06-11 — header.css — mobile-menu Download CTA rendered BLACK not white. `.bai-hdr-menu a { color: var(--fg-2) }` (specificity 0,1,1) out-specified `.bai-hdr-cta { color: #fff }` (0,1,0), so the menu's dark link color won on the CTA `<a>`. Fixed with `.bai-hdr-menu .bai-hdr-cta { color: #fff }` (0,2,0). **Recurring gotcha:** a CTA placed inside a styled menu/list inherits that container's link rules — scope the CTA color to beat them (same class of bug as the earlier menu-cta double-show fix). Also this round: removed footer "Try Skills"; Developer H1 "BlueAI" de-gradiented to match the sentence (the lockup above stays the gradient Wordmark); added the trailing → to the SeoCta close CTA.
2026-06-11 — SeoNav + seo-home.css — mobile menu polish (designer caught): the open dropdown was translucent (rgba(245,246,253,.97) + blur) → hero text ghosted THROUGH the links, and the page below (incl. a 2nd download CTA) showed, so it read as floating. Fixed: opaque menu (rgb(245,246,253), dropped the blur) + a tap-to-close SCRIM (`.seo-nav-scrim`: absolute, top:100% so the bar stays crisp, height 100vh, rgba(8,10,31,.42), fades in) dimming the page below the menu. Standard focused-overlay pattern; resolves the duplicate-CTA clash too. Verified at 390px. **Taste candidate:** a mobile dropdown menu must be OPAQUE and dim the page behind it (scrim) — never translucent over live content (ghosting + competing CTAs).
2026-06-12 — new FileUpload.tsx + agent.css + CareerForm + FinanceForm — fixed file-upload SEMANTICS (designer: "after uploading there should be a cancel/remove option, and Choose File shouldn't stay once something's uploaded"). Built a reusable stateful `FileUpload` (hidden native input + custom trigger): EMPTY = upload icon + "Choose file" + "No file chosen"; FILLED = file icon + filename (ellipsis) + remove (✕) button, "Choose file" gone, box flips dashed→solid (`is-filled`); ✕ clears back to empty. Replaced the raw `<input type=file>`+`::file-selector-button` on BOTH upload fields (Resume / holdings). Changed those field wrappers `<label>`→`<div>` so the label-click doesn't double-fire the picker alongside the trigger button. Removed dead `.jmf-file*` CSS + the now-unused `UploadIcon` import in the forms (FileUpload owns it). Verified full cycle by simulated DataTransfer. — Why: a native file input is a single-state control that lies about its state (shows "Choose File/No file chosen" even when filled, no removal). **Taste/KB candidate: every input must render ALL its states correctly — a "choose" affordance must become a "chosen + remove" affordance once filled; don't ship a control that can only represent its empty state.** Reviewed the other field types (pills single/multi-select, tabs, checkbox, text/textarea) — semantically sound.
2026-06-12 — social-rewards RewardsHero.tsx + social-rewards.css — removed the "See how it works ⌄" link under the hero CTA (designer: "remove see how it works here"). Also deleted the now-unused `Chevron` glyph + `.sr-seehow` CSS (no dead code). Hero CTA is now just the "Post on r/BlueStacks" button. — Why: designer call; tidier single-CTA hero.
2026-06-12 — social-rewards RewardsHero.tsx + social-rewards.css — CORRECTION to the entry below: I declared social-rewards "no changes needed" after verifying COPY matched verbatim, but never checked the HERO COMPOSITION — designer caught it. Our hero cards were two neat upright flex columns inside a 1200px band; the LIVE hero is a scattered "Reddit collage": 6 cards absolutely positioned, bleeding off BOTH viewport edges, rotated ∓10/5/2°, scaled .86/.95/.88, opacity .5/.85/.6 (middle card most prominent), staggered top 150/250/372. Rebuilt: `.sr-hero` now full-width + overflow-hidden + desktop min-height 580 (was max-width 1200 → that band is what made the cards look like tidy columns); `.sr-collage` absolute-fill; 6 `.sr-card-pos` with per-card inline left/right + transform + opacity (captured from the live). Verified by SCREENSHOT (this page has no continuous anim, so the shot worked). — Why: **content fidelity ≠ layout fidelity.** A verbatim-copy audit that skips the visual composition is a Gate-8 miss. **KB/taste candidate: when auditing a page "faithful", screenshot-compare the COMPOSITION, not just the text — especially decorative/collage hero treatments that copy-checks can't catch.** Minor remaining nuance: headline wraps after "AI" vs the live's after "1,000" (text-wrap:balance) — left as-is.
2026-06-12 — social-rewards + developer — sweep pages 5 & 6 of 6: discrepancy audit, NO changes needed. Verified live-vs-ours: social-rewards content matches VERBATIM (H1 "Earn up to 1,000 AI Credits for BlueAI", hero sub, all 5 steps incl. the Discord/BlueAI-bot flow, all 4 quality items, 6 FAQ — all identical to `rewards-data.ts`); developer is a teaser that matches word-for-word (lockup + "Build with BlueAI" + sub + "Claim up to 25,000 Credits"). — Why: these two were built from verbatim copy in an earlier session (not stubbed like the agent demos), so they were already faithful. Sweep complete: 4 agent pages rebuilt, 2 standalone verified clean.
2026-06-12 — prediction-market-agent (new MarketsForm.tsx + agents-data MARKETS + page) — sweep page 4 of 6 (all 4 AGENT pages now done). (1) Rebuilt "Get odds alerts" demo as a faithful interactive TABBED form (Watch markets ↔ Ask about odds): subtitle + Watch tab (Markets-to-watch textarea + "Alert me when odds move (optional)" text input via `.jmf-opt`) + Ask tab (question textarea) + the odds-info-not-betting-advice checkbox + "Get my alerts". Note our old stub used ±1/±3/±5 PILLS for the threshold — live uses a free-text input; corrected. (2) Added 5 SEO blocks (which markets / Polymarket-vs-Kalshi / legal-safe / build-your-own / is-this-betting-advice). (3) hiwHeading "From markets to alerts". (4) Odds-table heading em-dash→comma to match live ("World Cup 2026 winner, Polymarket vs Kalshi"). Markets has only ONE data section (odds table, already present) — no extra missing section like finance. — SEO copy fetched via `node fetch` (the extension blocked DOM reads containing market URLs — "[BLOCKED: Cookie/query string data]"; SSR HTML + tag-strip got it). **KB note:** when the in-page extension blocks reads on URL-bearing content, curl/fetch the SSR HTML instead.
2026-06-12 — ai-trading-agent (new FinanceForm.tsx + Trades section in page + agents-data FINANCE + agent.css) — sweep page 3 of 6. (1) Rebuilt "Get your analysis" demo as a faithful interactive TABBED form (Upload my holdings ↔ Ask about a stock) — added the missing subtitle, the upload-helper `.jmf-hint`, the education-not-advice agreement checkbox, and tab switching (Upload=file+hint, Ask=textarea w/ live placeholder). New `.jmf-tabs/.jmf-tab`. (2) Added the ENTIRELY MISSING "Every trade the agents have made" section — eyebrow "The agent's moves" + 15-row trade log (date · style · BUY/SELL · ticker · $value), BUY=`--bai-success-*`, SELL=`--bai-danger-*`, scoped `.ag-trades` (grid; mobile grid-areas reflow). (3) Added 5 SEO blocks (data&indicators / which stocks / legit-legal-safe / build-your-own / is-this-financial-advice). (4) hiwHeading "From holdings to inbox" (was the leaked "From targets to done"). — Why: same fidelity bar as career/creator. **Recurring (KB):** the agent pages each carry a whole bespoke data section (career=openings, finance=trade-log, markets=odds) we'd stubbed or omitted — inspect for missing SECTIONS, not just demos.
2026-06-12 — ai-video-creator (new VideoCard.tsx + page + agent.css + public/videos/) — downloaded the 3 REAL "Made by the agent" clips + poster JPGs from bluestacks.ai/videos/ (kai-bride / ceo-married-rival / steam-engine, ~36MB total) into `public/videos/`; replaced the placeholder gradient frames with real `<video>` (poster + click-to-play, loop/playsInline/preload=metadata/no native controls — mirrors the live). Click-to-play needs client state → `VideoCard` ('use client'); play overlay hides while playing. Designer asked to bring the live videos onto our page. — Why: design-only handoff can use the product's own real CDN assets (now.gg asset rule). NOTE: ~36MB of mp4 now in the repo → fine for Vercel static hosting; flag at push.
2026-06-12 — ai-video-creator (new CreatorForm.tsx + glyphs.tsx + agents-data CREATOR + AgentShell + agent.css) — SAME discrepancy sweep as apply-to-jobs, page 2 of 6. (1) Rebuilt the "Generate a video" demo as a faithful interactive form (reused `.jmf-*`): added the missing SUBTITLE, the 4th Style option "Talking-head reel", corrected Platform labels (YouTube→"YouTube Shorts", Reels→"Instagram Reels"), Length labels (30s→"30 sec"), textarea (not input) with the live placeholder, defaults Faceless short / YouTube Shorts / 30 sec. (2) Added 7 always-visible SEO blocks (micro-drama / faceless channel / post-schedule / YouTube automation / makes money / AI allowed / cost) — creator has NO `whatIs`, so AgentShell now renders a standalone seo-blocks section before the FAQ when whatIs is absent. (3) Fixed the hardcoded hiw heading "From targets to done" (career copy leaking onto every agent) → per-agent `hiwHeading`; creator = "How does it make AI videos?" (matches the live's block-1 steps). (4) Gate 3: extracted shared `Check`/`UploadIcon` glyphs to `glyphs.tsx` (now used by 2 forms). — Why: "do the same discrepancy test on the other pages." **Recurring (KB):** the agent demos were ALL simplified `.jf` stubs diverging from their live forms; and shared-template hardcoded copy (the hiw heading) leaks one agent's words onto the others — per-agent data, not hardcoded strings.
2026-06-12 — apply-to-jobs (agents-data CAREER.seoBlocks + AgentShell + agent.css) — added the 6 always-visible SEO content blocks (Which job boards / Is it safe / Will it tailor / How different / How many / What do I need) under the "What is" section — the live page has these as H3+paragraph SEO copy and we were missing them (designer caught; pasted the live copy). New optional `seoBlocks?` field on AgentData, rendered in the what-is section, styled `.ag-seo-block`. Normalized "Blue AI"→"BlueAI". Confirmed our FAQ accordion (8 items) ALREADY matches the live FAQ accordion exactly — no change there. — Why: live SEO pages carry both always-visible content blocks AND a FAQ accordion; we'd only built the accordion.
2026-06-12 — AgentShell.tsx + agent.css (.ag-more) — fixed the "More BlueAI agents" cards: (1) emoji was cramped top-left because `.v-agent .ag-more-card span` (spec 0,2,1) overrode `.ag-more-ic`'s flex/20px (0,2,0) — the icon IS a span → re-scoped the description rule to `.ag-more-tx span` so it no longer bleeds onto the icon tile; (2) added the trailing → (shared `<Arrow size={16}/>`, accent color, hover-slide) the designer noted was missing. — Why: a bare `.card span` rule silently captures EVERY descendant span incl. icon wrappers — scope text rules to the text container. **Recurring (taste/KB candidate):** descendant element selectors (`.x span`, `.x p`) are specificity traps — always scope to the inner wrapper class.
2026-06-12 — apply-to-jobs (new CareerForm.tsx + agent.css .jmf-*) — REBUILT the "Get your job matches" form to live fidelity (designer caught a major gap). Last session's CareerDemo was a hand-built static stub (4 fake fields, single-select pills, no states); the LIVE form has 8 elements: title+subtitle, real email + roles inputs (focus ring), **4-way MULTI-select Location (US+Remote default on, with check icons, + Global + Other)**, a 7-option Seniority **dropdown**, a dashed Resume **file upload**, an **agreement checkbox**, and the → CTA. Re-inspected the live bluestacks.ai/apply-to-jobs DOM via the Claude-in-Chrome extension (extracted exact Tailwind classes per field), then re-expressed in our tokens. Made it a 'use client' component (pill-toggle state), design-only (submit preventDefault). Own `.jmf-*` namespace — the shared `.jf` stubs on the other 3 agent pages were left intact. Also restored the hero-left "Latest openings + See all 24" mini-list (the live has NO left Download button) via a new `heroAside` prop on AgentShell. Accent = `--bai-mkt-blue` (solid, like live — not our brand gradient); focus ring via `rgba(var(--bai-mkt-blue-rgb),.15)`. — Why: "faithful look, our DS underneath" means inspecting the SOURCE DOM, not eyeballing/reconstructing from memory. **Recurring-category flag (KB/reasonings candidate):** replication tasks REQUIRE live-DOM inspection of every element; a hand-built stub that "looks about right" is the failure mode the designer caught twice now. The other 3 agent demos are likely the same stub-vs-live gap — audit them too.
2026-06-11 — MarketingHeader.tsx + header.css — added the trailing → to the header "Download for PC" CTA (desktop bar + mobile menu) via the shared `<Arrow/>`, completing "every primary CTA gets the arrow" (the header was the one CTA missed last session). Sized `size={16}` (not the canonical 18) — optically proportioned to the compact 14.5px nav pill (taste rule 16). **Scope gotcha:** `<Arrow/>`'s hover-slide CSS (`.site-arrow` translateX) is scoped to `.v-site`, but the header is route-independent `.bai-hdr` (outside any `.v-site`) → the slide wouldn't fire; re-declared `.bai-hdr-cta .site-arrow` + `:hover` in header.css. — Why: brand contract "every primary CTA carries the → arrow," and shared-chrome CSS can't borrow a page-scoped rule.
