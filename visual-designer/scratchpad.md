# blueAI — Scratchpad
Inline correction-resolution log. One line per correction the moment it's
resolved. Promoted to decisions.md / taste.md at audit passes, then wiped.

Format: `YYYY-MM-DD HH:mm — <file> — <what changed> — Why: <one phrase>`

--- Pending audit entries ---
(empty — last promoted 2026-07-03, S8 audit: 59 entries → taste 34–37 + rule-32 scope clause, 14 decisions, KB creator-v2-era section, project-insights route truth, evolution S6.5/S7/S8, session-log backfill, dead Ldv2 chrome CSS removed)
2026-07-22 (cont.) — /blueai-desktop — designer, on a screenshot of the new "past conversation" note: "this can be better UX wise and chat history can contain better language and history a few could be a bit longer for presentation." Three fixes: (1) the note was passive text-only ("This is a past conversation — start a new chat to continue") sitting where the composer used to be — rebuilt as a real component (small clock icon + shorter "You're viewing a past conversation." line + an actual blue "Start a new chat" BUTTON wired to `newChat()`), so the affordance is actionable, not just descriptive. (2) History card TITLES read like file names, not natural chat titles — "Berlin UX roles"→"UX roles in Berlin", "Carousel: AI news"→"This week's AI news carousel", "Resume tailoring"→"Tailoring my resume for product design", "Free Fire FAQ"→"Free Fire gameplay review script" (all now phrased as natural sentences matching how the underlying user message actually reads, the way a real auto-titled conversation would). (3) Two threads lengthened for presentation quality — "UX roles in Berlin" 2→6 messages (3 full turns: find roles → which pays most → tailor resume for that one) and "Tailoring my resume" 2→4 messages (2 turns) — while the scheduled-run and still-"working" threads stay short since brevity is realistic for those specific states. — Why: a note that only describes the situation makes the user go hunt for the actual new-chat control elsewhere; a note that also acts on it removes that extra hop. And history that's uniformly 1-exchange-long undersells what the product can actually do — a couple of longer, natural threads read as real usage, not scaffolding.
2026-07-22 (cont. 2) — /blueai-desktop — designer: "more longer histories please in all of them" — my previous pass only lengthened 2 of 6 threads (kept the scheduled-run + in-progress ones short on a "realism" judgment call the designer didn't actually ask for). Extended all 6 to 6-8 message multi-turn conversations, including the 2 scheduled entries (Daily rewards run, Weekly game roundup) which now show a natural follow-up exchange after the automated result. Caught + fixed 2 `count` (badge shown in the history card) vs `msgs.length` (what actually replays) mismatches introduced by the lengthening — count must stay ≥ msgs.length or the card undersells what you see on open. Verified via DOM: all 6 threads now render 6-8 `.bai-msg` elements on load. — Why: "in all of them" is an explicit override of my earlier selective judgment call — when the designer states a scope explicitly, don't re-apply the narrower judgment call from last time.
2026-07-22 — /blueai-desktop — PM feedback batch (6 numbered points + 2 self-found bugs). (1) Removed the Effort picker entirely (EFFORTS/curEffort/effortBtn/effortMenu/renderEffort + its status-bar wrap) — "we don't support that right now." (2) New-chat icon still didn't read as "new chat" even after the earlier pencil→plus-square fix; replaced with a proper chat-bubble+plus ("message-square-plus") glyph in both places it appears. (3) "Try this skill" no longer auto-runs a fake scripted result — it now just pre-fills the composer (`Use my "<name>" skill`) and focuses it, leaving send to the user. (4) Input placeholder rotation is now a real typewriter (type in ~45ms/char → hold 1.5s → erase ~25ms/char → 400ms pause → next hint) instead of an instant swap; typing/erasing pauses (not skips) if a real flow question owns the placeholder. (5) BIG ONE — what PM actually meant by "how chat messages should look" turned out to be a completely different ask than styling: reroute the 5 message-type demo away from the dev-only "Chat states" toggle entirely, into KEYWORD-TRIGGERED replies in the live chat itself — type a word like "plan"/"warn"/"error"/"success"/"option" and BlueAI's reply renders that message type for real. Added a 6th type (Error — red card, mirrors Task-completed's chrome) since the PM's list included it and it didn't exist yet. Implemented as `keywordReply(text)` checked in `sendMessage()`'s reply branch (substring match on stems: plan/warn/error+fail/success+done+complete/option+choice), falling back to the old random RESPONSES on no match. DELETED the entire Chat-states toggle system (var chatStatesOn, renderChatStatesPreview, setChatStates, its Preview-panel row, the `.states-preview` CSS) since it's now fully redundant — confirmed this was the right read before touching anything (designer explicitly said "let me know if you understood this... ask me anything"). (6) Chat History: continuing a past thread was never supported, but loading one still showed a live composer implying you could — now `loadThread()` adds a `viewing-history` class that hides `.bai-inputbar` + `.bai-status` and shows a small "this is a past conversation" note instead; `newChat()` clears it. SELF-FOUND, from the designer's own screenshot + question: (a) the Preview panel's Session row (Onboarding/Default) was overflowing its 208px card — widened to 236px (matches blueai-product's own card width) + tightened seg-button padding/font by ~1-2px; verified via getBoundingClientRect that the segmented control now sits inside the card with margin. (b) "chat states doesn't appear through onboarding, but onboarding only changes the intro right?" — designer's OWN diagnosis was exactly correct: the Preview panel's since-removed Chat-states row was gated on `pageState==='default'`, but pageState only controls the BOOT sequence (which intro plays), not which screen you're currently on — once the onboarding tour lands you on the same chat screen, it's the identical screen `default` boots straight into, so gating on pageState there was simply wrong logic. Moot now since that row is deleted with the rest of the toggle, but logged so the mistake (conflating "which boot path" with "which current screen") doesn't recur elsewhere in this file's progressive-disclosure logic. Verified all 7 fixes via DOM/behavior assertions (screenshot tool was stuck/unavailable this session — a genuine environment issue, not an app bug; every check instead confirmed via computed styles, classList state, and bounding-rect math) + 0 console errors. — Why: point 5 is the clearest lesson of the day — a vague one-line PM note ("how chat messages should look") looked like a styling ask but was actually a workflow/architecture ask; asking a clarifying question surfaced that instead of guessing wrong and building the wrong thing entirely.
2026-07-21 (cont. 2) — /blueai-desktop — designer, on a Settings screenshot: "everything feels like equal spacing" (related label+card should sit tight, unrelated sections should have generous separation) + "scroll everywhere should be subtle and only visible when scrolling, otherwise fade out." (1) SPACING: `.bai-set`'s uniform flex `gap:9px` (every child equally spaced regardless of relationship) replaced with margin-driven hierarchy: `.bai-set-label { margin:24px 0 8px }` (+`:first-child` zeroes the top) — 8px down to the card/row it OWNS, 24px up to separate it from the PREVIOUS section's card. Verified via getBoundingClientRect deltas: 8px label→card, 24px card→next-label, consistently, across all 6 Settings sections. This is the same "spacing is hierarchy: tight within groups, wide between" principle already codified for WSUP — first time applied here. (2) SCROLLBARS: generalized the chat body's existing bespoke fade-thumb (native scrollbar hidden, custom `.bai-scroll` thumb shown only during active scroll, auto-fades ~900ms after idle) into two reusable functions — `refreshFadeScroll(el,thumb)` (geometry only, called after any content re-render so the thumb never shows stale size/position) and `bindFadeScroll(el,thumb)` (wires the scroll listener + show/fade lifecycle) — then wired it to every other scrollable surface: Settings (`.bai-set`), Skills + Scheduled lists (`.bai-list-body`), the Subpane stack (`.bai-subpane-body`, refreshed once per `paintSubpane()` so History/category-drilldown/details/create-form all inherit it for free), and the wide-sidebar recents list (previously a native `scrollbar-width:thin`, now consistent with everywhere else). VERIFY GOTCHA (documented for next time): a synthetic `dispatchEvent(new Event('scroll'))` check that reads `getComputedStyle(thumb).opacity` in the SAME synchronous tick reads the PRE-transition value (0) even though the `.show` class was just added correctly — false-negative. And a check performed >900ms after the scroll (i.e. after the auto-hide timeout already fired) also reads a bare `bai-scroll` className and looks broken. Neither is a real bug; the only trustworthy check is `classList.contains('show')` read WITHIN the 900ms window, or a screenshot. Wasted several round-trips suspecting a real bug before re-deriving this. — Why: uniform spacing reads as "nothing is more related than anything else" — the whole point of grouping is to make relationships visible via proximity; and scrollbars matter for consistency once more than one surface has one — leaving the chat body as the only "nice" one made every other list look unfinished by comparison.
2026-07-21 (cont.) — /blueai-desktop — designer: "removing the worker-picker launcher also killed the only path to the jobs flow, so all 5 message states are unreachable — fix it, and use the state toggler to preview them directly like we did in blueAI product." Two follow-ups: (a) added a dedicated "chat | states" pill toggler that instantly renders one of every message type (Plan/Warning/Input/Task-completed/Intermediate) by calling the SAME render functions directly (no flow needed), composer hidden while previewing — same pattern as blueai-product's ChatStatesPreview. (b) designer then asked for the actual canonical Skills/Scheduled/Settings flows from /blueai-product to be ported here (style translated to terminal-modern, not copied), plus the state toggler itself fixed to match blueai-product's pattern. Researched /blueai-product's real files first (skills.jsx/schedule.jsx/settings.jsx/ai_mode.jsx/byok_settings.jsx + the inline dev-preview-panel component in index.html) rather than guessing from memory. Asked 2 scope questions (both answered "full depth/parity, adapted"): BIG PORT — (1) built a reusable Subpane push/pop stack (back-header + scrollable body) generalized from the just-shipped Chat History pane, since Skills needed 2 stacking levels; (2) Skills rebuilt to real depth: 3 official categories + My Skills (starts empty — the empty state IS the real first-run state) → category drill-down → skill card (toggle/Try/Details or Edit+Delete) → details subpane (owner/status/category/updated) → 3-tab create/edit flow (write instructions / upload ZIP / create-with-BlueAI, the last one hands off to chat with a prefilled composer) → sticky "Build a custom skill" footer CTA + a live search that flattens matches inline; (3) Scheduled upgraded: freeform "when" text → structured cadence picker (Once/Daily/Weekly/Monthly + time), inline delete-CONFIRM (not instant remove), one-time loading shimmer on first tab visit (hooked into setMode, guarded by a `schedShimmerShown` flag); (4) Settings rebuilt from a single theme toggle into a 6-card stack: Appearance (existing) + Connected instance (static) + Notifications (Telegram-equivalent pairing-code flow, reworded from Prime-monetization framing since this agent has none) + AI Mode (Cloud/Hybrid two-tier, Auto sub-flow is a REAL simulated download→install→running state machine with a cancellable progress bar, Custom sub-flow is endpoint+model+Apply) + Own API key (BYOK, the same 4-stage model as blueai-product: off → no-key warning+provider picker → key-entry → saved+masked+AI-model row) + AI defaults (accordion-style default-model picker reusing the MODELS array, max-tokens/max-steps/timeout fields, Save with a transient success line); (5) the two separate always-on pill togglers replaced with ONE collapsible bottom-right "Preview" panel (gear-icon FAB when collapsed, matches blueai-product's exact fixed-dev-chrome-color convention rather than following the app's own theme) whose rows are computed progressively — Session always, "Chat states" only appears when pageState=default AND the active tab is chat — wired through a new `currentTabMode` var set inside `setMode()` so the panel re-renders its rows on every tab switch. Chose an accordion-in-place model-picker over reusing the composer's upward-opening `.bai-menu` popup for the Settings "Default model" row specifically because that popup opens via `bottom:100%` and could get clipped by `.bai-set`'s `overflow-y:auto` if the row sits near the top of the scroll area — a real, if narrow, layout risk not worth taking for a low-value reuse. Verified every card/flow/state live (screenshots + DOM assertions) including light theme (all new cards are pure `--bai-*` tokens, zero hardcoded colors, so theme-switch required no extra work) and 0 console errors throughout. — Why: the designer's brief was explicit — the FLOWS already exist and are proven in the canonical product; the job here was faithful port + honest re-skin, not re-invention, and NOT inventing content that doesn't fit this agent's story (no Prime upsell language) matters as much as the parts that were kept faithful.
2026-07-21 — /blueai-desktop — large PM feedback pass (11 points) on the terminal-modern prototype (DS-unbound experiment; craft gates still apply). (1) Onboarding cards → PM's literal 5 labels (Posted to Instagram/Free game installed/Daily streak kept/Coins collected/Morning brief delivered), no checkmark, but a "Here's what I can do for you" framing line added above the carousel — designer's own resolution to the direct conflict with the PREVIOUS session's fix (past-tense cards read as "already done"); retimed the 3-card 9s rotation to a 5-card 15s rotation (same 3s-per-card cadence, re-percented keyframe). (2) Subtext → PM's exact copy. (3) Tour copy → PM's exact headings/body for skills/scheduled/chat. (4)+(5) Skills + Scheduled tabs rebuilt from "·· soon" stubs into real list+empty-state+inline-creation-form panes (seeded 2 sample items each so both the populated and PM's exact empty-state copy are reachable by removing items; Scheduled cards get an on/off toggle). SELF-CAUGHT bug: first pass duplicated the pane label (my new list-head repeated "Skills"/"Scheduled" that the shared main-title bar already shows) — fixed by dropping the redundant label, keeping only the "+ New X" button in the list-head. (6) Replaced the cramped title-button dropdown with a full "Chat History" pane (back arrow + cards showing message count + relative time) + added a "Scheduled" pill badge (2 new seeded scheduled-run entries) to both the new History pane and the always-visible wide-sidebar recents list — directly answers "was this chat scheduled." (7) New-chat icon (was a pencil-in-box = reads as edit) → plus-square, fixed in both places it appeared (title row + sidebar Chat-row icon). (8) Chat input placeholder → rotating contextual example prompts (Claude-Code-style), gated off whenever a flow's own `s.allowText` placeholder is active so it never fights a real question. (9) Added Warning (non-blocking amber heads-up) and Task-completed (green terminal card) message-render functions + a "◆ PROPOSED PLAN" label on plan-kind result lists; Input-needed and Intermediate were already covered by the existing ask/options and workSpinner mechanics, so left those as-is rather than over-building. Threaded all 5 states into the `jobs` flow (flows.js) so they're demoable end-to-end, not just theoretical. Verified all 11 points live (screenshots + DOM checks) before reporting back. — Why: PM's product-review pass on a not-yet-launched redesign; several items were genuine UX gaps (icon reads as wrong action, cramped/broken history UI, tabs still stubs) rather than taste preferences, so applied directly; the one real tension (card copy tense) was raised as a question rather than guessed on.
2026-07-08 — /blueai-product BYOK — PM feedback pass (5 items). (1) OOC popup copy → heading "Use your own key" (was "Use Your API Key"), body "Add a key to continue using BlueAI.", button "Add API key", subtext "You can manage it later in settings." (shorter → now one clean line). (2) DELETE-ON-OFF state model: switching the BYOK toggle off in Settings now deletes the key (ByokSettings toggle → setOn(false)+onRemove()); there is no "key saved but off" state anymore, so CreditsByokRow collapsed from 3 states to ONE (the invite) — removed the amber "Your own key is off" nudge. Re-enabling starts fresh (no stale key). (3) Credits-screen "credits are paused, with N waiting" banked line now only renders when credits>0 (at 0 the "0 waiting" read was confusing — PM). (4) Non-prime credits screen was top-congested → more vertical rhythm (balance block 18→40px top, subtext 14→26px btm, MORE-WAYS label 16→30px top). (5) Invite row copy → "Add your key in Settings to use BlueAI without spending credits." Verified all 5 via Playwright (popup copy, delete-on-off + fresh re-enable, banked-line-hidden@0, invite copy). — Why: PM copy + a simpler state model (off = deleted, not parked) that removes a whole confusing sub-state.
2026-07-08 — /blueai-product credits-screen congestion — designer: "screen feels congested / everything towards the top in some stages; utilise the space and DESIGN the bring-your-own-key + running-your-own-key." Root fix = make the BYOK sections carry the screen instead of being thin strips at the top. (1) INVITE (no key): CreditsByokRow upgraded from a one-line row → a full CARD (gradient-wash key circle + "Bring your own key" + "Use BlueAI without spending credits." + hairline + 3 benefit checks [works with OpenAI/Anthropic/Gemini · billed to your provider not BlueAI · stored on this device only] + full-width "Add your key in Settings →" CTA). (2) RUNNING (key on): the byokActive panel enriched — Active badge, "No BlueAI credits are consumed…", provider·model CHIPS (green-dot provider + mono model), + Manage CTA. [FOLLOW-UP same day: the paused-balance note ("your credits are paused, N waiting…") was REMOVED entirely — designer flagged it redundant with the "No BlueAI credits are consumed while this is on" line directly above it. The whole byokActive credit screen no longer references the banked balance; the "no credits consumed" line carries the message. LESSON: don't say the same thing twice in one card — a reassurance line and a status line that mean the same thing is one line too many.] Both now fill the mid-screen so the layout reads intentional, not top-congested; MORE-WAYS sits below with real breathing room. Verified all states via Playwright (invite 3-benefits+CTA; running prime-banked shows chips + "49,812 waiting"; running @0 hides the paused note), 0 real errors. CANDIDATE taste rule: to fix a "congested/top-heavy short screen", don't just pad — give the screen's PRIMARY action real design weight (a proper card with icon/benefits/CTA) so it occupies the space with intent rather than leaving a dead lower half. — Why: the designer wanted the space USED, not just spread; a substantial BYOK card is the natural hero of the credits screen when you're out of/off credits.
2026-07-08 — /blueai-product BYOK invite card — PM: "too much info, remove the 3 benefit points, rest fine" + (when I flagged the points-removal would re-open the empty-space gap) "use a SQUARE card instead of rectangle, that solves the empty space too." So: dropped the 3 benefit checks, and made the invite a TRUE square (`aspectRatio:'1/1'`, full-width → ~360×360) with content vertically CENTRED (icon → "Bring your own key" → "Use BlueAI without spending credits." → full-width CTA). The square fills the mid/lower screen so there's no dead space; MORE-WAYS scrolls just below. CORRECTION (same session): a literal `aspectRatio:1/1` square was TOO big — full-width → ~360×360 pushed the MORE-WAYS section into heavy scroll. Designer: "woah that's too much… he didn't mean exactly square." Dialled back to a MODERATE centred card (padding '34px 24px 26px', no aspect-ratio) — chunkier than the old thin strip but compact enough that both MORE-WAYS cards stay in-frame (verified Claim-25k within the app frame, no heavy scroll). LESSON: "make it square" from a PM = "make it chunkier/more balanced," NOT a literal 1:1 — a true square on a full-width mobile card is huge; interpret the intent (fill the dead space, look intentional) and size to keep the rest of the screen visible. Left the "Running on your own key" card rectangular (has provider·model chips = more content). Verified: points gone, moderate card balanced, 0 errors.
2026-07-08 — /blueai-product Prime upsell pricing — PM: add the pricing info (from the live version) that Prime is $3.99 this month and $7.99 next month onwards. (1) Compact "Get AI Credits with Prime" card (byok-active prime-non-member, index.html): subtitle → "5,000 AI credits every month", button "Try Prime" → "Try for $3.99", + a muted "$7.99 next month onwards." line below (mirrors the live card's structure: this-month price on the button, next-month line under). (2) Full PrimeUpsellCard (byok.jsx, used in OOC popup + standalone prime-non-member credits): next-month price corrected $4.99 → $7.99 (the old $4.99 was the stale Figma-teaser number; the live version = $7.99). Both Prime-upsell surfaces now consistent. Verified via rendered-element checks + screenshots, 0 errors. — Why: real pricing transparency (first-month promo vs standard) + one canonical next-month price everywhere.
2026-07-08 — /blueai-product credits screen — PM: a PRIME MEMBER who switches their own key ON loses the whole Prime path — the byokActive branch short-circuited FIRST (geo-agnostic) and replaced everything with just the "running on your own key" card, so the Prime balance + "Top Up or Manage Credits" (top-up/manage) dissolved entirely. FIX: inside the byokActive branch, when `primeGeo && isPrime`, stack a SECOND card below the key card — a "Prime credits" card styled to MATCH the running-key card (icon+title+"Paused" chip + "{balance} credits · renews in 11 days" + outlined "Top Up or Manage Credits"), NOT the full standalone Prime layout (balance/spend-limit/breakdown) — per designer ("similar card, not exactly the only-prime one"). Container made a flex column (gap 12) so the two cards read as one screen. Gate scoped to `primeGeo` (Prime doesn't exist in non-prime geos; isPrime/creditsPrime can stay true across a geo switch in the dev toggler). FOLLOW-UP (designer: "this too" for prime-non-member): the gate is now `primeGeo && (isPrime ? <Prime-credits manage card> : <Get-Prime UPSELL card>)` — so a prime-geo NON-member on their own key keeps the Prime upsell (compact matching card: "Get AI Credits with Prime · 5,000/mo · from $3.99 · Try Prime"), not the full standalone PrimeUpsellCard. Verified via SCREENSHOTS (rendered-element checks, not body.textContent): prime-member+key = key + manage card; prime-nonmember+key = key + upsell card; non-prime+key = key card only. — Why: BYOK-on PAUSES the Prime path (member's credits or non-member's offer), it never forfeits it — every prime-geo user keeps their path reachable while on their key.
VERIFY-NOTE (harness, important): on the CDN/Babel product page, `document.body.textContent` INCLUDES the inline `<script type="text/babel">` source, so `textContent.includes('<any JSX string>')` is a FALSE POSITIVE (matches the source even when unrendered). This made a correct fix look like a "both branches render" bug for ~5 checks. ALWAYS verify render state via `querySelectorAll('p'/'button'/…)` on real elements (or a screenshot), NEVER `document.body.textContent.includes` on this page. (Pairs with the msedge disk-cache cache-bust note below.)
2026-07-08 — /blueai-product BYOK invite card — designer asked for "some UX aesthetic — a stroke circle expanding out from the key, opacity going down with range." First built an animated radar RIPPLE (ba-ripple keyframe, 3 staggered rings) — then designer: "can it be not animation." Switched to STATIC concentric stroke rings: removed the ba-ripple keyframe/class from index.html (no dead CSS); byok.jsx renders 3 static rings (d 68/86/104, opacity .34/.18/.08 — fading with distance) absolutely centred on the key. KEY LAYOUT MOVE: the badge box stays 58px in the flex column and the rings OVERFLOW it (absolute, margin-offset centring) so they add the halo WITHOUT reserving height — content below doesn't shift (my first static attempt used a 132px wrapper which pushed the 2nd MORE-WAYS card off; this fixes that). Rings sized to clear the title (max 104 → badge marginBottom 24 gives clearance). Verified both MORE-WAYS cards fully visible, rings don't overlap the title, 0 errors. — Why: same "range-faded radar halo" read, static, and crucially non-layout-shifting (absolute overflow, not a reserved square zone).
2026-07-10 — /blueai-product chat screen — human: "we have several states right - like default, error, success so we need a warning state too." Investigation found the amber "warning" tone already EXISTED in FINAL_STYLES but mis-named `input` and completely dead (makeTaskSteps only ever produced `tone:'success'` — fail/warning were unreachable). Traced to the original PM design source (design-source/blueai-pm/ui_kits/webapp/Chat.jsx) which defines exactly 4 message types incl. `warning: "I need a bit more information to proceed. Could you clarify..."` — confirms this is a real, previously-intended state that never got wired up during porting. FIRST PASS: renamed FINAL_STYLES.input → .warning; added an outcome param to makeTaskSteps + a 3-way "Outcome" toggle (Success/Warning/Error) that you'd set THEN type+send a message to see it play out. CORRECTED (same session, human): "instead of switch outcome and type anything, can we say the toggler as show chat states — if enabled we show all types in chat with respective msg?" — replaced the outcome-then-type flow with a single "Chat states" Off/On toggle; when On, ChatScreen renders a new `ChatStatesPreview` (one UserBubble + FinalBubble per tone — success/warning/fail — rendered instantly, no scripted delay, composer hidden since there's nothing to send) instead of the live conversation; makeTaskSteps reverted to its original always-success form (the outcome-branching logic was no longer needed, so removed rather than left dead). Verified via Playwright: all 3 bubbles render simultaneously with correct colors (success #d9fbe4, warning #fdf6cf, fail #fdecec), feedback thumbs only on the success example, composer absent while On, normal chat (composer + home) restored when Off. — Why: reviewing 3 message states shouldn't require typing 3 separate messages — a single toggle that shows every state at once is faster for PM/design review, and matches how the OTHER preview rows (Geo/Membership/Key) are direct state-pickers, not "set a flag then go trigger it manually."
2026-07-10 — /blueai-product chat screen — human: "the red text inside the error container feels a lil less contrast." Measured (WCAG contrast ratio, not eyeballed): error text #e07070 on bg #fdecec = 2.74:1 — fails AA's 4.5:1 floor, vs. success #1f2937/#d9fbe4 = 13.19:1 and warning #8a6116/#fdf6cf = 5.07:1. Error was the clear outlier. FIX: FINAL_STYLES.fail text+link #e07070 → #991b1b (7.28:1, verified via computed style in-browser + screenshot) — darker/more saturated red, matches the visual weight of the other two tones' dark-text-on-light-tint pattern. Left bg/border/stamp untouched (stamp is unused dead code, not a text-contrast concern). LESSON: when a contrast complaint comes in, compute the actual ratio (simple luminance formula, no tool needed) rather than eyeballing — it confirms/quantifies the fix and reveals whether siblings (success/warning here) have the same issue. — Why: accessibility is measurable, not a vibe check; compute before and after.
2026-07-10 — /blueai-product Settings — new feature: human confirmed the "Hybrid AI Mode" concept found in the PM's BlueAI Onboarding.zip design review is REAL/live-scoped (not exploratory like the Relax onboarding finding, which was explicitly dropped). Asked to "check the latest design ... and implement." Found 3 variants of "BlueAI AI Mode Settings" (base / v2 / standalone) — diffed base vs v2: v2 adds the Auto-vs-Custom method split (BlueAI-bundled local model vs user's own VLM endpoint), conflict detection ("another local model e.g. LM Studio running"), and richer OptionCard states (pending/badge). Confirmed "(standalone)" is v2's code bundled into one dependency-free file (identical AIModeCard signature), not a further iteration — so v2 = latest. IMPLEMENTED: new `ai_mode.jsx` (window.AiMode: AIModeCard, HybridModal, useAiMode hook) — full state machine ported faithfully (Cloud/Hybrid → Auto/Custom → idle/downloading/installing/running/failed/blocked), simulated download timer kept as-is (dev-only scripted product, matches the rest of blueAI's "no real backend" philosophy). Card chrome adapted to the PRODUCT's existing Settings-card style (white/shadow/border, matching BluestacksCard/TelegramCard) rather than the design source's flatter card — "match what's already there" over pixel-matching incidental container chrome. HybridModal reuses the shared `ModalOverlay` (title + dynamic close-handler-per-state) instead of hand-rolling new overlay chrome. Wired into settings.jsx between TelegramCard and BYOK (matches the source's card order exactly). Two states genuinely unreachable via normal clicking (hwSupported, conflict) got 2 new rows in the existing floating Preview panel, gated on `activeTab==='settings'` (progressive disclosure, same pattern as "Chat states" gated on isChat) — did NOT replicate the source's full internal scenario-tester (10+ buttons incl. force-fail states); that panel is PM tooling scaffolding, not shipped-feature surface, and no one asked to preview the failure states specifically. VERIFY GOTCHA (new): with a modal open, the SAME button label ("Download & enable") exists TWICE in the DOM at once — once inline in the card (opens the modal) and once in the modal's footer (actually starts the download) — a naive `document.querySelectorAll('button').find(text match)` grabs the first (wrong) one. Always scope modal-button queries to the modal's own container (e.g. the `position:fixed` overlay div) when both a trigger and its resulting modal can show matching text simultaneously. Verified full Auto flow (download % → installing → running → "Hybrid AI is ready") and Custom flow (endpoint/model → Apply → On chip → conflict banner) via Playwright + screenshots, 0 errors. — Why: a real product feature (not a demo toggle) deserves a real interactive implementation; only add dev-preview scaffolding for states that are truly unreachable by clicking, and keep new panel rows scoped to when they're relevant.
2026-07-10 — /blueai-product — designer caught 2 overflow bugs from screenshots on the just-shipped AI Mode feature. (1) Preview panel: "Hybrid hardware" label + "Supported"/"Unsupported" options together were too wide for the fixed 236px panel — pill bled past the card's right edge. FIX: shortened to match the panel's existing terse-label convention (every other row uses short binary labels) — "Hardware" + Yes/No, "Local AI conflict" → "AI conflict" (preemptive, same risk profile). (2) OptionCard: the title+pending-badge header row had no flexWrap, so a longer tag ("Choose a method", 16 chars) spilled past the card's border in the narrower two-up Cloud/Hybrid layout. FIX: added `flexWrap:'wrap'` to that row — badge now wraps to its own line under the title instead of overflowing (kept `whiteSpace:'nowrap'` ON the badge span itself so it wraps as a whole pill, not mid-text). Verified both via Playwright (bounding-rect right-edge deltas, both now negative i.e. contained) + screenshots. LESSON: any dev-panel row or card badge that's user-content-length-dependent needs either a length budget check against its container BEFORE shipping, or a structural wrap fallback (flexWrap) — don't assume short existing labels generalize to new, longer ones. — Why: same class of bug twice in one review (preview panel width budget, then component-level width budget) — both were fixed-width-container + nowrap-content collisions.
2026-07-10 — /blueai-product AI Mode — designer pushed back on my flexWrap fix itself: "changing container on click feels like a bad UI interaction, can't we just change the tag copy instead?" Correct instinct — flexWrap stopped the overflow but introduced a NEW problem, the Hybrid card visibly growing taller the instant you click it (a layout-shift regression, same family as CLS). Measured precisely rather than eyeballing: at this card's real width, "Choose a method" pill needs 103px against 90px available — no fit at any copy that long. First swap ("Select method", 88.5px) LOOKED like it should fit but was off by 0.3px in the live render (143.8px needed vs 143.5px available) — too fragile to ship on a coin-flip margin. Settled on "Pick method" (77.5px pill, ~11px of real headroom) — verified card height is IDENTICAL before/after clicking Hybrid (86.5px both), confirming zero reflow. Kept the flexWrap as a defensive fallback (never hurts, catches any future longer string) but the actual fix is the copy fitting on one line. Updated both the badge AND the "Choose a method" section header below it to "Pick method" for consistent wording. LESSON: when a text-fit bug has TWO possible fixes (let it wrap vs. make it not need to), prefer "doesn't need to wrap" for anything that appears on/after a click — a growing container mid-interaction reads as broken even when nothing overflows. And when measuring a fix, don't stop at "looks close on paper" — check the live rendered margin; <1px slack is not a real fix. — Why: interaction-triggered layout shift is a worse UX smell than the original overflow it was fixing.
2026-07-10 — /blueai-product chat screen — PM (Abhisht, in a Slack thread on the "Chat states" screenshot): "yes, bas warning wala nahi hai ismein" then, on "is yellow one not warning?": "that's human input required." So the amber tone I'd labeled `warning` is actually the LIVE product's existing "human input required" state (don't touch — PM confirmed it's correct as-is) — a genuinely separate "Warning" was still missing. Asked the human what Warning should mean (3 options: soft-success-with-caveat / mid-task heads-up that doesn't block / other) → picked "heads-up before/during a task, doesn't block progress" (e.g. "may use more credits than usual"). FIX: (1) renamed FINAL_STYLES.warning → .needsInput (semantic correction, same amber, unchanged per PM). (2) Built a genuinely NEW Warning: a distinct `WarningBubble` component (orange bg #fff7ed/border #fed7aa/text #9a3412 + caution-triangle icon), styled like a StatusBubble (task keeps going, not a terminal card) rather than a FinalBubble (which would wrongly imply the conversation ended) — this is the key structural difference from needsInput/success/fail, which ARE terminal. Wired a `warning`-role step into renderStep for the live scripted flow, and added it as a 4th example in ChatStatesPreview ("Farm resources on the world map" → "Heads up — this may use more AI credits than usual since it involves repeated actions."). Verified contrast before shipping (text #9a3412/bg #fff7ed = 6.88:1, icon #c2410c = 4.88:1 — both comfortably pass) and all 4 states via Playwright (computed colors match exactly: success #d9fbe4, warning #fff7ed/orange, needsInput #fdf6cf/amber, fail #fdecec/red). LESSON: when a PM says a color/copy "is" something specific ("that's human input required"), that's an existing product-semantics fact to preserve, not a design opinion to weigh — don't repaint it, free up the conflicting label and build the actually-missing state instead. Also: a "doesn't block progress" state should look structurally different (status-style, not a terminal card) from states that end the exchange — the container choice communicates blocking vs. non-blocking as much as the color does. — Why: matching the PM's real mental model of the product prevents a review loop where every round just relabels the same bug.
2026-07-08 — /blueai-product credits-screen out-of-credits subtext — designer: "text wrapping can be better, after a period." Applied taste 14 (multi-sentence line breaks at the full stop, one sentence per line): split the string on `/(?<=\.)\s+/` → each sentence its own `display:block` span. Now "You're out of credits." / "Add your own key to keep using BlueAI." (and the credits>0 variant likewise) instead of a ragged mid-phrase wrap. Each sentence still wraps internally if narrow (mobile-safe). Verified 2 distinct rows, 0 errors. — Why: house rule 14; period-breaks read as intentional, mid-phrase wraps read as accidental.
VERIFY-NOTE (harness): msedge-channel Playwright reused a DISK-cached copy of /blueai-product across launches — an edit's fresh HTML didn't load until I cache-busted the URL (`?v=fresh1`). When verifying edits to the CDN/Babel static product page, append a unique query param to the goto URL (curl the served file to confirm the edit shipped, but the browser still needs the cache-bust).
2026-07-03 — /moneymaker FOUNDATION built (post-S8-audit, phase ② of the plan) — new island route + mm/ components (MmEngine three.js golden-vortex wealth engine: 4.5k particles spiraling INTO a bright core, pointer parallax, ResizeObserver, reduced-motion static frame, full dispose; MmSessionCounter "while you've been here +$x.xx" FOMO ticker; MmLedgerTicker CSS marquee w/ one honest LOSS row) + mm-data + moneymaker.css (.v-mm, OWN local token layer: obsidian #060812 · champagne gold #d4af6a · ivory ink · Fraunces serif display · JetBrains Mono numerals) + Screen Library index entry. Direction per designer: DS-UNBOUND, "completely new, out-of-the-world, award-winning, premium+trust" — my pick = private-bank editorial luxury (serif + gold on obsidian; money as LIGHT, not literal bills), distinct from BOTH prior proposals. Anchors all resolve (earners + close sections built lean) per taste 35. Risk disclosure line styled as premium fine print (trust device). — Why: signature-first build order (if the hero doesn't wow, iterate THERE before building 8 sections).
2026-07-07 — /blueai-product ONBOARDING FLOW for logged-out users (designer: 5 attached screenshots specifying the exact flow; asked "do you got everything or do you need to ask me anything" — verified against the live code before answering rather than guessing). Discovery FIRST: 2 of the 5 screenshots (the "Check your browser" + "Logged in / Welcome back!" steps) already existed pixel-for-pixel in login.jsx's LoginModal (steps 'browser'/'success') — reused as-is, zero new code for those two. New work: (1) onboarding.jsx (NEW file) — OnboardingWelcome: circular gradient logo + "Hi, I'm BlueAI / What can I do for you first?" + 3 suggestion rows (icon/title/desc/circular-chevron, same hover-lift anatomy as ProductHome's category cards — Gate 2.2 sibling inheritance) + "Something else in mind? Just type it." link. (2) login.jsx — new step==='default' && v==='onboarding' variant: "You need AI Credits to use BlueAI" / gradient "500" + "Free AI Credits" / "on your first login" / check "Enough for ~10 tasks" / gradient "Sign in to claim" pill / Terms link — reused the existing Divider style + gradText pattern from creditsfirst/PrimeUpsellCard rather than inventing new chrome. (3) chat_product.jsx ChatScreen — new isOnboarding/onNeedLogin props + onboardingEngaged state (reset on sessionKey like the rest): shows OnboardingWelcome when NOT engaged, the plain IntroCard-only view (no ProductHome) once a suggestion/typeIt is picked (matches screenshot 2 exactly — this IS the "next screen" the designer described), and gates run() on isOnboarding BEFORE the zeroCredits check (keeps draft on cancel, same precedent as the BYOK out-of-credits gate). (4) index.html — ProductTopBar gained isLoggedIn (strips to bare logo+wordmark when false, matching screenshot 2's header exactly); App() converted the previously-DEAD `const isLoggedIn = CONFIG.isLoggedIn` into real state + a new "Session" preview-toggle row (first row, above Geo/Member/Credits); handleLoginSuccess flips isLoggedIn true + bumps sessionKey to drop into the DEFAULT flow. **CORRECTED same session (designer): the message that opened the sign-in gate must AUTO-SEND after login, and the category home (ProductHome) must NOT show in that case only** — I'd first built it to clear the draft and land on the category home (misread "go through the default flow" as "start fresh"). Fix: onNeedLogin(t) now hands the pending text up → App stores it in `pendingMessage` → handleLoginSuccess seeds `{text, autorun:true}` → ChatScreen's seed effect calls run() (instead of just prefilling the composer) → conversation starts → `started` true → ProductHome is skipped. Boundary verified: a plain New Chat (or a fresh login with no queued message) still shows the category home — auto-send is exclusive to the onboarding→login handoff. `pendingMessage` cleared on gate cancel too (no stale auto-send on a later unrelated login). LESSON: "go through the default flow" meant the default flow's LOOK (header/nav/credits), not "discard the in-flight task" — when a spec says "then continue normally," continuing the user's pending action IS the normal path; don't drop their intent at a state boundary. FOLLOW-UP (designer): the IntroCard greeting ("👋🏻 Hi, I'm BlueAI / Your AI worker...") was pinned at the top of the scroll area in ALL conversation states — designer wants it gone once a message is sent, in BOTH onboarding and default cases. Fix: gated the IntroCard on `!started` (was always-on except during the onboarding welcome) — `{!started && !(isOnboarding && !onboardingEngaged) && <IntroCard/>}`. Now: onboarding welcome carries its own greeting (no IntroCard), engaged-pre-send shows the "Just send your message…" greeting (screenshot 2), default home shows "pick a task below…", and the moment a task sends (started=true, either flow) the greeting clears so the conversation opens clean. Verified both paths via DOM + screenshots, zero console errors. Taste candidate: a home-screen greeting is a PRE-CONVERSATION affordance — retire it once the conversation begins (it's answered its purpose), don't leave it as a permanent scroll header (echoes taste 30: a signal has a lifespan tied to the journey stage).
2026-07-07 — /blueai-product PREVIEW PANEL → progressive disclosure (designer: the state toggler shows all settings even when they don't apply to the current screen; only applicable options should show, and one toggle shouldn't appear until its prerequisite is set — "more relatable to the product"). Rebuilt the panel to mirror the product's real dependency chain instead of a flat dump of all 4 rows: Session (root) → Onboarding (logged out) shows NOTHING else (no credits/geo concept pre-login) · Default (logged in) → Geo → Prime geo shows Membership + Credits · Non-prime geo shows Own key → and Credits only when NO key (with a key, "no credits consumed" per the credits screen, so the Credits toggle is genuinely moot → hidden — the exact "toggle not applicable → don't show" the designer described). Implemented as a rows[] array built from state + interleaved dividers via React.Fragment (clean divider handling: hairline before every row except the first, so no leading/trailing/orphan dividers as rows appear/disappear). Also relabeled for product clarity: "Member: Prime|Non-prime" → "Membership: Prime|Free" (Free = the non-subscriber tier that sees the Prime upsell); "Key: No key|Key added" → "Own key: Not added|Added". Underlying state (primeGeo/creditsPrime/keyAdded/zeroCredits) persists while hidden — flipping back reveals the last value, no reset. Verified all 4 disclosure states via one-action-per-eval DOM reads (multi-step in a single synchronous eval reads stale DOM — React doesn't re-render mid-eval, a repeat of the scrub-verification trap): onboarding=[Session] · default+prime=[Session,Geo,Membership,Credits] · default+nonprime+nokey=[Session,Geo,Own key,Credits] · default+nonprime+keyadded=[Session,Geo,Own key]. Zero console errors. Taste candidate: a state/dev control should model the product's OWN dependency tree (each control gated on its prerequisite) — a flat list of every possible toggle invites setting combinations that can't occur in the real product; progressive disclosure makes the control self-documenting about what actually depends on what. — Why: the toggler is a comprehension tool for the designer/dev; if it shows impossible combinations it misrepresents the product's state model.
2026-07-07 — /blueai-product ONBOARDING FIDELITY PASS against the cloud design ZIP (designer: "UI issues in the onboarding we built from screenshots — uploaded the cloud design so you can match properly"). Extracted the ZIP; the canonical sources were blueai/relax_flow.jsx (TaskPicker + OnboardingChat) + blueai/login.jsx (creditsfirst gate). DISCOVERY-FIRST paid off again: the browser/signing/success login states were already 1:1 in my login.jsx (design-identical) → zero changes there; the real gaps were the picker, the chat greeting/hint, and the gate. Fixes to MATCH the design: (1) onboarding.jsx OnboardingWelcome rebuilt to the design's TaskPicker — vertically CENTERED chrome-free screen (was top-aligned inside the chat shell), real assets/BAILogo2.svg sparkle logo 64px (copied the asset in; was a hand-built gradient circle), "Hi, I'm BlueAI" 18px muted + "What can I do for you first?" 25px/800, 3 BIG cards (50px icon tiles r14, 17px titles, 14px subs, r16, 1.5px #dbe3ee border, shadow, colored-border 30px chevron circles), the design's exact deeper palette (games #047857/#D1FAE5, codes #0369A1/#E0F2FE, gift #BE123C/#FFE4E6) + the design's glyphs (ChatCatIcon game/discover/social — added 'discover' to ChatCatIcon; my old build wrongly used a wallet CashIcon for games), staggered fade-up entrance, gift prompt "partner" not "girlfriend". (2) chat_product.jsx: added the missing "Send a message to watch BlueAI work" pulsing hint pill above the composer (isOnboarding && !started); onboarding composer border→1.5px #60a5fa r18 w/ focus ring (design); greeting padded down + 16px sub via a new IntroCard `onboarding` prop. (3) login.jsx onboarding gate ported 1:1 from the design's `creditsfirst` — glow orb (cfGlow) behind a 100px 3-STOP-gradient "500" (#0EA4C5→#7B4CFF→#A855F7, ls -4px; was a flat 56px 2-stop), TWO gradient-fade dividers (was one), wide 230px gradient button #22ACE0→#7B4CFF 16px/800 + big shadow. ARCHITECTURE: lifted the picker to an App-level phase (`onbPhase` 'welcome'→'chat') rendered chrome-free (no top bar / no bottom nav) — the design's picker is its own screen; picking a task sets onbPhase='chat' + seeds the composer prefill; ChatScreen lost its internal onboardingEngaged/OnboardingWelcome (welcome now lives one level up). Verified all 5 screens via Playwright screenshots (the preview screenshot tool wedged again on this page — Playwright is the fidelity-verify path): welcome=centered+chrome-free ✓, chat=greeting+hint pill+prefill+stripped header+bottom nav ✓, gate=500 hero ✓, success ✓, Continue→auto-send conversation ✓; default New Chat still shows category home (regression ✓); "Just type it"→empty focused composer ✓; zero console errors. NOTE (deferred, flagged): login.jsx `creditsfirst`(300)/`minimal`(300) variants are now unreachable (variant hardcoded "onboarding") — dead-code cleanup candidate for the next audit, left this pass to keep it fidelity-focused. LESSON: when a designer says "match the design," extract their source and port the exact values (100px not 56px, 3-stop not 2-stop, the real logo asset, the real palette) — a screenshot-approximation gets the STRUCTURE right but drifts on every dimension; the source file is ground truth. — Why: the screenshots gave me the flow; only the design source gives the pixel-accurate execution the designer actually wants.
2026-07-07 — /blueai-product credit-gate CTA hover (designer: "should have some hover effect right?"). The design's creditsfirst button shipped with NO hover — but a primary CTA that's inert on hover reads dead and breaks consistency with every other CTA in this product (New Chat, credits CTA, byok pill all respond). Added: "Sign in to claim" → translateY(-2px) + shadow 0 6px 28px/0.55 → 0 12px 36px/0.7 + brightness(1.05), 0.16s ease; and the Terms link (also inert) → color #94a3b8→#64748b on hover. Verified via Playwright computed-style before/after (transform none→matrix(-2y), shadow deepened) + a clipped screenshot showing the lift/glow. — Why: faithful-to-source ≠ blindly copying an omission; interactivity affordances (hover response on every clickable) are a house rule (taste 35: every visible control must DO something believable) that overrides a static mock's missing state. Candidate taste rule: port the design's LOOK exactly, but always add the interaction states a static export can't show (hover/active/focus) per the product's established response pattern.
2026-07-07 — /blueai-product login-modal hover SWEEP (designer: "same treatment" — extend the gate-CTA hover to the modal's other inert buttons). Added a DRY pair of helpers in LoginModal: `blueBtn(restShadow)` (solid-blue CTAs → translateY(-1px) + brightness(1.06) + shadow→0 6px 18px rgba(25,144,255,0.42), restoring each button's own resting shadow on leave) and `linkHover(base,hover)` (text links darken) — Gate 3 reuse instead of copy-pasting handlers onto 5 buttons. Applied to: Continue (success), Try again (browser), "Not seeing the browser tab?" link (browser, #f59e0b→#d97706), and the default-variant Sign in + Terms (currently unreachable dead variant, but treated for consistency if ever re-enabled). Helper returns ONLY the handlers — `transition` added inline per button (a `style` key in the helper would clash with each button's own `style` prop; spread-order would silently drop one). Verified reachable ones via Playwright computed-style before/after: Try again none→translateY(-1px)+new glow, Continue 0 2px 8px→0 6px 18px, both confirmed + screenshot; zero console errors. — Why: consistency — once one CTA in a surface reacts, the inert siblings read as broken; a shared helper keeps all five identical and cheap to maintain.
2026-07-07 — /blueai-product BYOK RELOCATED to Settings + simplified out-of-credits popup (designer, from the cloud design's "BlueAI AI Mode Settings.html" + 5 screenshots): "add BYOK setting in prime AND non-prime geos in the settings panel; the option to add the key should be in settings. When credits exhaust, tell users they need credits; showing all options in the popup is too much — use a concise popup + button that routes to Settings and highlights the BYOK section. In the credits screen, an option/button with more context instead of the inline form." Reworked the whole BYOK IA: (1) NEW byok_settings.jsx — a STAGED BYOK settings card (own file, keeps byok.jsx <300): toggle off→desc only · on+no-key→amber "No API key configured" notice + "Add a provider" dropdown (Anthropic / Azure OpenAI / Gemini (AI Studio) / Google Vertex AI / OpenAI, matching the design) · provider picked→API Key input + provider-specific hint ("Find at https://console.anthropic.com/settings") + "Your key is stored encrypted on this device only." + Cancel / Validate & Save · saved→provider + masked "••••qAAA" + Remove + a per-provider "AI Model" dropdown (claude-haiku-4-5). Rendered in Settings between Telegram and Chat AI Settings, for BOTH geos. (2) byok.jsx gutted of the inline form (ByokFields/ByokForm/KeyAddedPanel DELETED — no dead code) and given: ByokUpsell (the concise "Use Your API Key" popup body — key-in-a-circle + "Add your API key to continue using BlueAI" + full-width "Add API Key" + "you can manage/change later in Settings", screenshot 5) and CreditsByokRow (the credits-screen context row: invite state "Bring your own key / Add an API key in Settings…" → routes; active state "Using your own key / {provider} · no credits consumed / Manage →"). OutOfCreditsModal keeps its NOT-ENOUGH-CREDITS banner chrome; byok mode now shows ByokUpsell (was the full form — the "too much" the designer flagged). (3) App: BYOK state lifted (byokOn, keyAdded, keyInfo shared across Settings + credits + popup) + goToByokSettings() router — closes popup/credits, opens Settings, byokOn=true, and pulses a highlight ring on the BYOK card (data-section="byok", scrollIntoView + 4px blue ring for ~2s). OOC "Add API Key", the credits row, and the prime-geo "Or add your own key" link ALL route through goToByokSettings (BYOK is now the universal escape hatch, so it's reachable from every out-of-credits surface, prime or not). Verified via Playwright (preview screenshot tool wedges on this page): all 4 settings stages screenshot-match the design, provider dropdown = the exact 5 options, saved state has Remove + AI Model + masked key; OOC popup = concise BYOK prompt; "Add API Key" → Settings with the highlight ring computed-present; credits row shows the context copy; 0 real HTTP failures. CANDIDATE taste rule: a config UI (key entry, provider setup) belongs in Settings, not crammed into an interrupt popup — the popup's job at the moment of friction is to NAME the problem ("not enough credits") + offer ONE next step (a button that routes), not to host the whole form. LESSON: when moving a feature's home, route EVERY entry point to the new home (popup + credits screen + upsell link) and delete the old inline UI, so there's exactly one place the key is actually added. — Why: the designer's core point was IA, not pixels — BYOK is a settings concern; interrupt popups should be thin signposts to it, and every surface that hits the credit wall should point at the same door.
2026-07-07 — /blueai-product INDEPENDENT UX AUDIT LOOP over all screens updated this session (designer flagged the OOC-popup footnote "ⓘ …later in Settings" — icon vertically centered against 2-line centered text, floaty + ragged — and asked to run an independent UX auditor to fix UI issues on all updated screens, loop-engineering style). Fixed the footnote first: align-items flex-start + icon marginTop 1.5 (sits on line 1) + text-align left → clean 2-line wrap with icon anchored top-left. Then ran the LOOP: captured all 13 updated screens (welcome/chat/gate/browser/success/conversation/default-home/4×settings-byok/ooc-popup/credits-row) via Playwright clipped to the app frame, spawned an independent general-purpose auditor agent (fresh eyes, UI-craft rubric, no-feature-scope). ROUND 1 was INVALID — my capture script collapsed the dev panel BEFORE using its Session toggle, so "switch to Onboarding" silently failed and 6 screens captured the wrong (logged-in-home) state; the auditor correctly caught that the files didn't match their labels. LESSON (capture-harness): a screenshot audit is only as good as the capture — VERIFY each shot shows its intended state (assert a state-specific string per shot) before trusting ANY finding; a broken capture produces confident-but-worthless findings. Fixed the harness (panel stays OPEN, viewport widened to 1180 so the frame clip excludes the panel, per-shot state assertions all green) and re-ran a FRESH auditor (avoid anchoring on round-1's bad-data findings). ROUND 2 triage (verified every finding against ground truth rather than blindly applying): FIXED — 3 em-dashes (house taste rule 6 = no em-dashes: BYOK desc + 2 status-bubble strings). REJECTED as false-positive — "'Anthropic' is misspelled 'Anthropic'" (hallucination; grep-verified spelling correct). REJECTED as by-design/fidelity — send button flat #1990FF not gradient (the product mirrors the LIVE app's action-blue send/New-Chat; the gate's gradient is the design source's deliberate hero emphasis, not drift). REJECTED as misread — "red dot at 0 credits is contradictory" (the dot is a low/out warning via lowDot=credits<1000, working; NOT a 'new credits' badge). FLAGGED (not auto-changed, respecting "UI only" + no-silent-copy): 2 pre-existing default-flow em-dashes (Productivity desc, IntroCard sub); the lowDot threshold nuance (non-prime full=500 < 1000 so the warning dot shows even at a full free-tier balance — real but state-logic); and design-faithful/pre-existing nitpicks (default-home card baseline, "Chat History" affordance, browser-step spacing, gate 500 rhythm). Re-verified the 2 fixes render + no regression. NET: the footnote was the one real defect on the updated screens; the rest were false-positive/by-design/pre-existing/copy — a strong sign the screens were already clean. CANDIDATE reasoning rule: an independent auditor's findings are LEADS, not orders — verify each against source/ground-truth and the product's actual constraints (fidelity, existing intent) before applying; a fresh-eyes agent will confidently report hallucinations (a misspelling that isn't) and rule-violations-that-are-actually-fidelity-choices. — Why: the value of the independent auditor is surfacing what MY eyes miss; the value of ME is filtering its output through what it can't know (the live-app fidelity contract, the dot's real semantics, which copy predates the session).
2026-07-07 — /blueai-product BYOK made UNIVERSAL + geo-aware credit screen (designer shared a PM Q&A: "can prime-geo users also BYOK? what if they have prime credits AND their own key?" → PM: "yes they can use their own key; if they have both, whatever they've switched on will work"). So BYOK is universal (prime + non-prime) and the BYOK toggle is the MASTER SWITCH: byokActive = keyAdded && byokOn. Changes: (1) CREDIT SCREEN — new top-level byokActive branch (any geo) rendering a "Running on your own key" panel (green key circle + "{provider} · No BlueAI credits are consumed while this is on." + "Manage in Settings ›") ABOVE a muted banked-balance line ("Your Prime/BlueAI credits are paused, with {balance} waiting for when you switch your key off") — so a prime member sees their 49,812 is preserved, not lost, while on their key. This directly answers "what opens in the credit screen for this state." When byok is OFF, the geo-specific view (prime balance / prime upsell / non-prime 500+invite) shows as before. (2) SEND GATING — ProductChatScreen gets zeroCredits={zeroCredits && !byokActive}: a byok-active user never hits the out-of-credits popup (they run on their key). Verified: byok-active send produces the conversation, no OOC popup. (3) LOW-CREDIT DOT suppressed while on own key (credits paused, no warning needed). (4) TOGGLER RETENTION (designer: "the state toggler should retain the state right") — the "Own key" row now appears in PRIME geo too (was non-prime only), labelled Off/On, val={byokActive}, and its setter flips BOTH keyAdded + byokOn together (mirrors Settings' addKey). So: add a key in Settings → toggler shows Own key=On; flip Own key=On in the toggler → Settings shows the saved key; Credits row hidden whenever byok is on (moot). Verified both directions sync. (5) CreditsByokRow gained a 3rd state (key added but toggle OFF → "Your own key is off · turn it on in Settings" nudge) so the non-prime credit view never wrongly claims "using your own key" when byok is off; the row now keys off an explicit `active` prop, not `keyAdded`. Caught + fixed an em-dash I introduced in the banked-balance line (rule 6). Verified via Playwright: prime+key-on credit screen (49,812 paused), free+key-on (505 paused), toggler retention both directions, send-not-gated, 0 real errors. — Why: the PM answer defines the model — BYOK is a universal switch, credits are PAUSED not consumed when it's on, and the preview toggler must mirror that switch in every geo so the previewed state and Settings never disagree. Removed now-dead CONFIG.loginVariant (only consumer was this one LoginModal, now hardcoded variant="onboarding" since it's the only real caller). VERIFIED end-to-end via Playwright DOM clicks (not eyeballed): Onboarding toggle → welcome screen screenshot-matches image 1 → pick a suggestion → prefilled composer screenshot-matches image 2 → Send → gate modal screenshot-matches image 3 → Sign in to claim → browser/signing/success (pre-existing, unmodified) → Continue → lands on full DEFAULT header + ProductHome, Session toggle auto-flips to Default, draft cleared. Also verified the cancel path (X close mid-gate) keeps the draft intact, and "Just type it" focuses the composer without prefilling. Zero console errors throughout. CANDIDATE KB: before building a multi-screen flow from screenshots, diff each screenshot against the ALREADY-BUILT screens first — 2 of 5 here needed zero new code once checked, which also surfaces exact reusable chrome (Divider/gradText) for the screens that DO need building. — Why: matches the designer's explicit spec verbatim (default-flow handoff, no auto-continuation) while maximizing reuse of proven chrome (Gate 2/2.2) over re-deriving new modal chrome from scratch.
2026-07-03 — /blueai-product NON-PRIME-GEO BYOK (PM: WW release; Prime only in 21 geos → other geos bring their own key, still get 500 free credits, no subscription screens). Built new blueai/byok.jsx → window.Byok { ByokForm, KeyAddedPanel, PrimeUpsellCard, OutOfCreditsModal } + wired into index.html + chat_product.jsx. State model = TWO axes: primeGeo (Prime exists?) × [member prime/non-prime | key added/not] + a zeroCredits preview flag; credits=zeroCredits?0:(primeGeo?(creditsPrime?49812:505):500); lowDot=credits<1000 (replaced the !creditsPrime hack). NEW: on-send out-of-credits popup (didn't exist for either geo) — chat run() guards `if(zeroCredits){onNoCredits();return;}` (keeps draft); OutOfCreditsModal shows a red "NOT ENOUGH CREDITS" banner + PrimeUpsellCard (prime geo, image 1) or ByokForm (non-prime, image 2). Credits screen non-prime-geo branch = balance/500 (or "Using your own key") + ByokForm↔KeyAddedPanel(masked key + Edit/Remove) + kept the earn section. Dev toggler → two-tier (Geo → Member|Key) + Credits Full/Empty, now ALWAYS visible (needed to test the chat popup). DESIGNER DECISIONS (asked, 4): light theme (the dark wireframe was a layout sketch — app stays consistent) · build BOTH popups · two-tier toggle · keep earn section. Reuse: extracted PrimeUpsellCard so the credits screen + prime popup share ONE source (Gate 3, now used in 2 places); modal shell modeled on LogoutModal. Verified all 4 credits states + both chat popups + prime-member/non-member regression via DOM evals; 0 console errors. "Use Manual" label kept verbatim from the wireframe (flagged to designer as slightly odd; offered "Use my key"). — Why: model the geo dimension the WW release introduces, faithfully + previewably, without touching the prime-geo behavior.
2026-07-03 — /blueai-product FIGMA-FIDELITY PASS on the out-of-credits popups (designer flagged UX/UI issues + linked Figma "Instance Manager" nodes; pulled them via the Desktop Bridge — figma_get_status/capture_screenshot/execute, REST token was expired so bridge-only). FINDINGS from the real frames (Sign Up & Subscription §, banner 19615:62248 "Hero" + card 19615:62106 "Teaser" + 3 "Configuration" full-context variants): the drawer = a FROSTED "NOT ENOUGH CREDITS" banner PEEKING behind a white radius-12 card. Corrected my build: banner → muted-maroon `#3d2029` text at Regular/500 weight + wide tracking + filled `#eb553e` triangle (was bold bright-red `#dc2626` + outline); card → radius 12 (was 16), border `#b6b8cc`@80%, soft shadow `0 4px 32px rgba(0,0,0,.10)` (was heavy 0 20 60 .3); gradient-fade dividers; button → centered HUG pill radius-40 ~32-tall (was full-width tall); PRICE `$4.99` next month (was `$7.99`). Verified via DOM: banner rgb(61,32,41)/500/uppercase, card radius 12 + border + shadow exact, banner peeks 36px behind card, Try pill 157px hug (not 300 full). BIG LOGIC FIX (designer caught): a PRIME MEMBER out of credits was wrongly shown the "Get Prime" upsell — should be a TOP-UP (you don't re-sell Prime to someone who has it). So OutOfCreditsModal is now 3-mode: `!primeGeo?'byok':creditsPrime?'topup':'prime'`; added TopUpCard ("Top up to keep going" + "Prime credits renew in 11 days" + "Top Up Credits", built from the member screen's existing Top-Up-or-Manage action). Refactored ByokForm→ByokFields(+card wrapper) so the modal hosts fields without a double-card. DESIGNER DECISIONS (asked, 3): member top-up = design from existing pattern (no Figma frame exists — all 3 Figma error-drawer variants are non-member Prime offers: $0 trial / 7-day free trial / $3.99 teaser) · canonical non-member offer = the $3.99 teaser · BYOK = my wireframe version stands (no Figma frame). Verified 3 popup modes + banner/card chrome; 0 console errors. — Why: match the shipped Figma exactly + fix the member-upsell logic error; the top-up window is net-new (not in Figma) so built on-pattern.
2026-07-03 — /moneymaker RE-THEME "engineered money" (designer caught 3 things on the live hero: font "pretty basic — what all AI-developed sites use"; theme read as "jewellery + live trading news", not an AI money product; engine core too bright to read through). Did the asked-for RESEARCH first (madegooddesigns on Playfair/Fraunces cliché + Fontshare, Awwwards/Ballistic fintech 2026, Robinhood/COLLINS rebrand): high-contrast serifs (Fraunces/Playfair) = the AI-default "luxury" look; premium money-tech 2026 = distinctive grotesks + black/white/neutrals + ONE neon accent + money-as-DATA (numbers/motion), never gold ornament. Designer picked direction "Engineered money" (my rec) over editorial-serif + terminal-brutalist. CHANGES: palette obsidian+champagne-gold → graphite #0a0b0d + off-white #f4f5f2 + single neon money-green #00e38c (gold vars deleted); type Fraunces → Clash Display (display) + Satoshi (body) via Fontshare CDN (free-commercial), mono stays; H1 gradient-clip → solid ink; CTAs → solid green pill w/ dark ink; earner cards → neutral surface + green top-edge on hover; ticker muted to a quiet proof rail (neutral tags, green only on amounts, slower). ENGINE rebuilt as a PRECISION INSTRUMENT: 4k green data-particles in a FLAT disc (halved vertical scatter), 3 hairline orbit rings + small exact core ring in a shared tilt rig, restrained glow. BRIGHTNESS: core glow dimmed (alpha .7 peak) + scale 3.4→2.1-2.6 + a dark scrim layer ADDED BEHIND the copy in the veil (it previously vignetted only the edges — the text zone was the brightest spot). Verified: tsc 0, fonts actually loaded (document.fonts.check), palette/type computed correct, 0 console errors. CANDIDATE TASTE RULES for audit (cross-project, general): (1) avoid the AI-default display faces (Fraunces/Playfair/Space-Grotesk-everywhere) — pick characterful, less-worn faces (Fontshare tier) deliberately; (2) "premium money" ≠ gold+serif+sparkle (that's jewellery grammar) — convey wealth via NUMBERS/data/precision + one accent (Robinhood-school restraint); (3) a hero's ambient visual must never out-contrast the copy — scrim the text zone, not just the edges. — Why: the designer's read was right; research-backed re-theme instead of patching the old skin.
2026-07-03 — /moneymaker FULL REDESIGN → "The Autonomy OS" (designer brief: premium futuristic AI-agent site, NOT a trading/crypto dashboard; theme = /ai-video-creator-v2's language; GSAP parallax/3D/cinematic scroll; Vision-Pro/Runway/Linear tier; explicitly invoked the frontend-design skill). Killed the graphite+green "engineered money" skin entirely. REUSED creator-v2's cues at the source: GradientCanvas COMPONENT imported as the hero sky (Gate 2 — shared, not re-derived), frosted-glass grammar (white/.72 + blur 16-18 + iris glow shadow), Space Grotesk display + gradient-clipped word, --bai-cta-gradient pill CTAs, global --bai-*/--fg-* tokens (page now DS-CONSISTENT; only the composition is bespoke). NEW STRUCTURE: MmHero (parallax orbs + copy + the 3D-tilting glass "Autonomy OS" panel: agent chips + cycling activity feed + count-up "Earned today" + session FOMO chip; GSAP scrub un-tilts the panel + lifts copy on scroll; pointer-fine tilt) · MmAgents (4 glass cards w/ living CSS orbital marks, stagger reveal) · MmStory (PINNED night-shift scene, scrub: moon arcs + sky lightens + 3 beats surface + wake line; then trust pillars + --bai-cta-band close) · MmEngine/MmTicker DELETED (no dead code). GSAP via `gsap` + `gsap/dist/ScrollTrigger` registered in-effect, gsap.context revert on unmount, reduced-motion skips everything. VERIFIED with PLAYWRIGHT SCREENSHOTS (WSUP's install, script in session scratchpad → blueai/__preview/, gitignored) at 1440 + 390, 4 scenes each — the preview screenshot tool wedges on WebGL so Playwright IS the visual-verify path now. GATE-8 CATCHES (mine, pre-present): (1) pinned GSAP section froze at desktop width when the preview viewport shrank → page-level h-overflow; FIX overflow-x:clip on .v-mm (pinned/parallax layers can never scroll the page) + real flex bug (.mm-feed-what nowrap w/o min-width:0); (2) agents H2 `balance` broke THROUGH the full stop → explicit <br> at the sentence (taste 14); (3) feed column ran 3 rows vs the 4-chip column → 4 rows. CANDIDATE KB: pin-spacer inherits init-time width — ScrollTrigger.refresh is rAF-deferred (dead in backgrounded tabs); overflow-x:clip is the belt-and-suspenders for any pinned/parallax page. — Why: designer's 3rd direction call (jewellery → engineered-money → Autonomy OS on the house theme); consistency with creator-v2 beats a bespoke skin.
2026-07-03 — /moneymaker LIVE PROOF DEMOS (direction approved → next beat from the plan). New MmDemos.tsx → MmAgentDemo({kind,offset}): a tiny glass "job window" inside each earner card — 3 phase-driven steps (working label → done label) then the payoff lands as a gradient-wash ✦ chip; loops with per-card 900ms stagger so the grid never syncs; reduced-motion rests at the completed state. Scenes: Creator drafts→renders→publishes→+$3.22 ad revenue · Predictor watches odds→positions→resolves YES→+$18.75 · Trader rule-armed→sized-to-caps→executed→+$41.20 · Flipper finds→buys $89→sells $117→+$27.40. NO charts/tickers (the brief's hard avoid) — proof = the agent visibly doing its JOB (taste 8: show, don't claim; phase-driven per the legacy-scene precedent). CSS: .mm-demo inset (ink-wash 3.5% + hairline, min-height 132 so looping phases never reflow the card), active dot pulses mkt-blue, done dot green-ink, payoff chip = gradient-wash pill + mono amount. Verified: tsc 0, 4 windows × 3 rows live with staggered phases advancing (DOM), Playwright re-shoot desktop+mobile (cards caught at four DIFFERENT mid-loop states — exactly the intended "alive" read), 0 console errors, no overflow. — Why: the "wow, it makes money" beat is watching it earn, not reading a range.
2026-07-03 — /moneymaker NIGHT SCENE fixes (designer caught 2 on live scroll: (a) "nothing happens till this much scroll — feels broken" — empty sky for a long stretch; (b) the moon is "just a white ball — make it art: moon-like, clouds, stars"). ROOT CAUSE of (a): every reveal lived INSIDE the pinned scrub timeline, but a pinned section is on screen for a full viewport-height BEFORE the pin engages (start 'top top') — so the approach + early scrub showed bare sky. FIX: the head now reveals on a separate PRE-PIN trigger ('top 72%', non-scrub) so content is present as the section arrives; beats start at 0.12 of the scrub; pin shortened 190%→150%; moon arc retuned to x:-26vw→24vw (unit-based — xPercent on a small element only moves ±its own width, too subtle). (b) SKY ART, all CSS: moon = 5 crater spot-gradients + base radial + INSET terminator shading + double halo (lunar + faint iris); 2 repeating-tile STARFIELDS (radial-gradient dots, 230/340px tiles) twinkling out of phase + scrub parallax at different depths; 3 blurred CLOUD banks self-drifting (46-70s alternate) + scrub parallax. All added to the reduced-motion off-list. Verified: tsc 0, Playwright reshoot — cratered moon + stars + clouds + head + ALL 3 beats visible mid-scrub (vs empty sky before), no overflow, 0 console errors. CANDIDATE KB rule: pinned-scene choreography — anything the user should see AT pin-start must reveal on a pre-pin trigger; only the story beats belong to the scrub (a pinned section is visible ~100vh before it pins). — Why: a scroll scene that opens on dead air reads as broken, not cinematic.
2026-07-03 — /moneymaker LUNAR ARC rebuilt (designer: "liked the animation, wanted it more realistic — but the moon animation felt REMOVED"). The prior tween was a flat diagonal with element-relative xPercent (±its own 96px width — near-invisible travel) on a shortened 150% pin; my paused-preview eval couldn't even see the scrub (rAF frozen → transform reads none there; NOT trustable for scrub verification). REBUILT as a true 3-segment lunar arc on the scrub: set(x:-30vw,y:24vh,scale:.9) → rise (sine.out to -9vw/-5vh) → apex glide (10vw/-9vh, scale 1.08 — nearer at the top) → set (sine.in to 30vw/7vh, scale .96); pin back to 185% so the night breathes; beats still land from 0.15 (dead-zone fix intact). PROVED IN A REAL BROWSER (Playwright probe, msedge): moon center measured at 4 scrub depths = (313,342)→(588,104)→(829,71)→(1080,175) w/ width 100→108→96 — a genuine rise→apex→set parabola with the scale swell; screenshots at each depth confirm composition (rising beside the visible headline, cresting above it). CANDIDATE KB (2): (1) scrub-driven motion CANNOT be verified from the paused preview tab — rAF is frozen, transforms read stale/none; use a live Playwright probe that scrolls + MEASURES rects at multiple depths; (2) xPercent on small elements = travel relative to the ELEMENT (±1×width) — for scene-scale traversal use viewport units (vw/vh). — Why: an arc you can miss isn't a scene; measure motion, don't eyeball it in a frozen tab.
2026-07-06 — /moneymaker TWO NEW VARIANTS built from the strategy-meeting brief (Autonomy OS = variant 1 unchanged visually, light waitlist-copy touch-up only: CTAs -> "Join the waitlist" wired to the sitewide WAITLIST_URL SSOT, sub-copy weaves in the sell-time/skill/capital line). New shared src/lib/mm-shared-data.ts (WAITLIST_URL re-export, MM_MANIFESTO the exact 3-line progression, shared worker roster) so the messaging pivot is ONE source across all 3 variants, not copy-pasted.
VARIANT 2 "Mission Control" (/moneymaker/mission-control, mm2/ + moneymaker-mission.css) — SpaceX-launch-broadcast aesthetic per the brief. Procedural-only signature (NO scraped SpaceX imagery/branding — same IP-safety precedent as Pexels-not-scraping): Mm2Starfield (2-depth twinkling canvas), Mm2Ship (an ORIGINAL abstract capsule — SVG, not a literal rocket — + a small dedicated flame-particle canvas, exposes setBoost(0..1) via useImperativeHandle so GSAP scroll can intensify thrust with ZERO re-renders), live mission clock + capital counter, 4 "Worker Units" with telemetry-log proof demos (3rd skin of the now-3x-proven phase-driven mechanic), a swarm-formation canvas (nodes+links, ties to the meeting's scalability point), and a flight-manifest waitlist close with a scripted reservation counter. Palette: near-black + cool blue/green mono + Chakra Petch display + JetBrains Mono telemetry.
VARIANT 3 "The Capital Shift" (/moneymaker/capital-shift, mm3/ + moneymaker-capital.css) — manifesto-led editorial/keynote built directly on the meeting's exact line. SIGNATURE DESIGN DECISION: CSS authors the RESTING composition (the settled staircase: 2 small dim lines + 1 dominant gradient line) so reduced-motion/no-JS render the CORRECT final state immediately; GSAP only animates FROM a bolder equal-weight look INTO that already-correct CSS rest state (gsap.from, not to) — directly applies the night-scene dead-zone lesson from earlier this session (never let motion be a PREREQUISITE for a correct layout). Abstract "Two ways to earn" comparison (a flat line vs a rising gradient curve — deliberately NOT a literal chart/axes, honoring the "avoid stock chart-heavy layouts" instruction). Mm3Network canvas (hub+worker nodes, workers link to nearest hub only = a network not a mesh) reused in both ambient (sparse hero bg) and dense (dedicated scale section) modes via one component + a mode prop. Close line is the meeting's punchline VERBATIM: "Everyone will own an AI worker. It's the best way to earn money." Palette: deep violet-black + iris(#7B4CFF)-to-magenta gradient (threads ONE hue back to the blueAI brand while still reading distinct) + Unbounded display.
COPY JUDGMENT CALL (mine, flag for designer review): softened the meeting's "solve income inequality" framing into brand-safe benefit language (broadened access to capital-generating labor) rather than asserting it as a factual claim — avoids an overreaching marketing statement.
INFRA INCIDENT + FIX: running `npx next build` (production) while the dev server was LIVE corrupted the shared `.next` cache (both variants AND the previously-working /moneymaker + / started 500ing with "Cannot find module './819.js'") — same root-cause CLASS as the documented multi-session .next contention bug, this time self-inflicted by running build+dev against the same default distDir simultaneously. Fix: stop dev server -> rm -rf .next -> restart; confirmed all routes healthy after. NEW RULE: never run a production `next build` verification while a dev server targeting the same distDir is running — stop it first, or accept tsc-only pre-verification and defer `next build` to just before the git push (dev server already stopped by then).
VERIFIED (Playwright, both variants, desktop 1440 + mobile 390): tsc 0, next build 22/22 routes green, zero console errors post-cache-fix, zero horizontal overflow, all sections screenshot-reviewed (hero/fleet-or-compare/swarm-or-workers/close), demo state-matrix DOM-checked (exactly one active + N idle, matches CSS), reduced-motion headline-visibility checked on both. Index (src/app/page.tsx) updated: /moneymaker relabeled "1. Autonomy OS", two new entries "2. Mission Control" / "3. The Capital Shift". — Why: give the designer 3 genuinely distinct executions of the same brief to choose/mix from, each internally coherent and reusing the now-proven proof-demo + canvas-signature patterns rather than reinventing per variant.
2026-07-06 — /moneymaker REMOVED the "sell time -> sell skill -> sell capital" (Then/Later/Now) copy from ALL 3 variants (designer: this was the meeting's INTERNAL rationale/reasoning, not meant to be shown to users). Scope was wider than the literal quote — the same framing had also leaked into prose (variant 1's sub-copy said "You used to sell your time, then your skill..."; variant 3's sub said "For most of history, income meant trading your time, then your skill..."). Fixes: (1) mm-data.ts (v1) sub reverted to product-first copy, no historical claim. (2) mm-shared-data.ts gutted to JUST the WAITLIST_URL re-export — MM_MANIFESTO, MM_MANIFESTO_PUNCH, MM_WAITLIST, MM_WORKERS were ALL dead code on inspection (defined, never actually imported anywhere — each variant had its own local data file instead) — removed rather than left as unused exports (no-dead-code), with a code-comment flagging the framing as internal-only so it doesn't get re-added by accident. (3) Mm2Hero.tsx: removed the "// You sold your time / your skill / your capital" terminal-log block entirely (no replacement needed — eyebrow+H1+sub+CTA already carries the hero) + its now-dead CSS. (4) Mm3Hero.tsx: this was the BIG one — the whole variant's hero WAS the manifesto reveal (a 3-line "Then/Later/Now" staircase). Replaced with a single direct headline: eyebrow "BlueAI · your own AI worker" + H1 "Put your capital to work." (gradient on the payoff phrase) + a plain product-benefit sub, reusing the section-eyebrow style instead of a new class (Gate 1/3 reuse), dropped the era-specific GSAP tweens for a single staggered rise-in. CAUGHT MYSELF: a mobile-breakpoint override (`.mm3-era-2` font-size at ≤640px) was left behind pointing at a deleted class — found via grep, retargeted to the new `.mm3-h1`; this is exactly the kind of dangling reference a "remove the obvious render, forget the responsive override" sweep misses — grep the class name across the WHOLE stylesheet, not just the main rule block. VERIFIED: tsc 0, and a scripted Playwright pass across all 3 live pages asserting the forbidden phrases (/sold your time/i, /sold your skill/i, /sell your capital/i, Then...Later...Now) do NOT appear in rendered body text — zero hits on all 3, zero overflow, zero console errors. Skipped a redundant `next build` this round (the .next-cache-collision-with-live-dev-server incident from earlier today) — will run it once, dev server stopped, right before the push. — Why: internal strategy rationale ≠ public marketing copy; the fix required checking for the IDEA leaking in paraphrase, not just grepping the literal quoted sentence.
2026-07-06 — /moneymaker ROCKET WOW-FACTOR + TRUST-DEEPENING ACROSS ALL 3 VARIANTS (designer: bring out the wow factor on the mission-control rocket idle+scroll; go broader on the 3 trust levers with real sections/rich content/quality animations).
ROCKET (mm2): split the ascent-owning wrapper (.mm2-ship-wrap, GSAP scroll transform) from the ship itself (.mm2-ship, CSS idle animation) so the two transforms never fight — a pattern worth remembering for any future "idle-animated element that ALSO gets scroll-scrubbed" case. Added: idle hover-bob+wobble (@keyframes, always-on), a boost-driven GLOW layer (--boost CSS custom property written imperatively via setBoost/useImperativeHandle — zero re-renders, same pattern as the existing flame-particle boost) that brightens+swells with scroll, a breathing idle pulse layered on top via filter:brightness (doesn't fight the JS-driven opacity/scale), a window glint pulse, hotter/taller flame at high boost. Converted Mm2Starfield to the SAME forwardRef+setBoost pattern — stars gain downward drift + STREAK into lines past a boost threshold (a proper "warp speed" cue). Ascent trajectory made a pitching-over arc (added x-drift + more rotate/scale) instead of a straight vertical rise. VERIFIED (Playwright): glow opacity measured 0.49 idle → 0.82 mid-scroll (confirms the boost wiring is real, not just visually-plausible); screenshots show visible star-streaking + an elongated flame trail during ascent.
TRUST — same shared content (mm-shared-data.ts: MM_CUSTODY 4-step flow, MM_FAQ 4 objections incl. an HONEST "is this live yet?" entry, MM_BRAND_LINE borrowing BlueStacks' credibility), THREE distinct presentations per variant (Gate 1/2: reuse content, vary composition, matching the project's established multi-variant precedent from the hero-options era): (1) Autonomy OS → new MmTrustDeep.tsx, extracted OUT of MmStory.tsx (kept file-size sane) — glass step-flow with connector arrows + an FAQ accordion (grid-template-rows 0fr→1fr, the established SiteFaq/Ldv2 KB pattern — reused, not reinvented) + a quiet brand byline. (2) Mission Control → new Mm2Safety.tsx, inserted Fleet→Safety→Swarm→Manifest (capability→trust→scale→ask narrative arc) — the custody flow as a PRE-FLIGHT CHECKLIST (rows fade in, then each check-badge pops a beat later — "row arrives, then confirms", back.out ease) + FAQ as a COMM LOG transcript (>>INCOMING / MISSION CONTROL tags) + a GROUND CONTROL brand cert line. (3) Capital Shift → new Mm3Trust.tsx — the SAME 4 steps staged as a confident numbered editorial list (large gradient 01–04, no cards/boxes — matches the keynote voice) + the same accordion pattern re-skinned to the iris/magenta palette.
CAUGHT MYSELF (important): while wiring variant 3, re-grepped for the internal manifesto phrasing and found it had ALSO leaked into capital-shift's page.tsx — both the top-of-file comment AND, critically, the exported `metadata.description` (the `<meta name="description">` tag — crawler/social-preview-facing, NOT part of document.body.innerText, so my earlier Playwright forbidden-phrase check against body text legitimately could not have caught it). Fixed both. LESSON for the KB: a "no forbidden copy" sweep must ALSO grep page.tsx `export const metadata` blocks specifically — SSR meta tags are real public-facing copy that a body-text-only check structurally cannot see.
VERIFIED: tsc 0 throughout; full Playwright sweep (both variants × desktop+mobile, scrolled through the ENTIRE page in 500px steps to fire every ScrollTrigger + settle every pin) — zero overflow, zero real console errors (one 404 on /moneymaker desktop reproduced as a transient first-hit compile blip, gone on immediate retry — not a code issue); accordion click-to-open verified functionally (not just visually) on Mission Control's comm-log; reduced-motion sanity-checked (ship still renders, no dead states). — Why: "quality animations" meant proving the motion is real (measured, not eyeballed) and "rich content" meant one honest content source presented three genuinely different ways, not three copies of one section reskinned in name only.
2026-07-06 — TWO CORRECTIONS from designer screenshots, both diagnosed via live DOM geometry rather than eyeballing the screenshot. (1) moneymaker.css `.mm-custody-arr` (variant 1 custody-flow connectors): designer flagged "does this section look right?" — measured the arrows via getBoundingClientRect() and found the 20px arrow badge sat in an 8px grid gap, physically overlapping 12px into the next `.mm-custody-step` card (explains the "clipped sliver" look). FIX: widened `.mm-custody-flow` gap 8px->34px, resized arrow to 18px centered in the gap (`right:-26px`) — mathematically incapable of touching either neighboring card regardless of paint/stacking order, so the fix doesn't depend on reasoning about z-index precisely. Re-verified: all 3 arrows `overlapsNextCard:false`, 9px clearance each side. (2) moneymaker-capital.css `.mm3-hero-inner` (variant 3 hero): designer asked "isn't this empty on the right? modern UX?" — correct call, not deliberate negative space (the ambient network canvas was too faint to read as an intentional anchor). Took the lower-risk fix (center vs. give the right side its own visual weight): `.mm3-hero-inner` -> `align-self:center; align-items:center; text-align:center` (was left-aligned via default flex stretch), eyebrow/sub/CTA-row alignment overrides flipped to center, sub gets `margin:auto` to center within the wider column. Re-verified: leftGap==rightGap==290px at 1440px viewport, zero overflow desktop+mobile, zero console errors. CANDIDATE KB: diagnose visual-alignment complaints via getBoundingClientRect() gap/overlap math BEFORE touching CSS — turns "does this look right" into a measurable yes/no and the fix into something provably correct rather than eyeballed. — Why: both were genuine defects the designer's eye caught correctly; empirical verification (not re-eyeballing) confirms the fix actually lands.
2026-07-06 — TRIED, THEN REVERTED: built the "keep left-aligned, give the right side real presence" alternative (Mm3Network `focus="right"` bright hub cluster w/ shadowBlur glow + thicker inter-hub links, plus a `.mm3-hero-glow` CSS radial blob on `mix-blend-mode:screen` occupying the same coordinates so the two light sources read as one) — verified working (screenshot showed a genuine focal cluster, left-aligned, zero overflow/errors) but the designer's next message was "revert last change - centre aligned was fine": the centered version from the entry above was the right call after all. Reverted `.mm3-hero-inner`/`.mm3-sub`/`.mm3-cta-row` back to centered, deleted `.mm3-hero-glow` entirely (no dead CSS), and reverted Mm3Network.tsx to its pre-`focus`-prop form (no unused `bright`/`focus` plumbing left behind) rather than just disabling the prop — a rejected experiment shouldn't leave inert code in a component 3 other sections don't need. Re-verified: leftGap==rightGap==290px, zero overflow, zero console errors. CANDIDATE KB: when offering the user two options and they pick one, still prototype-and-hold the other lightly — but if they later reverse the call, fully revert (delete the unused branch/prop), don't just stop calling it; half-wired optionality is its own kind of clutter. — Why: the designer tested both live and confirmed the original recommendation was correct; the ask was a straight revert, not a blend of the two.
