# blueAI — Scratchpad
Inline correction-resolution log. One line per correction the moment it's
resolved. Promoted to decisions.md / taste.md at audit passes, then wiped.

Format: `YYYY-MM-DD HH:mm — <file> — <what changed> — Why: <one phrase>`

--- Pending audit entries ---
2026-06-13 — SCRAPPED the /live-demo byte-exact clone (designer: redundant now that the DS redesign exists). Removed: the index PAGES entry, the `/live-demo` redirect in next.config, `public/live-demo/` (served files); updated the v2 page comment + renamed the index entry "Live Demo Homepage · DS" → "Live Demo Homepage" (no non-DS twin left to disambiguate). Verified: /live-demo → 404, /live-demo-v2 → 200, no dangling `/live-demo` refs. **KEPT (flagged):** `design-source/blue-ai-demo/` — the pristine PM source the v2 widget was derived from (reference material in the source folder, not shipped); offered removal. **Route still /live-demo-v2** (the "v2" is now naming debt with no v1) — offered to rename to /live-demo for a clean URL, but held (touches the route folder + public/ + the widget src ref; not asked). — Why: the clone was a side-by-side comparison aid; once the DS version is the keeper, the comparison artifact is dead weight (no dead code / redundant pages).
2026-06-16 — live-demo-v2 (Ldv2Workers + Ldv2Lower footer via ldv2-data + live-demo-v2.css) — Connected the island funnel to the SEO flow: each worker card gets a "See it work →" link to its agent page (creator→/ai-video-creator, career→/apply-to-jobs, finance→/ai-trading-agent, markets→/prediction-market-agent); footer Product column gains Social Rewards (/social-rewards) + Developer (/developer). Sticky top nav left anchor-only. — Why: designer wants /live-demo-v2 navigable into the rest of the site like /seo.
2026-06-16 — taste rule 32 (LANDING = ISLAND FUNNEL) — ⚠️ REVERSAL flagged for audit: linking out contradicts taste 32 + the S6 island decision (the designer's OWN S6 correction: "anchor-only nav + login wall are the point"). Designer green-lit "Footer + worker CTAs" → page role shifts island ad-funnel → connected site page. Reconciliation kept the funnel's spine: sticky nav STAYS anchor-only; outbound links live in the footer (end-of-page, low leak) + as in-content "learn more" deepeners. Rule 32 needs a SCOPE CLAUSE at audit (it governs the persistent nav of a standalone funnel; footer + learn-more links are fine once a page is a connected hub). — Why: pages change role; making the reversal conscious, not silent drift (Gate 6.5 rule-conflict, outcome #2 = amend with scope clause).
2026-06-16 — Ldv2Workers "See it work" CTA treatment — full-page <a> (cross-route, scoped CSS), <Arrow size=14>, marketing-blue, gap-nudge on hover — Why: brand contract = every nav action carries the → arrow (decision 2026-06-12); full-page <a> for cross-scoped-route nav (CLAUDE.md / decision 2026-06-10).
2026-06-16 — Ldv2Workers + live-demo-v2.css — Moved "See it work" from the card BOTTOM to the top-right slot, REPLACING the static "LIVE" badge (removed the .live span + its dead CSS). — Why: designer's call; the LIVE badge was decorative+identical on all 4 cards, and section eyebrow / "4 workers live today" stat / result chips already carry "live" — swapping decorative chrome for an actionable link aligns with taste 30 (retire redundant always-on signals).
2026-06-16 — Ldv2 footer (ldv2-data LDV2_FOOTER) — Dropped the "Workers" in-page anchor from the Product column; replaced the dead "Company" column (About/Careers/Blog/Contact, all #) with the 4 individual workers (head renamed "Company"→"Workers"), DERIVED via LDV2_WORKERS.map so footer↔card titles/routes can't drift. — Why: designer wants the individual workers reachable from the footer, not a generic anchor; SSOT to prevent drift (taste 19/26).
2026-06-16 — live-demo-v2.css @media ≤560px — CAUGHT + FIXED a regression I introduced: the wider "See it work" link in the top row squeezed 3/4 worker titles from 1→2 lines on mobile. Fix: ≤560px the CTA drops to its own line under the title (flex-wrap + flex-basis:100%; padding-left:55px NOT margin — margin overflowed by 55px). Verified 375+1280: titles 1 line both, no overflow, CTA inline-top-right on desktop / own-line on mobile. — Why: the worker NAME is primary and must not wrap for a secondary link; recurring-category-#2 discipline = walk positioned elements through breakpoints BEFORE presenting (caught it myself, didn't ship the caveat).
2026-06-16 — live-demo-v2 page.tsx + MarketingHeader.tsx + ldv2-data + Ldv2Lower + (deleted) Ldv2Nav.tsx — STRUCTURAL: /live-demo-v2 now uses the SHARED SEO chrome (MarketingHeader + MarketingFooter) instead of the custom funnel Ldv2Nav/Ldv2Footer. Extended MarketingHeader (NOT forked) with two optional props: `links` (default HEADER_LINKS) + `cta` (default {Download for PC → DOWNLOAD_URL, new tab}); CTA now uses ext() so a same-tab anchor works. Live-demo passes localized in-page anchors (Workers#workers/How it works#how/Why BlueAI#why/Reviews#reviews) + Social Rewards/Developer (cross-page ↗, same as SEO) + cta {Hire a worker → #hire, same tab}. Footer = shared MarketingFooter as-is (Live agents/Product/Learn/Company; Learn links stay canonical /seo#). — Why: designer wants the live-demo page to carry the same SEO header/footer (one chrome SSOT), differing only by the Hire-a-worker CTA + page-localized scroller anchors.
2026-06-16 — VERIFIED shared-component safety + breakpoints — /seo + inner pages UNCHANGED (default props: Download for PC → download URL new-tab, SEO links) — confirmed live. /live-demo-v2: shared header sticky 71px frosted, hero clears it (overlap 0), no .ldv2 style-bleed into .bai-hdr links, no console errors; mobile = burger only (desktop .bai-hdr-center display:none, no doubled nav, no overflow), menu carries all links + Hire-a-worker CTA. — Why: editing a component used on 8 surfaces + recurring-cat-#2 breakpoint discipline.
2026-06-16 — taste rule 32 — ⚠️ REVERSAL NOW COMPLETE (escalates the 2026-06-16 footer/worker-CTA entry above): the live-demo page is no longer an island funnel — it has the full shared nav incl. outbound links + a localized anchor nav. This is the "full hub" path (offered + declined earlier, now adopted). AUDIT: taste 32 needs the scope clause (governs standalone ad-funnels, NOT a connected site member); reclassify the live-demo page island→hub. The login wall (#hire) stays — the GATE is intact, only the chrome opened.
2026-06-16 — SUPERSESSION note for audit — the earlier 2026-06-16 footer entries (folded Social Rewards/Developer into Ldv2 Product col; then individual workers replacing About/Careers/Blog/Contact in a custom "Workers" col) are now MOOT — the custom Ldv2Footer was deleted + replaced by the shared MarketingFooter (its Live agents col already carries the 4 agents; Product col already carries Social Rewards/Developer). Do NOT promote those two footer decisions; promote only the chrome-swap. The worker-card "See it work" CTAs (top-right desktop, own-line mobile) STAND.
2026-06-16 — DEAD CSS flagged for audit — live-demo-v2.css now has unused rules from the removed custom chrome: .ldv2-nav* / .ldv2-links / .ldv2-login / .ldv2-cta / .ldv2-nav-in / .ldv2-brand / .ldv2-footer* / .ldv2-fgrid / .ldv2-fcol* / .ldv2-blurb / .ldv2-fbottom + the ≤560px .ldv2-login/.ldv2-nav-in rules. Inert (no matching elements) but dead — remove in the audit-pass cross-file sweep (mirrors S4's .jf/.ag-stage cleanup). NOT removed inline (careful surgery; .ldv2-brand was shared by nav+footer, both gone now). — Why: no-dead-code, but heavy CSS removal is audit-cadence.
2026-06-16 — SCOPE/direction (designer) — /live-demo-v2 is the FINALIZED homepage, KEPT at that route (not promoted to /; / stays the Screen Library index). /seo + /hero-options + /hero/{stage,stage-original,3-cards} are DEPRECATED but NOT removed/delisted yet — explicit "do that later." Mapped the full blast radius (Explore audit, parked for the removal pass): DELETE-safe = components/seo/* + lib/seo-data + Hero{Stage,StageOriginal,Cards,Nav} + rich scenes (CareerScene/CreatorScene/FinanceScene, only HeroStage uses them) + home/{BaiHome,FeatureRows,AllSkills} (only the hero pages render BaiHome) + their CSS (hero-*.css, seo-home.css, hero-nav.css, homepage.css). KEEP-shared = the 4 Legacy scenes (Ldv2Workers uses them) + MarketingHeader/Footer + site-data. REPOINT-later = the /seo links in the shared chrome (header/footer brand + default HEADER_LINKS + footer Learn) → the new homepage. CURRENT TASK = reconcile the style guide + DS to the finalized homepage, field-by-field / icon-by-icon: ADD/UPDATE only (known gaps: the WORKER CARD is undocumented; the MarketingHeader per-page links/cta override is undocumented), LEAVE the deprecated seo-*/hero-*/home-* SG entries for the later removal pass. — Why: designer narrowed scope to DS/SG accuracy NOW, deletion LATER.
2026-06-16 — SG Foundations pass (Foundations.tsx) reconciled to the finalized homepage — COLORS: 3 stale labels fixed (CTA-gradient "Download pill"→"primary CTA pill"; "Marketing surface" group label + caption repointed from "homepage · SEO · hero chrome" → the surviving marketing pages: homepage + agent/social-rewards/developer). TYPE: reviewed, no change (universal scale, no deprecated ties). ICONS: added a "Social — brand logos" sub-group (Discord/X/Reddit) — filled + brand-colored (#5865F2/#000000/#FF4500 via a local SOCIAL_COLOR map, since the is-* classes are scoped to .bai-hdr/.bai-ftr); SVG paths reused from site-data SOCIAL (SSOT). Verified render (12 icon cards). — Why: the DS must document what the finalized homepage renders; the social trio shows on it (header+footer) but wasn't a named Icons entry.
2026-06-16 — Content width A′ (designer-chosen) — finalized homepage was the lone outlier (1240/32); the shared column used by the inner pages (.v-site .site-wrap, .v-agent .ag-hero) + the shared footer (.bai-ftr) is 1200/24. Aligned the homepage DOWN to 1200/24: --ldv2-content 1240→1200; gutters 32→24 on .ldv2-wrap + .ldv2-stats-grid + .ldv2-ctaband margin. ONLY live-demo-v2.css touched (footer + inner pages untouched → no regression). Verified body↔footer content edges align (both 57px @1280); the 1640 nav stays full-bleed over it (taste 20). SG Scales note rewritten: was the stale "--bai-content 1180px"; --bai-content is now used ONLY by deprecated surfaces (hero-*.css, homepage.css) → left defined (removal-later) but no longer cited as the shared column. New note: "1200px · 24px gutter — shared column (homepage · agent/social-rewards/developer · footer); 1640 nav full-bleed over it." — Why: one consistent content column; taste-20 footer-aligns-to-content.
2026-06-16 — SG deprecation-removal + finalized-add (style-guide/page.tsx NAV + ComponentsSection + MarketingPages + SiteComponents) — designer: "remove anything deprecated from the style guide, add everything finalized." REMOVED 8 deprecated SG entries: hero-nav (legacy) + seo-task/seo-info/seo-stage/seo-hero/seo-cta + home-feature/home-skill — from the NAV arrays, the ComponentsSection hero-nav block (+ HeroNav import), and MarketingPages (rewrote the file: dropped the 7 seo/home blocks + the now-unused imports seo-home.css / homepage.css / Sparkle). ADDED the WORKER CARD (id ldv2-worker, heavy PreviewAnatomy: icon tile · title · See-it-work CTA · scene stage · result chip) as the homepage centerpiece; reordered the Marketing-pages group homepage-first (ldv2-* then rw-*). DOCUMENTED the MarketingHeader per-page links/cta override (the "Hire a worker" homepage CTA) as a new site-header anatomy row. Repointed all SG copy that named SEO/hero → the surviving surfaces. Verified live: 8 deprecated ids gone, worker card + CTA render, override row present, NO console errors. — Why: SG must reflect only the finalized design system (live-demo homepage + agent/social-rewards/developer + in-app PM DS).
2026-06-16 — SG pass — NOTES for the later removal task — deprecated COMPONENT/ROUTE files remain ON DISK (per "removal later"): seo/*, HeroNav, HeroStage/Cards/StageOriginal, BaiHome/FeatureRows/AllSkills + hero-*.css/seo-home.css/homepage.css/hero-nav.css. HeroNav.tsx is now FULLY orphaned (its only importer was the SG). homepage.css is STILL imported by ComponentsSection (the DownloadCta demo borrows .bai-home .dl-cta) → when homepage.css is deleted, rescope that demo. Homepage How-it-works (Ldv2How) + CTA band (Ldv2Cta) intentionally NOT given separate SG entries — they're page-scoped instances of the already-documented site-hiw / site-cta-band patterns (document the pattern once, not per page). — Why: keep the removal-pass checklist accurate; avoid DS duplication.
2026-06-16 — SG audit cont. (the SHARED CHROME) — designer reframed: audit the SG against the SURVIVING surfaces (live-demo homepage + connected agent/social-rewards/developer pages); anything SEO-homepage-UNIQUE goes. Since the SG renders the REAL MarketingHeader/Footer, the fix is at the SOURCE = site-data.ts (so it corrects the SG AND the live chrome on all 6 surviving pages). HEADER_LINKS: removed What is BlueAI / Tasks / FAQ (SEO-only sections) → now Workers / How it works / Why BlueAI / Reviews → /live-demo-v2# + Social Rewards / Developer ↗ (mirrors the homepage nav, absolute hrefs so they work from any page). FOOTER Learn col: removed What is BlueAI + FAQ (the homepage has NO FAQ — designer's point), repointed How it works → /live-demo-v2#how, added Why BlueAI. Header + footer brand-logo href /seo → /live-demo-v2. SG site-buttons: removed the "Learn more" ghost (.site-btn-ghost is rendered NOWHERE but the SG demo) → .site-btn-ghost CSS in site.css now DEAD (flag for the removal pass). SiteComponents brand anatomy row → /live-demo-v2. Verified live: 0 SEO leftovers in the header, footer Learn = How it works / Why BlueAI, both brand links → /live-demo-v2, 1 CTA button (no ghost), no console errors. — Why: the chrome (the SG's most-visible shared component) still pointed at the deprecated SEO sections; the audit repoints everything to the finalized homepage. NOTE: the homepage override (LDV2_HEADER_LINKS, bare #) now mirrors the default (absolute) — kept for same-page smooth scroll + the "Hire a worker" cta.
2026-06-16 — SG audit cont. (remaining groups, element-by-element, codewise + DOM) — AGENT PAGES group: all 11 entries render the REAL surviving agent-page components (CareerForm/FinanceForm/FileUpload/VideoCard/form-kit molecules) → KEEP all. APP-PM group: all 6 (Cards/Overview/Inputs/Nav/Credits/Icons) document the in-app product DS (the surviving homepage widget + /blueai-product) → KEEP. COMPONENTS group: REMOVED the deprecated "Download CTA" entry — DownloadCta is rendered only by the deprecated BaiHome (+ the SG demo); no surviving page uses it, and the surviving brand CTA is the arrow-only .site-btn already documented under "CTA button + Arrow". Removed the block + the DownloadCta + homepage.css imports from ComponentsSection + the NAV item (homepage.css now has ZERO SG importers — only deprecated BaiHome). Rest of Components (buttons/status/pill-badges/credits/pills/feature-cards/bubbles/composer/header) = in-app DS reference → KEEP. BUG FOUND + FIXED (designer asked): PM Icons labels used text-[9px] — an ARBITRARY value BELOW the type-scale floor (config defines xs=10px as the caption token) → changed to text-xs. Same class as the S5 text-2xs latent-size bug; taste 26 (everything tokenised). Verified: download-cta gone, label computes 10px, the rest of Components intact, no new console errors. The CDN-Tailwind / in-browser-Babel console warnings are PRE-EXISTING /blueai-product no-build notices (that page is intentional CDN+Babel), retained from a screenshot-wedge tab-drift — unrelated to the SG. preview_screenshot reliably wedges this session (4× incl. after a server restart; the capture keeps landing on the heavy /blueai-product CDN+Babel page → 30s timeout) → all verification done via DOM/computed-style reads. — Why: SG documents only surviving designs; DownloadCta is deprecated-only; the 9px arbitrary label violated the tokenisation law.
2026-06-16 — DEEP VERIFICATION (designer challenge: "sure nothing's missing/wrong/dead/stale?") — Ran `next build` (authoritative): EXIT 0, ✓ Compiled successfully, ✓ types valid, ✓ 17/17 routes generated — proves every edit (chrome swap, SG removals, content-width, worker card, site-data repoint, DownloadCta removal, text-xs fix) compiles + type-checks; no broken imports, no type errors. Explore-agent dead-code sweep + my greps: ZERO dangling refs to removed symbols (LDV2_NAV/LDV2_FOOTER/Ldv2Nav/Ldv2Footer — only an explanatory comment); ALL <Tok to=..> SG anchors resolve; NO /seo or /hero links left in surviving chrome/SG (all → /live-demo-v2); NAV↔render integrity clean (48 ids, no dupes/orphans); no broken images; no empty sections; no console errors (only pre-existing /blueai-product CDN+Babel warnings). FIXED this pass: removed dead .site-btn-ghost CSS (its only consumer was the removed SG demo). CONFIRMED deprecated-on-disk (not new orphans, parked for the removal pass): home-data.ts (used by FeatureRows/AllSkills→BaiHome), --bai-content + homepage.css (used only by deprecated CSS), the /seo + /hero route/component files, and the root index still listing "SEO Homepage" + "Hero Options" (parked delisting). HONEST CAVEAT: could NOT do a pixel-level screenshot (preview_screenshot reliably wedges this session on the heavy /blueai-product CDN page) — all "visual" checks were DOM/computed-style reads, not a rendered image. — Why: prove-it-compiles + no-dead/stale, end to end.
2026-06-16 — SG SCOPE DECISION + full re-audit (designer chose "A — marketing-site DS only" after catching in-app dashboard content still in the SG: "why do I see this on SG"). The SG is now the SURVIVING MARKETING-SITE DS ONLY. The in-app PRODUCT DS reference is OUT — it documented the standalone widget.html + /blueai-product clone, which NO DS-built marketing surface is built on (the widget is a black-box iframe per ldv2-motion note). REMOVED: the entire "Components" group (buttons/status/pill-badges/credits/pills/feature-cards/bubbles/composer/panel-header — all in-app chat+dashboard) AND the entire "App (PM)" group (cards/overview/inputs/nav/credits/icons). DELETED the 3 now-orphaned SG files: ComponentsSection.tsx, PmComponentsA.tsx, PmComponentsB.tsx. page.tsx: dropped both NAV groups + both render sections + their imports; rewrote the intro ("marketing-site DS", dropped "PM supplement (status·accent)") + section descriptions (dropped SEO/marketing-homepage) + footer source (dropped blueai-pm). FOUNDATIONS re-audit (grep-verified vs surviving CSS): Colors — trimmed the "Accent + semantic status (from PM DS)" group to just Success + Danger (only ones a surviving surface uses — agent trade log BUY/SELL via --bai-success/danger in agent.css); removed Accent/Indigo/Warning/Info/Scheduled/Jobs (found ONLY in the removed SG + globals defs + deprecated homepage.css). Icons — removed "Send" (composer = in-app, removed); kept Check (agent PillsField uses it) + the rest. Site & patterns / Agent pages / Marketing pages: all verified marketing-surviving → KEPT. SG is now Foundations + Site&patterns + Agent pages + Marketing pages. FLAG for the token-cleanup pass (pairs with deprecated-file removal): --bai-accent/indigo/warning/info/scheduled/jobs are now marketing-orphaned but still referenced by deprecated homepage.css, so NOT pulled from globals yet. — Why: designer scoped the SG to the marketing site; the in-app product DS isn't part of the surviving marketing designs (Gate 6.5 — reverses the earlier "full DS reference" call, made explicit).
2026-06-16 — Creator page overhaul STARTED (PM brief: rebuild /ai-video-creator as an award-winning creative-tool landing — invideo/Nim/Higgsfield-style — on the blueAI LIGHT DS; GSAP + three.js approved). Direction LOCKED (designer chose): LIGHT + cinematic — out-execute the dark refs on blueAI's own brand, NOT copy dark; one dark --bai-cta-band "stage" planned for the example reel. Installed three + gsap + @types/three. Built FOUNDATION + HERO: creator-data.ts (hero/pills/HERO_MODELS/SHORTS/LONGFORM/TEMPLATES/MODELS/CAPS — brand-tint placeholders + our 3 own videos, NO third-party scraping per the copyright flag); GradientCanvas.tsx (three.js fragment-shader living iris→cyan→blue mesh gradient on near-white + cursor bloom; prefers-reduced-motion→static; ResizeObserver-sized; disposes on unmount); CreatorHero.tsx (frosted "Create a video in one shot" prompt [mock] + BlueAI-branded model picker + Generate + 6 format pills; framer rise-in); creator.css scoped .v-creator (all --bai-* tokens). page.tsx rewritten BESPOKE (off AgentShell) + shared MarketingHeader/Footer. Verified on :3001: hero renders, WebGL buffer sizes correctly (1265×744 after a ResizeObserver fix — preview_resize doesn't dispatch window 'resize'), no console errors. — Why: the landing must feel like a real creative tool; award-winning via execution on the brand, not abandoning the light DS. NOTE: CreatorForm.tsx now ORPHANED (only the old AgentShell page used it) → flag for cleanup. Higgsfield scraping DECLINED (third-party copyright on a public Vercel deploy) → safe placeholders. NEXT: format galleries (Shorts/Long-form auto-scroll rows) · templates · models grid · dark cta-band cinematic stage · capabilities — with GSAP ScrollTrigger choreography.
2026-06-16 — Creator page overhaul — ALL 6 SECTIONS COMPLETE (continues the prior entry). Built + wired the 4 remaining sections after Hero+Formats: CreatorTemplates (8 icon-tiles, hover arrow-reveal), CreatorModels (6 engine cards, gradient top-edge + tier badge), CreatorCaps (4-up "what it makes" strip), CreatorStage (dark --bai-cta-band cinematic FINALE — 16:9 featured reel [ceo-married-rival poster, inert play] + 5-cell filmstrip, framer useScroll/useTransform PARALLAX on the reel). Final order: Hero → Formats → Templates → Models → Caps → Stage→Footer (dark band→footer mirrors the live-demo-v2 closing pattern). MOTION-LIBRARY SPLIT (decision): raw GSAP only for the seamless marquee (Formats); framer-motion (codebase standard, SSR-safe) for ALL reveals + the stage parallax — did NOT introduce GSAP ScrollTrigger (avoids its SSR-registration pitfalls; framer covers scroll-driven motion here). All sections on --bai-* tokens (cta-band finale, gradient-wash icon chips, mkt-slate heads, shadow-hairline cards); tint placeholders Tier-3. Verified :3001 (clean reload): hero+24 cards+8 tpl+6 mdl+4 caps+reel+5 strip+footer all render, no Next overlay, parallax matrix live on reel, marquee still animating (no regression), on-dark eyebrow override correct, tpl arrow hidden until hover; `tsc --noEmit` EXIT 0. DIAGNOSED the 6 console "NotFoundErrorBoundary recreate" errors as STALE Fast-Refresh HMR artifacts (warn-level showed the 1:1 paired "[Fast Refresh] full reload due to unrecoverable error" — from mid-build module gaps), NOT a live hydration bug: clean browser reload kept the count at 6 (not 12) + DOM came back complete. — Why: finish the award-winning creative-tool landing on the light DS; framer-over-ScrollTrigger for SSR safety; honest error triage (the recurring "is anything dead/wrong" discipline). NEXT (parked): CreatorForm.tsx still ORPHANED → cleanup pass; deprecated seo/hero/home files + token cleanup still parked.
2026-06-16 — Creator page ASSETS FILLED (designer: "fill the assets, scrape from somewhere?"). RE-FLAGGED + held the line on the IP risk: declined scraping Higgsfield/Nim/invideo (their branded copyrighted output → public Vercel deploy) — same call as the prior session, designer agreed. Routed to ROYALTY-FREE stock instead (designer chose). Source = Pexels API (license: free, commercial OK, no attribution req → clean for a public deploy); designer generated a free key (can't create accounts myself). Pulled via PowerShell + Pexels videos API: 12 theme-matched POSTERS (one per gallery card, portrait for Shorts / landscape for Long-form, sized w=800/1280, 28–216KB) + 4 SD CLIPS (kai-bride, ceo-married-rival, steam-engine for gallery hover-play; reel-feature 960×540 landscape for the 16:9 stage reel — ceo is portrait so a dedicated landscape clip was grabbed for the wide frame). All in public/videos (17 files, ~20MB). WIRING: creator-data — every card got a `video` slug (poster, layered OVER its tint fallback so never blank) + `clip:true` on the 3 with mp4s. CreatorCard → client; clip tiles play muted/loop on hover (preload="none" = lazy, loads only on first hover), static poster otherwise. CreatorStage reel → click-to-play <video> (poster+mp4, hides play-btn/foot + fades scrim while playing). creator.css: .cr-card-media + .cr-reel-media (absolute cover, z:0 under the z:1 scrim / z:2 chrome). VERIFIED: tsc EXIT 0; all 17 assets serve HTTP 200 (direct dev-server probe); SSR HTML carries reel video + 6 hover videos (3×2 marquee) + 13 posters + 4 clip refs, hero/formats/templates/reel content all present, no error overlay (the "is404" substring = Next's embedded not-found boundary template, present on every dev page — confirmed false positive via notFoundIsBoundary). INFRA INCIDENT (not my code): mid-verify the dev server started returning /ai-video-creator 404 w/ 90–246s latencies — root cause in the log = `.next/cache/webpack PackFileCacheStrategy ENOENT rename 7.pack.gz_→7.pack.gz` = the MULTI-SESSION .next contention we diagnosed earlier (other blueai session writing the same cache) corrupting route resolution → /_not-found fallback. FIX: preview_stop → cleared .next/cache (no lock) → preview_start blueai-3001 → route 200 again. Also: preview_eval reliably wedges on this page now (WebGL hero + video els strain the headless renderer; an in-page fetch HEAD tipped it) → all verification done via direct HTTP probes + SSR-HTML greps, NOT the renderer (designer views live in their own browser). — Why: fill the empty gallery/reel with real licensed media without the copyright exposure of scraping branded AI-video sites; footage present (hover-play tiles + click-play reel) on top of real photographic posters.
2026-06-16 — Creator overhaul MOVED to a new route (designer: "wanted this on a NEW page, revert /ai-video-creator"). The overhaul had been done IN-PLACE on /ai-video-creator (the original agent-page). Fix, clean + git-safe (blueai IS a git repo): (1) created NEW route src/app/ai-video-creator-v2/page.tsx with the bespoke overhaul content (same shared creator/* components + creator.css + assets — nothing duplicated, just re-pointed; distinct metadata title "Studio concept (v2)"); (2) reverted the original via `git checkout HEAD -- src/app/ai-video-creator/page.tsx` (restores the AgentShell + CreatorForm + VideoCard version exactly — no reconstruction risk); (3) indexed the new page on the root index (PAGES array, after live-demo-v2, label "AI Video Creator — Studio (v2)"). Route name = ai-video-creator-v2 (matches the live-demo-v2 convention; user said "creator-2" → flagged renamable). SIDE EFFECTS: CreatorForm.tsx is UN-orphaned now (the reverted original imports it again → the earlier "orphaned, flag for cleanup" note is MOOT). The original page's own VideoCards (kai-bride/ceo/steam-engine) now show real media too (I'd downloaded those posters/clips this session) — a bonus, not a problem. ALSO fixed the filmstrip bug from the prior turn (had edited STRIP const to a string array but not its .map → tsc error `'tint' does not exist on string`; map now reads the slug directly) → 5/5 strip cells real posters. VERIFIED: tsc EXIT 0; HTTP probes — /ai-video-creator 200 (hasAgentMade=true, hasOverhaul=false = clean revert), /ai-video-creator-v2 200 (hero + 1 reel video + 6 hover tiles + 5 strip posters), / 200 (lists v2 + label). INFRA: my port-3001 dev server died AGAIN mid-task (preview_list showed only the other session's port-3000 server, cwd N:\Antigravity Main) — restarted blueai-3001; recurring multi-session .next contention, env issue not code. — Why: designer wanted the original creator page preserved + the overhaul as a separate explorable page; git revert = exact restore, new route = additive, zero loss.
2026-06-16 — v2 creator page — HIGGSFIELD-GAP CLOSE (designer shared the Higgsfield homepage, asked "what's missing in v2"; I gave a structural gap-read → designer "sure" to building the top 3). Reference read: Higgsfield = a deep stack of NAMED, visually-PROVEN feature showcases + bold accent banners for rhythm; v2 was a lean single-pass landing. Built 3 additions on /ai-video-creator-v2 ONLY (light DS, fresh Pexels stock so no section repeats media, no scraping): (1) ENGINES "shown not told" — CreatorModels rebuilt from 6 TEXT cards → 6 MEDIA-topped cards, each a real example still (pulled 6 fresh distinct landscape posters eng-flagship/fast/4k/twin/voice/caption: film set / motion-blur / drone landscape / presenter / podcast mic / phone-shoot) + tier badge on the media; MODELS data got a `poster` slug each. (2) CreatorBanner — one brand iris→cyan gradient flagship band ("One prompt in. A finished video out." + inert CTA) for banded rhythm, placed after Formats. (3) CreatorDrama — micro-drama SPOTLIGHT for our differentiator (2 large 9:16 hover-play tiles = the kai-bride + ceo clips, "cast/look/voice consistent shot to shot", play-glyph fades on hover→plays). New order: Hero→Formats→Banner→Templates→Engines→Drama→Caps→Stage→Footer. Intentional NON-gaps (told designer): image/audio gen (BlueAI = video only), pricing/credits chrome (design-only), going dark/neon (light+cinematic locked — transferred the RHYTHM pattern not the theme). VERIFIED: tsc EXIT 0; HTTP probe 200 (banner present, 6 mdl-media + 6 eng posters, 2 drama tiles+clips, section order correct); screenshots — banner gradient + CTA, 6 engine media cards w/ badges, 2 drama tiles w/ play glyphs, index lists "AI Video Creator — Studio (v2)". INFRA: port-3001 server died AGAIN twice this task (multi-session .next contention) → restarted blueai-3001 each time; renderer needs explicit location.href nav after a restart (url-param screenshot raced once, showed the index). NOTE: creator.css growing but scoped-stylesheet = pragmatic 300-line exception. — Why: bring v2 to the reference's showcase density without copying Higgsfield's content/theme; close the biggest "told not shown" gap (engines) + add rhythm + spotlight the micro-drama moat.
2026-06-16 — INFRA FIX (distDir isolation, designer-approved) — the recurring port-3001 dev-server deaths = two `next dev` on the same project sharing `.next/cache` (webpack PackFile rename race → route 404s). FIX: next.config.js `distDir: process.env.BLUEAI_DIST_DIR || '.next'` (default untouched → other session unaffected) + `.claude/launch.json` blueai-3001 gets `"env": { "BLUEAI_DIST_DIR": ".next-3001" }`. VERIFIED the launch-config `env` IS honored: after restart `.next-3001` was created (my server builds there; default `.next` stays the other session's). No more shared-cache race.
2026-06-16 — v2 video sourcing (designer: "use those 3 videos we had on v1"). HONEST FINDING: the real v1 demo videos do NOT exist on this machine — globbed the whole project + Downloads; the only kai-bride/ceo-married-rival/steam-engine files are the PEXELS STAND-INS I downloaded this session (public/videos was empty at session start → the v1 VideoCards pointed at never-checked-in files, hence blank). Wired all 3 slugs into v2's video moments: kai-bride + ceo = micro-drama spotlight (hover-play) + gallery hover; steam-engine = gallery + now the STAGE REEL (swapped reel off the generic `reel-feature` Pexels businessman clip → steam-engine, the only LANDSCAPE demo = native 16:9 fit, no crop; reel caption updated to the explainer; head sub de-"micro-drama"'d; filmstrip steam-engine→rome-fell to avoid reel/strip adjacency dup). DELETED reel-feature.jpg/.mp4 (orphaned → no dead assets; "use the 3" = no 4th stock clip). AUTO-REPLACE mechanism told to designer: drop real files at public/videos/<slug>.mp4(+.jpg) → replaces everywhere by filename, zero code change. VERIFIED: tsc EXIT 0; HTTP 200 (reel=steam-engine + caption, reel-feature gone, strip=rome-fell); screenshot — reel shows steam engine #117 landscape native-fit + 5 distinct filmstrip stills. — Why: honor "use our 3 demos" (all 3 now featured, no external reel clip), be honest the real files aren't present (stand-ins until designer drops real ones), keep the server stable.
2026-06-16 — Hero prompt placeholder → TYPEWRITER (designer: the static instructional placeholder was weak; wanted typed examples that "come and go"). Replaced the long static placeholder with a typewriter that types→holds→deletes→cycles 6 example prompts (designer's ideas + format spread: faceless "losing your job", "Appy Fizz ad for Instagram", mafia micro-drama, space-facts short, black-hole explainer, newsletter→TikTok). IMPL: useTypewriter hook (recursive setTimeout: 52ms type / 26ms delete / 1900ms hold / 420ms gap) in CreatorHero; text renders in a GHOST layer (.cr-prompt-ghost, absolute over the empty readOnly textarea) so it gets a real BLINKING CARET (.cr-caret, --bai-mkt-blue, steps blink). Reduced-motion → first example static, caret animation off. Data: CREATOR_HERO.placeholder removed → CREATOR_HERO.examples[] (SSOT). VERIFIED: tsc EXIT 0; DOM poll showed the ghost advancing ("Create a f"→"Create a facele") + caret present = typewriter live. NOTE: preview_screenshot now RELIABLY wedges on this page — the continuous typewriter timer + WebGL hero never let the headless renderer reach document_idle (capture hangs 30s). All verification via DOM evals; designer views live. — Why: a creative-tool hero should feel alive; rotating real prompts teach what to type far better than one static instruction.
2026-06-16 — Formats gallery — BIGGER + minimal play affordance (designer: section bigger; drop the big centered play button; play on hover with a minimal corner triangle). Cards: cr-card--v 300→380px, cr-card--h 188→232px (mobile 240→300 / 150→184); marquee gaps 20→24 (rows) + 16→18 (set). Affordance: removed the centered .cr-card-play from the GALLERY cards (CreatorCard) → replaced with .cr-card-cue (bare white play triangle, top-right, drop-shadow, scales 1.14 on hover) — "minimal triangle only" per designer; hover-to-play unchanged. NOTE: .cr-card-play base CSS KEPT (the drama-spotlight tiles still use that class); only the dead `.cr-card:hover .cr-card-play` gallery rule removed. The cue is on ALL 12 tiles (video indicator) but only the 3 clip tiles actually play on hover — flagged to designer (sourcing 9 more clips would make all play). VERIFIED: tsc EXIT 0; DOM — 24 cues, 0 centered-play in gallery, vCardH=380, hCardH=232, hoverVideos=6. — Why: lighter, more modern gallery (hover-plays like the refs); minimal corner cue beats the heavy centered button that over-promised playback on the 9 static tiles.
2026-06-16 — Formats gallery — removed the SHORT/LONG-FORM badge (designer: repeating on every tile). Dropped the .cr-card-badge span from CreatorCard + its now-dead CSS rule. Format is still conveyed by row grouping (portrait vs landscape) + the meta line ("Micro-drama · 1:02"). Tiles now = poster + minimal corner play triangle + title/meta. VERIFIED: tsc EXIT 0; DOM badges=0, cards/cues/foots all 24. — Why: redundant always-on label (taste 30 — retire redundant signals).
2026-06-16 — Hero controls made INTERACTIVE (designer: "each item should do something on click; right now nothing happens"). Design-only (no real gen) but believable: (1) PILLS — each HERO_PILLS item got a `prompt`; click drops it into the field (sets `picked`), highlights the pill (.cr-pill.is-active, gradient-wash), and STOPS the typewriter (useTypewriter gained an `enabled` flag = picked===null). (2) GENERATE (→) — click sets busy → swaps the Arrow for a CSS spinner (.cr-spinner) ~1.3s → smooth-scrolls to the .cr-formats gallery (the "results" reveal); captures the current typed line as the prompt if no pill was picked. (3) MODEL <select> = native, already works. VERIFIED via simulated clicks: Micro-drama pill → ghost="A 3-part mafia micro-drama about a betrayal" + that pill is-active (activeCount 1) + typewriter stopped; Generate → page scrolled (scrollY 868, formats at viewport top). tsc EXIT 0. NOTE: React re-render is async — synchronous read right after .click() shows stale state; verified by reading in a SEPARATE eval call. Also clicking too soon after reload = no-op (hydration race). — Why: a prototype's controls must respond or it reads as broken; pills-fill-prompt + generate-reveals-gallery are the believable design-only behaviors.
2026-06-16 — Hero prompt made EDITABLE (designer: "I can't click/type inside the main field, why?"). The field was readOnly (so the typewriter ghost could overlay it) — wrong for the MAIN input. Fix: textarea is now controlled+editable (value/onChange/onFocus/onBlur, readOnly removed); the typewriter is now an animated PLACEHOLDER shown ONLY when `value===''  && !focused` (clicking in clears it → real native caret → type). Pills setValue (still editable after); is-active when value===prompt. Generate unchanged (spinner→scroll, uses the field value). VERIFIED: readOnly=false; simulated typing stuck (value="my own video idea…") + ghost removed; tsc EXIT 0. — Why: a prompt field you can't type in reads as broken; typewriter belongs as a placeholder, not a lock.
2026-06-16 — Creator v2 FREEMIUM FLOW (designer: Generate → Google login popup like the live-demo widget → "1 free generation" → on login, complete generation + add a "user library of generated content" section above "Made with the Video Creator"). ALL MOCK (design-only — NO real Google OAuth, NO real generation; renders = our sample clips). Built: (1) CreatorStudio.tsx — React context provider (prompt/model/loggedIn/generating/generations/modal + requestGenerate/login/closeModal); wraps <main> in page.tsx, renders the modal. requestGenerate: signed-out→login modal; free-used→limit modal; else runGenerate (2.2s → prepend a Gen{title=prompt||'A faceless short', model, video=rotating kai/ceo/steam clip}, scroll to library). (2) CreatorLoginModal.tsx — mock "Continue with Google" (matches widget's .gbtn + conic-gradient .gmark + "Mock login… no real account" note) in 'login' mode; 'limit' mode = "That was your free generation" + See plans/Maybe later. framer AnimatePresence. (3) CreatorLibrary.tsx — "Your generations" section, renders ONLY after first generate (null before); loading card while generating, then auto-play (muted/loop) 9:16 result cards newest-first + "New" badge. Inserted BEFORE CreatorFormats. (4) CreatorHero now consumes the context (prompt/model controlled; Generate→requestGenerate; busy=generating). CSS: modal (scrim+blur, card, brand orb, gbtn/gmark, note), library grid (4→3→2 cols), loading card (dashed + ink spinner). VERIFIED via simulated clicks: Generate→login modal ("Your first video is on us"); Continue with Google→modal gone + library appears + 1 gen (kai-bride.mp4, autoplay, "New", "Just now · BlueAI v4"); library-before-formats confirmed; 2nd Generate→limit modal, no extra gen. tsc EXIT 0, page 200. — Why: a believable freemium demo (login wall → free render → personal library) — the standard creative-tool conversion hook, fully mocked for the handoff. SECURITY: explicitly NOT real auth (no credentials, no account) — pure visual prototype.
2026-06-16 — Removed the "limit"/upgrade modal (designer: "this is not needed"). The freemium flow now = login wall ONCE (signed-out Generate → Google login modal), then generate freely (no per-gen cap, no "That was your free generation / See plans" wall). CreatorStudio: dropped `freeUsed` + the `modal:'limit'` branch (modal type now 'login'|null); requestGenerate = signed-out→login, else→runGenerate. CreatorLoginModal: removed the limit JSX (renders the login content directly). creator.css: removed the now-dead .cr-gbtn--primary, .cr-modal-later, .cr-modal-orb--emoji. Login modal copy ("1 free generation") kept as the sign-in hook. VERIFIED (clean paced run): signed-out Generate→login modal; sign in→gen1; 2nd Generate→NO modal, gen2 added (gens=2). tsc EXIT 0. (A rapid-scripted-click run showed a transient login-modal blip = synthetic race inside the 2.2s timer, not reproducible at human pace.) — Why: the upgrade wall nagged; designer wants a clean sign-in-once-then-create demo.
2026-06-16 — Library section made MINIMAL (designer ref: Google Flow's understated "Creations will appear here" strip; our version had a big centered hero headline + one oversized lonely card in whitespace). Redesign: dropped the marketing cr-section/cr-sechead treatment → compact left-aligned header (.cr-lib-head: 19px .cr-lib-title + count chip on the left, "Demo · sample renders" note on the right) + a compact left-aligned auto-fill grid (minmax(min(150px,100%),168px), justify-content:start) that fills left→right as you generate (no lonely giant card). Cards shrank 9:16 ~168px, foot title -webkit-line-clamp:2, tighter padding. Removed the now-redundant cr-lib-grid col overrides in the @media blocks (auto-fill handles responsive). VERIFIED: tsc EXIT 0; DOM — title 19px, card 168px, head space-between, no .cr-sechead in library; screenshot shows "Your generations 1" minimal header. — Why: the library is the user's own content (app-like), not a marketing pitch — it should be quiet + dense, not a hero.
2026-06-16 — Library got a CONTAINER (designer: "can this section have a white-10 container or something" — the bare card floated on the white page). Wrapped the head+grid in .cr-lib-panel: var(--bai-surface) #F7FAFF tinted off-white + 1px divider border + 22px radius + shadow-hairline (the light-DS read of "white 10%" = a faint surface panel, since a literal white panel is invisible on the white page). Also gave result cards a brand-duotone fallback bg (style FALLBACK) so a render tile is never blank if the clip is slow/stale (the designer's screenshot showed a blank card — same robustness pattern as the gallery). VERIFIED: tsc EXIT 0; panel bg rgb(247,250,255) + border + 22px radius; screenshot shows the panel + the card now playing the real clip. — Why: the library is the user's own content area — a contained panel sets it apart from the marketing page; tile fallback = never-blank.
2026-06-16 — Generation OPEN + SHARE/PUBLISH upsell (designer: click a generation → popup with promote/share to Instagram/TikTok/YouTube → clicking a platform → popup "create & publish with our creator agent + virtual device with BlueAI" + Download BlueAI CTA). Built CreatorShareModal.tsx (two views in one modal): DETAIL = wide cr-genmodal (video left w/ controls + title/meta + "Promote to" + 3 platform buttons IG/TikTok/YouTube w/ brand-colored glyphs) → PUBLISH = centered cr-modal (upload-orb + "Publish to {platform} with BlueAI" + body about the creator agent on a virtual device + .cr-dl-cta "Download BlueAI" → real DOWNLOAD_URL from site-data, new tab + "← Back"). CreatorStudio: added openGen/shareTo state + openGeneration/closeGeneration/share/backToDetail; renders <CreatorShareModal/>. CreatorLibrary cards now clickable (role=button, Enter/Space, hover lift) → openGeneration. CSS: cr-genmodal/gen-detail/gen-video/gen-info/gen-platforms/cr-plat(+--ig/tt/yt brand hovers)/cr-dl-cta/cr-modal-back + mobile stack. BUG FOUND + FIXED (important): the modals used framer **AnimatePresence**, which ORPHANED the scrim DOM node when presence toggled during its 0.32s animation (login modal lingered after sign-in — even the X wouldn't remove it; modal state WAS null but the dead scrim stayed → 2 stacked scrims). Fix: dropped AnimatePresence from BOTH CreatorLoginModal + CreatorShareModal → conditional `if(!x) return null` + plain motion.div (enter anim only, instant unmount). VERIFIED under FAST clicks: generate→login modal; sign-in→scrims 0 (no orphan); card→1 modal detail (IG/TikTok/YouTube); Instagram→1 modal "Publish to Instagram with BlueAI" + Download BlueAI (bluestacks URL). tsc EXIT 0. (preview_screenshot wedges on this page — WebGL hero never idles; all verified via DOM.) — Why: the share→publish→download funnel is the conversion goal (mock); AnimatePresence-under-fast-toggle is a real orphan trap — conditional render is the robust pattern here.
2026-06-16 — Banner illustration (designer: add the robot-creator image to the "One prompt in" gradient banner, right side). Designer-provided ChatGPT-generated PNG (transparent bg, robot at a creator desk + screen/mic/camera/YT+IG logos) → copied from Downloads to public/creator-agent.png (~1.9MB). CreatorBanner: added <div className="cr-band-img"> (CSS background, not <img> → degrades to empty if the file is absent, no broken icon). CSS: .cr-band-img absolute right (clamp 8-44px), full band height, width 44%, url(/creator-agent.png) right bottom/contain; .cr-band-inner narrowed 720→600 + z-index 2 (text always above); hidden ≤920px (text-only on tablet/mobile, inner max-width:none). VERIFIED: img serves HTTP 200; screenshot — robot on the right, transparent bg blends into the gradient, text left, balanced. NOTE: ~1.9MB PNG is heavy for web — offered to optimize (resize/compress). — Why: the banner's right half was empty gradient; the creator-agent illustration fills it + reinforces the "agent makes your video" story.
2026-06-16 — Templates made interactive (designer: "what should happen on their clicks"). Each TEMPLATES item got a tailored `prompt`; CreatorTemplates now useStudio().setPrompt → on click drops that starter prompt into the hero + smooth-scrolls to .cr-hero (ready to Generate). Mirrors the pill behavior; fulfils the "Start in one click" eyebrow. VERIFIED: tsc EXIT 0; clicking Documentary → hero prompt = "A long-form documentary on the rise and fall of an empire", scrolled to top. — Why: the preset grid was inert; a template should seed the prompt and hand off to the generate flow.
2026-06-16 — Removed the "Powered by the BlueAI video stack" (Engines/Models) section (designer: "not doable right now, remove it"). Full clean removal: page.tsx (import + render), DELETED CreatorModels.tsx, removed the MODELS export from creator-data (kept HERO_MODELS — that's the hero picker, separate), removed the Models CSS block + .cr-mdl-grid responsive lines + .cr-mdl from the reduced-motion list, deleted the 6 public/videos/eng-*.jpg posters. VERIFIED: precise grep (\bMODELS\b / cr-mdl / CreatorModels / eng-*) = ZERO dangling refs; tsc EXIT 0; page 200, section gone, Templates + Micro-drama intact. New order: Hero→Library→Formats→Banner→Templates→Drama→Caps→Stage. — Why: designer scoped it out; no-dead-code = pull the component/data/CSS/assets, not just the render.
2026-06-16 — Removed the Capabilities ("What it makes") + Stage ("See it in motion" dark reel) sections (designer: "these 2 don't make sense right now"). Full clean removal: page.tsx (imports + renders), DELETED CreatorCaps.tsx + CreatorStage.tsx, removed CAPS from creator-data, removed the Caps CSS + the whole dark Stage/reel/filmstrip CSS block + .cr-caps-grid responsive lines + .cr-strip mobile hide + .cr-reel-play from reduced-motion. NO assets deleted — the Stage reel/filmstrip reused steam-engine + gallery posters (silk-road/rome-fell/black-holes/roman-navy/chess-mind) which Formats/Library still use. KEPT shared: .cr-spinner/cr-spin (hero Generate + library loading), .cr-card-play (drama tiles), .cr-sechead (formats/templates/drama). VERIFIED: grep for dead tokens = only UNRELATED hits (original /ai-video-creator's own local CAPS; deprecated hero-stage's separate .v-stage .cr-stage; a comment) — zero in v2; tsc EXIT 0; page 200, both sections gone, Templates/Drama/Banner intact. New flow: Hero→Library→Formats→Banner→Templates→Drama→Footer (ends on the micro-drama spotlight → marketing footer). — Why: designer scoped them out; no-dead-code = pull component+data+CSS, keep shared utilities + still-used assets.
2026-06-16 — Brought v1's How-it-works + SEO text + FAQ into v2 (designer: add them after the micro-drama section). Built CreatorInfo.tsx — pulls the CONTENT from agents-data CREATOR (hiwHeading + 4 hiw steps, 7 seoBlocks, 9 faq) but restyled NATIVELY in the creator DS (cr-sechead/cr-eyebrow-sm/cr-h2 + new cr-hiw-*/cr-seo-*/cr-faq-* in creator.css) — NOT a transplant of the agent DS (site.css/agent.css), so v2 stays one coherent design. 3 sections: How it works (numbered gradient step cards) → "AI video creation, explained" (2-col SEO h3+p blocks) → FAQ (creator-styled accordion, grid-rows 0fr→1fr crawlable, same logic as SiteFaq). Wired after CreatorDrama. VERIFIED: tsc EXIT 0; SSR 200, hiw "How does it make AI videos?" + 4 steps, seo "AI video creation, explained" + 7 blocks, FAQ + 9 items, order after micro-drama; accordion toggles (clicked item 2 → opens, item 0 closes). New flow: Hero→Library→Formats→Banner→Templates→Drama→How-it-works→SEO→FAQ→Footer (FAQ→footer = natural close, fixes the earlier abrupt-ending flag). — Why: SEO crawlable text + standard how-it-works/FAQ belong on the marketing page; reuse v1's content, re-skin to v2 (don't import a 2nd DS).
2026-06-16 — Micro-drama section felt empty (designer: "incomplete or empty" — 2 portrait tiles floating centered in whitespace). Fix = give it structure + fill the width: rebuilt CreatorDrama from centered-head+row → TWO-COLUMN (.cr-drama-layout grid 0.92/1.08): LEFT = eyebrow/h2/sub + 3 checkmark feature bullets (.cr-drama-points: consistent cast · scripted series · cel-shaded/cinematic) + a "Make a micro-drama →" gradient CTA (.cr-drama-cta → setPrompt the micro-drama starter + scroll to hero, like templates); RIGHT = the 2 hover-play tiles (now flex:1 max-300 in .cr-drama-tiles). Stacks to 1-col ≤920. VERIFIED: tsc EXIT 0; DOM twoColumns=true, copy + 3 points + CTA + 2 tiles; screenshot — balanced, copy left / tiles right, no more empty center. — Why: a 2-tile row centered reads as unfinished; pairing it with a value-prop column + bullets + CTA fills the space and adds substance + an action.
2026-06-16 — Download BlueAI → 3-step installer guide (designer: clicking Download flips the popup to install steps). Added a 3rd share-modal view: CreatorStudio `installing` state + `startInstall()` (reset in close/share/backToDetail); the "Download BlueAI" <a> keeps href={DOWNLOAD_URL} target=_blank (real download in a new tab) + onClick={startInstall} (current tab flips to the steps). Installer view (.cr-gen-install): "Finish setup in BlueAI" + "Your installer is downloading — Didn't start? Download again" (link to DOWNLOAD_URL) + 3 numbered steps [1 Install & open BlueAI → sign in · 2 Sign in to the Play Store in the BlueStacks virtual device · 3 Install {shareTo}] + Done (close). Steps use the chosen platform ({shareTo}). CSS: cr-install-steps/cr-install-n/cr-install-link. VERIFIED: tsc EXIT 0; ran the flow — installer view shows "Finish setup in BlueAI", 3 steps = [Install & open BlueAI, Sign in to the Play Store, Install Instagram], download-again link + Done. (Server died mid-test → restarted blueai-3001; preview_eval `.cr-dl-cta.click()` threw on the target=_blank nav — headless artifact, not a bug.) — Why: the publish CTA should hand off to a real getting-started path (install → Play Store on the virtual device → install the target app) so BlueAI's agent can post.
2026-06-16 — Download BlueAI = button, not external link (designer: design-handoff, no real download needed; the cloud.bluestacks.com link was BLOCKED by the preview "only localhost URLs" + the blocked nav stopped the popup state from changing). Fix: both "Download BlueAI" (publish view) and "Didn't start? Download again" (installer view) changed from <a href={DOWNLOAD_URL} target=_blank> → plain <button onClick={startInstall}>. Removed the now-unused DOWNLOAD_URL import. CSS: .cr-dl-cta += cursor:pointer; .cr-install-link += button reset (background/border/padding/font:inherit). VERIFIED: tsc EXIT 0; full flow click-through — Download click cleanly flips to installer view ("Finish setup in BlueAI", 3 steps incl. "Install Instagram"), no block toast, Download-again is a BUTTON. — Why: design-only handoff demonstrates the flow with a state transition; the dev team wires the real download trigger. Preview blocks non-localhost URLs, so external-link CTAs can't be exercised in preview anyway.
2026-06-16 — Installer-steps view made "artsy" (designer: "this can be more artsy"). Added to the Finish-setup-in-BlueAI modal: (1) PULSING download orb (.cr-modal-orb--pulse::after = gradient halo, cr-pulse scale 1→1.7 fade); (2) indeterminate PROGRESS BAR (.cr-install-bar + sliding gradient span, cr-load) under "Your installer is downloading" so it feels live; (3) "Didn't start? Download again" moved to its own .cr-install-hint line; (4) STEP TIMELINE — .cr-install-steps::before = 2px vertical gradient connector linking the numbered badges (badges position:relative z-index:1 + soft glow shadow); (5) steps STAGGER in (framer motion.li, delay 0.15+i*0.1). installSteps now data-mapped (uses {shareTo} for step 3). Reduced-motion → pulse + bar animations off. VERIFIED: tsc EXIT 0; DOM — pulseOrb + progressBar(animating cr-load) + connector ::before (2px) + hint + step3="Install YouTube" (platform flows through). preview_screenshot WEDGES (more continuous motion now) → verified via DOM. — Why: a setup modal that pulses + shows a live progress bar + a connected timeline reads as crafted, not a flat list.
2026-06-16 — Installer "Done" link → proper CTA (designer: "Done can be better CTA than link"). Was a blue text link (.cr-modal-back override); now the gradient pill .cr-dl-cta (matches Download BlueAI), relabeled "Got it". Replaced the dead .cr-gen-install .cr-modal-back override with .cr-gen-install .cr-dl-cta { margin-top: 22px }. Base .cr-modal-back kept (publish view's "Back to my video" still uses it). VERIFIED: tsc EXIT 0, page 200 (reuses the proven .cr-dl-cta). — Why: a closing acknowledgment should read as a clear button, not a weak link.
2026-06-16 — Banner "Describe your video" CTA wired (was inert). onClick → smooth-scroll to .cr-hero + focus the .cr-prompt-input (focus({preventScroll:true}) so it doesn't fight the smooth scroll); focusing hides the typewriter ghost → live cursor ready to type. VERIFIED: tsc EXIT 0; click → document.activeElement = cr-prompt-input, ghost gone. — Why: the flagship banner's CTA should drop you at the prompt to start, like the pills/templates/drama CTA.
2026-06-16 — Sample gallery → hover-blink + click-to-copy-prompt popup (designer: the format gallery is a sample library; on hover the play icon should blink slowly = playing; click opens a popup like the my-generations one but the action is "Copy the prompt"). Built: (1) each SHORTS/LONGFORM card got a `prompt` (the example that made it); Card type now requires prompt. (2) CreatorCard is clickable (role=button, Enter/Space) → openSampleModal(card). (3) CreatorStudio: Sample type + openSample state + openSampleModal/closeSample + renders CreatorSampleModal. (4) CreatorSampleModal — reuses cr-genmodal layout (clip plays w/ controls, else poster) + title/meta + the prompt in a left-accent-bar quote (.cr-sample-prompt) + "Copy the prompt" CTA (cr-dl-cta) → robust copy: try navigator.clipboard.writeText in try/catch + unconditional setCopied(true) (so feedback shows even if clipboard is blocked in an iframe) → flips to "Copied ✓" 1.8s. (5) HOVER BLINK: .cr-card:hover .cr-card-cue runs cr-blink-slow (opacity 1↔.28, 1.6s), reduced-motion off. VERIFIED: tsc EXIT 0; click Space-Facts/Steam-Engine → popup w/ prompt + copy btn; copy → "Copied" (caught with a 250ms in-page wait — the 1.8s window kept reverting before the eval round-trip read). NOTE: clipboard fully works on real localhost/https (secure context + user gesture); preview iframe may lack it → robust handler still shows feedback. UNPUSHED (this + the banner "Describe your video" CTA are local, not in the last push fc919e5). — Why: the sample wall should feel like browsable playing videos + let people lift the prompt that made one.
2026-06-16 — Sample popup ASPECT-ADAPTIVE (designer: landscape clips were opening crushed into the portrait frame; suggested content-on-bottom for landscape). Fixed: Sample type gained `ratio`; CreatorCard passes its ratio into openSampleModal({...card, ratio}); CreatorSampleModal now branches — portrait (9:16) = side layout (cr-gen-detail, video left/info right, unchanged); landscape (16:9) or square (1:1) = STACKED (cr-genmodal--stack + cr-gen-stack: cr-gen-media-wide video on top at native aspectRatio, info below); X turns white over the top video. info block extracted + reused across both. VERIFIED: tsc EXIT 0; Chess (longform) → isStack + media aspect "16/9" + no side layout; "Bought His Bride" (short) → side layout + cr-gen-video + not-stacked. SQUARE decision (designer asked): NOT adding 1:1 sample clips (keeps the clean Shorts/Long-form 2-row gallery; square is secondary for a video tool), BUT the hero aspect-ratio picker (pending build) will offer 1:1 so the product supports it; library tiles would reflect each gen's chosen ratio in prod (mixed grid) — demo gens stay 9:16. — Why: a wide clip must show wide; the modal should serve the content's shape, not force one frame.
2026-06-16 — Hero aspect-ratio picker added (designer chose "Model + Aspect ratio"; the build had been skipped when they pivoted to the modal fix — they re-checked "did we do anything here?"). Added HERO_ASPECTS ['Auto','9:16','1:1','16:9']; CreatorHero now wraps the model pill + a new aspect pill (frame icon + native select) in .cr-prompt-controls (flex, wrap), Generate stays right. aspect = local useState (design-only; nothing consumes it yet — gens stay 9:16). KEPT the model picker (engine/quality is a real control; only the marketing section was cut). Did NOT add style/tools (designer chose the minimal 2-control set). Includes 1:1 so the product offers square even though we don't ship square samples. VERIFIED: tsc EXIT 0; DOM — controls group, 2 pickers, model "BlueAI v4", aspect options [Auto,9:16,1:1,16:9] default Auto. UNPUSHED (with the sample popup, banner CTA, modal-aspect fix — all local since fc919e5). — Why: aspect ratio is the highest-value creative control (sets the platform); kept inline-minimal to avoid clutter.
2026-06-16 — Native <select> dropdowns → custom themed dropdown (designer: the open native dropdown looked off-theme — correct, the native option list can't be CSS-styled). Built CreatorSelect.tsx (reusable): pill trigger (reuses .cr-model + chevron that rotates) + a rounded white surface menu (.cr-select-menu, divider border, overlay shadow) with .cr-select-opt rows (hover = surface bg, selected = mkt-blue + checkmark); closes on outside-mousedown + Escape; role=listbox/option a11y. Replaced BOTH hero pickers (model + aspect) — no native <select> left in the prompt bar. Removed the dead .cr-model-sel rule. VERIFIED: tsc EXIT 0; DOM — 2 themed triggers, no native select, open → menu (white bg, [Auto,9:16,1:1,16:9], is-sel + check), click 16:9 → trigger shows "16:9" + menu closed. UNPUSHED (batch since fc919e5). — Why: native selects break the DS on open; a custom popover matches the frosted-pill aesthetic + the reference (rows + checkmark).
2026-06-16 — Aspect dropdown upgraded to match the reference (designer: "is ours better?" — honest answer: no, the ref had shape-icons + names + a header; ours was plain rows). Enriched: HERO_ASPECTS → objects {value,label,hint} (Auto / Story 9:16 / Square 1:1 / Widescreen 16:9); CreatorSelect now takes SelectOption[] {value,label?,hint?,icon?} + optional `heading`, renders rows as [shape icon · name · ratio hint] + check, and the trigger shows the SELECTED option's icon + value. aspectIcon(v) in CreatorHero returns a frame glyph proportioned to the ratio (square/tall/wide/auto). Model picker reuses the same component (plain rows, Sparkle icon, no header). CSS: cr-select-head, cr-opt-left/ic/hint, is-sel icon+hint→mkt-blue, menu min-width 200. KEPT light theme + video set (skipped the ref's image-only 3:4 / 4:3). VERIFIED: tsc EXIT 0; DOM — heading "Choose aspect ratio" + 4 rows each w/ icon, names Auto/Story/Square/Widescreen, hints ''/9:16/1:1/16:9. UNPUSHED (batch since fc919e5). — Why: a creative-tool ratio picker reads far better with proportioned shape glyphs + named formats than bare ratios.
2026-06-16 — Dropdown z-index/stacking bug (designer screenshot: the format pills row painted OVER the open aspect menu). Root cause: .cr-prompt creates a stacking context (backdrop-filter: blur), so the menu (z-index 30 inside it) couldn't escape above .cr-pills, a LATER sibling flex-item in .cr-hero-inner. Fix: .cr-prompt → position:relative + z-index:3 (flex-item z-index applies in the flex column), so the whole prompt box + its menus paint above the pills. VERIFIED: opened aspect menu, found the overlapping pill ("Faceless video"), elementFromPoint at the menu↔pill overlap = MENU (was the pill before). Both hero dropdowns benefit. — Why: backdrop-filter stacking context traps abs-positioned children; lift the context, not the child.
2026-06-16 — Hero controls: removed Model, added Art style (designer asked "remove models — now what about art style / music / SFX / subtitles?"). DESIGN PRINCIPLE applied (gave it as a recommendation, designer chose "Aspect + Art style"): the hero prompt bar holds GENERATION-TIME choices only — things you can't fix after the render (aspect ratio, art style). Music / SFX / subtitle-style are POST-PRODUCTION layers (you'd want to see the cut first → editor territory), AND the hero copy already promises BlueAI auto-adds voice + captions → cramming 5 dropdowns upfront clutters the bar + contradicts the "one shot, just describe it" pitch (Sora/Higgsfield keep their bars minimal for the same reason). Model dropped too: without the (removed) Models section it was confusing engine jargon (v4 Fast / Cinematic 4K). IMPL: creator-data HERO_MODELS → HERO_STYLES [Cinematic/Anime/3D animation/Cel-shaded/Claymation/Realistic, each + a descriptor hint]; CreatorStudio context model/setModel → style/setStyle (default 'Cinematic') + Gen.model → Gen.style; the chosen style now TAGS each generation (library card + share-modal meta show the style, e.g. "Just now · Cinematic" — better than the old fixed "BlueAI v4"); CreatorHero swaps the model CreatorSelect → style CreatorSelect (Sparkle trigger icon, heading "Choose a style", name + hint rows), aspect picker unchanged, style first / aspect second. VERIFIED: tsc EXIT 0; DOM — 2 triggers [Art style=Cinematic, Aspect ratio=Auto], no Model; style menu portaled (not clipped), heading + 6 named rows w/ hints; selecting Anime → trigger "Anime" + menu closes (writes to context → tags gens). — Why: a creative-tool hero should be powerful but uncluttered; the generation-time-vs-post-production split is the clean rule for what earns a slot in the bar. CANDIDATE TASTE RULE for audit: "hero/prompt bar = generation-time creative controls only; post-production layers (music, SFX, captions, voice) live in the editor or are AI-automated."
2026-06-16 — Dropdown menu clipped at the bottom (designer screenshot: the open aspect menu's last row "Widescreen 16:9" was cut off). Root cause: .cr-hero has overflow:hidden (bounds the WebGL canvas) → an absolutely-positioned menu inside it gets clipped where it extends past the hero's bottom edge. The z-index fix (prev entry) stopped sibling-paint-over but couldn't escape an ANCESTOR's overflow clip. Fix: CreatorSelect now renders the menu via React **createPortal to document.body** with position:fixed computed from the trigger's getBoundingClientRect (top=rect.bottom+6, left=rect.left, minWidth=max(triggerW,200)); closes on outside-mousedown / Escape / scroll / resize (the outside-click handler excludes both the trigger and the portaled menu). The menu + option CSS (.cr-select-menu/.cr-select-head/.cr-select-opt/.cr-opt-*) was made GLOBAL (dropped the .v-creator prefix, z-index 1000, removed the absolute position/top/left — inline fixed handles it) since the portal renders OUTSIDE .v-creator; safe because the --bai-* tokens are :root-global. Trigger CSS stays .v-creator-scoped. VERIFIED: tsc EXIT 0; DOM (click aspect trigger → 250ms render wait → read): menu portaled=true (parent===body), 4 opts + header, rect bottom 719 ≤ viewport 1018 (NOT clipped), elementFromPoint at the bottom row = inside the menu (on top, pills don't cover), bg rgb(255,255,255), z-index 1000. Both hero pickers use the component → both fixed. — Why: an ancestor overflow:hidden can't be defeated by z-index; portaling the menu out of the clipping context is the robust fix (standard popover pattern).
(empty — last promoted 2026-06-13, S6 audit: 13 entries → taste 30–33, 6 decisions, KB S6 section, evolution S6, project-insights routes, shared reasonings ×4)
